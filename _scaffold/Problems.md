# Problems

Running ledger of blockers, bugs, friction, anything broken. Tapped before & after every move.

---

## 2026-05-05

- **after [per-lead memory as files — Atoms 8 + 9 + 10]:** All done. Watch items: (a) **Atom 8 took the additive path** rather than full SQL→file swap. The Discovery page now shows BOTH surfaces. Operator can compare. Full swap deferred until file-backed surface proves better in practice. (b) **`lead-facts.ts` and `journey-score.ts` are dead-code-but-load-bearing** — the SQL queries return empty when table is missing. Don't delete until ready to remove the SQL-backed Discovery surface entirely. (c) **`supabase/_deprecated/` is a new convention** — files outside `migrations/` aren't applied by Supabase CLI. Worth verifying with `supabase migrations list` if Jake ever uses the CLI to be sure. (d) **Markdown rendering is `<pre>` not real markdown** in the new Discovery panel — readable, ugly. Could add `react-markdown` later if Jake wants polish. (e) **Personal page swap deferred** — different aggregate shape, not load-bearing for demo. When ready: Octokit list of `_leads/active/` folders + parse each `06_discovery.md` frontmatter (or count files with `from_hash` set) for portfolio stats. (f) **Cron does NOT auto-trigger LLM regen** by design — manual button only until quality confirmed. After Andre demo, decide whether to wire regen into the cron sweep tick. (g) **`lead_facts` table** if applied to live Supabase: Jake has the DROP statement in `supabase/_deprecated/README.md` whenever he's ready. Empty table is harmless; dropping is safe.

- **after [per-lead memory as files — Atom 7]:** Done. Watch items: (a) **First-time regen on every lead is N×2 LLM calls** — for ~50 leads that's ~100 OpenAI calls for the first push of the button. Subsequent runs skip-by-hash so cost flatlines once content is stable. Tolerable for a real org's volume. (b) **Hash extraction is regex-based**, not a YAML parser — robust against ordering/spacing but a stray `from_hash:` in the document body would false-match. Mitigation: only match in the frontmatter block (start of file, between fences). Tested. (c) **24k-char input cap on verbatim** — long lead histories truncate. The truncation marker is preserved (`_(truncated)_`) so the LLM knows. If a lead's verbatim is genuinely huge, the most recent comms get clipped (verbatim is sorted asc, ascending). For comms-heavy leads this could mean the LLM misses the latest content; switch to descending if it bites. (d) **Concurrency-1 against OpenAI** — full regen of 50 leads at ~3-5s per call = ~5 minutes serial. Acceptable for a manual button; if cron auto-triggers regen later, parallelize to 3-5 lanes. (e) **Discovery output is currently free-form Markdown table** — Atom 8's Discovery page will need to parse it (regex over the table rows, or just render as Markdown verbatim). Rendering verbatim is simplest; structured-parse is for the chat agent's `lead_journey_score`-equivalent reads. (f) **No retry on OpenAI 5xx** — error gets recorded in summary; next regen attempt picks it up. (g) **Voice gate not yet enforced** — the system prompt has voice rules but no post-output linter. If a regen produces "We're excited to..." prose, it lands as-is. Atom 7.5 = pipe through existing `applyVoiceGate` if needed. (h) **Cron doesn't trigger regen** by design — manual only until quality confirmed. After Andre demo, decide whether to auto-include in cron or keep manual.

- **after [per-lead memory as files — Atom 6]:** Clean. Watch items: (a) **No unit test added** — the helper is a thin wrapper around already-tested `readLeadFile` / `writeLeadFile` primitives. Behavior surfaces in the first webhook-triggered fire on prod. (b) **`last_dirty_at` is a new field on `00_meta.json`** — sweeper's renderer doesn't yet emit it (will get overwritten on next sweep). That's actually fine — sweeper resetting `comms_dirty: false` is the desired state after sweep, and `last_dirty_at` survives because the renderer doesn't include it in its meta object. Wait, actually it WILL be lost — the renderer overwrites the whole file. Acceptable: the dirty signal's value is "is this lead waiting for sweep?" which the sweeper resolves on every tick. The `last_dirty_at` is only useful between webhook-fire and next-sweep, which is exactly the window the field exists in. (c) **No counter for "how many dirty flips since last sweep"** — could be useful for prioritization later. v1 = boolean. (d) **Webhook handler now does 2 GitHub round-trips** — read meta + write meta. ~500ms latency added. Still well under Close's 10s timeout but worth noting if other GitHub calls layer on top.

- **before [per-lead memory as files — Atom 6]:** Risks. (a) **GitHub round-trip in webhook handler** adds ~250ms latency to webhook ack. Close's webhook timeout is 10s so we have margin, but if GitHub 5xx's slow this down further, Close might retry → duplicate dirty flips → harmless (idempotent write) but logs noise. (b) **No folder = no flip** — first webhook for a brand-new lead does nothing because the folder isn't materialized yet. Acceptable: sweeper picks up the lead on next 2h tick anyway. Could pre-create the folder from the webhook but that means full hydration in webhook latency budget = too much. (c) **`comms_dirty: true` doesn't yet drive sweeper priority** — sweeper still iterates all leads in scope. The flag is informational; an Atom 6.5 could later sort dirty-first or skip clean-and-recent leads. v1 = just the truth. (d) **Dirty flip writes a commit to `leads-data`** on every webhook → busy days = many commits. Idempotent (no commit if content unchanged) so identical flips at `comms_dirty: true` no-op after the first. (e) **`writeLeadFile` requires `leadName`** — webhook only has `lead_id`. Need to read `00_meta.json` first to get the name (cheap, single fetch), then write back. If `00_meta.json` is absent we skip per (b).

- **after [ship everything to main]:** Pushed. Vercel building. Watch items first 24h: (a) cron will fire on its 2-hour cadence — `0 */2 * * *` UTC. First fire may surface schema-mismatch bugs (status_label exact strings, lead-id format edge cases) since this is the first real Close + GitHub round-trip. Errors land in cron response JSON; if Vercel function logs surface them, check there. (b) Manual `/test` button is the operator's escape hatch — Andre or Jake can re-fire any time. (c) Lead-assets migration `20260505210000_lead_assets.sql` is in the tree; if not applied to Supabase yet, the new asset upload routes will 500. Confirm in `/settings` env grid + manual SQL apply if needed. (d) `lead_facts` empty table still in Supabase, intentionally — drop coordinated with Atom 9 cleanup, NOT before.

- **before [ship everything to main]:** Risks. (a) Pushing parallel-agent code I haven't reviewed (workflow polish + lead-assets). Authorized by Jake explicitly. (b) Once Vercel deploys, the 2h sweep cron activates. First cron tick may surface bugs — Andre demo could be mid-cron-run. (c) `lead_facts` table exists in Supabase (Jake ran the migration); `journey-score.ts` still queries it. Empty table = empty Map = fine. Removing the table = errors. Jake aware not to drop it yet. (d) tsc clean is the only pre-flight check; vitest skipped for speed. The lead-memory libs have 105/105 from earlier; parallel-agent code untested by me.

- **after [per-lead memory as files — Atom 5] — code phase:** Mid-flight pivot to keep auth simple. Initial draft of the cron route had its own dual-auth (Bearer OR operator cookie) but `src/middleware.ts:31` already gates `/api/cron/*` with Bearer-only. Operator-cookie sweeps would either need middleware change OR a different route. Picked the latter: manual button routes through `/api/test` (existing operator-session matcher), cron route stays strict Bearer. Cleaner separation. The single-lead-id query param survived: `/api/cron/sweep-leads?lead_id=lead_xyz` for targeted re-sweep when debugging. Watch items: (a) **Manual button result is single-shot** — appears after the full sweep completes (60-90s). UX feels long. SSE streaming could come later; for v1 the loading spinner + delayed result is acceptable. (b) **`testLeadSweep()` truncates `swept[]` to first 25 entries** for the result pane — full data still flows back from the cron endpoint when Vercel runs it. If Andre's set ever exceeds 25, the result pane shows partial; tune cap if needed. (c) **`CRON_SECRET` is required for the cron route** but **optional for `/test`** — middleware lets `/api/cron/*` pass when `CRON_SECRET` is unset (dev convenience), so smoke from local dev works without configuring the secret first. (d) **First push of `leads-data`** is the actual remote-state change pending Jake's call. Safe (new branch, never merges, Vercel doesn't watch) but per CLAUDE.md must ask. (e) **`vercel.json` cron entry will only fire after a deploy** — adding the entry to the local file does nothing until `main` is pushed. So the cron is effectively dormant until everything ships together; manual button is the only smoke path until then.

- **before [per-lead memory as files — Atom 5]:** Risks. (a) **Vercel function timeout** — default 10s, max 60s on Pro plan, 900s with `maxDuration` config. Full sweep of 50 leads × ~1s = 50s. Adding `export const maxDuration = 300` (5 min) to the cron route gives margin. (b) **Pushing `leads-data` to origin** — first remote push of new branch. Safe (never merges into main; Vercel ignores it because Vercel only watches `main`) but irreversible without manual delete. Worth a one-line confirmation before push. (c) **`CRON_SECRET` collision with existing env conventions** — currently no `CRON_SECRET` in env.ts; need to add it as a new key. Vercel Cron sends `Authorization: Bearer ${CRON_SECRET}`. Must compare-with-`Bearer-prefix-stripped` or compare full string carefully. (d) **Manual button uses operator session, not `CRON_SECRET`** — two auth paths to the same route: cron header for Vercel, operator cookie for `/test`. Need both branches. (e) **First sweep cost** — ~50 leads × 1s = 50s of GitHub writes. Visible in result pane only after completion. UX-wise: result appears all-at-once after a long wait. Could stream progress via SSE later; v1 = single-shot response. (f) **No way to scope to a single lead from the button** — sweeps everything in scope. If smoke fails on one lead, debugging means reading the `errors[]` for that lead's message. Could add a `?lead_id=` param to the route for targeted re-sweep; one-line addition.

- **after [per-lead memory as files — Atom 4]:** Clean. One signature mismatch caught and fixed during build: `closeListLeadsByAssignee(userId, limit)` takes positional limit, not options object. Watch items: (a) **Cumulative commits on `leads-data`** — accepted at v1; Git Data API tree-build refactor available if log noise bites. (b) **`comms_content_hash` doesn't yet short-circuit render** — sweeper does file-by-file byte diff after render. Render is cheap enough not to bother optimizing. (c) **Per-lead writes are sequential** — ~250ms × 3-5 files = ~1s per changed lead, 50 leads × 3 lanes ≈ 80s full sweep. Tolerable for cron; manual button will show progress. (d) **`sweepActiveLeads` no-ops when `CLOSE_USER_ID_ANDRE` is unset** — returns empty result. Already set in `.env.local`. (e) **No transient-error retry on `sweepLead`** — recorded in `errors[]`, next cron pass retries. (f) **`isLeadInScope` ownership test silently skips when env unset** in test runner — acceptable for behavioral coverage of the status-filter primary path.

- **before [per-lead memory as files — Atom 4]:** Risks. (a) **Per-file commits could spam the git log** at scale — 50 leads × 3 changed files = 150 commits per sweep. At 12 sweeps/day that's 1800 commits/day on `leads-data`. Manageable for git but visually noisy. Mitigation noted as a watch item; Git Data API one-commit refactor available if needed. (b) **First-sweep cost** — brand-new lead with 50 activities = 1 listing + 50 file writes = ~51 GitHub calls. Well under 5000/hr but adds latency. (c) **Status filter assumes `status_label` is human-readable** with emoji prefixes (`🔴 Lost`, `✅ Won`, `🔴 Not Interested`); need to confirm exact forms in Andre's org during smoke. (d) **Per-lead errors should not abort the sweep** — try/catch each `sweepLead`, push to `errors[]`, continue. (e) **Race with webhook** — webhook flips `comms_dirty: true`, sweeper writes `comms_dirty: false`; if webhook fires DURING sweep, its dirty signal may be lost. Tolerable v1; Atom 6 webhook handler can mitigate via SHA-conditional write. (f) **`closeListLeadsByAssignee` paginates** — caps at 200 in one call; expand if Andre's active set exceeds.

- **after [per-lead memory as files — Atom 3]:** Mostly clean. Two minor friction points hit during build, fixed in-flight: (1) test attempted `as CloseActivity` cast on an inline activity literal and TS rejected the narrowing (cast removed, `activity()` factory's `as CloseActivity` carries the index signature). (2) First test of `activityFilename` had a typo expecting filename without `.json` suffix — caught by vitest, corrected. Watch items: (a) **`stripHtml` is naive** — handles common Close email HTML (paragraphs, line breaks) but won't render tables or lists nicely. Falls back to `body_text` first which Close almost always populates, so `stripHtml` is a "last resort" path; if Close ever sends emails with only HTML body, the verbatim file will look slightly off but readable. (b) **Date format `YYYY-MM-DD HH:MM`** in markdown tables — fine for LLM consumption but operators reading the digest will see UTC, not local time. Acceptable. Could swap to localized format in a later UX pass. (c) **`previewOf` truncates at 80 chars + `…`** — long subjects render with ellipsis. Fine for a digest table; the verbatim file has the full text. (d) **`stripHtml` doesn't decode all entities** — handles `&nbsp;`/`&amp;`/`&lt;`/`&gt;` but not `&#39;`/`&hellip;` etc. Sufficient for v1; expand if a real email surfaces them. (e) **No fixture-based golden test** — chose to assert structural properties (file presence, content patterns) rather than diff against a literal expected blob, because the blob would be brittle to format tweaks. Trade-off acknowledged.

- **before [per-lead memory as files — Atom 3]:** Risks. (a) **`comms_content_hash` must be deterministic** — used by Atom 7 to skip LLM regen when nothing changed. Hash input = sorted concatenation of activity_ids + their date_updated, NOT the full verbatim content (which has timestamps that drift due to Close's `date_updated` updates on read). Detail to get right. (b) **Activity ID short-form for filename** — Eliana used 8-char tails. Need to verify uniqueness within a single lead's comms folder; collision would clobber. Mitigation: include `_type` prefix (`call_` / `email_` / etc.) in filename + use 8 chars (last segment of acti_id). Worst-case collision detection: file-by-file, fallback to longer suffix. (c) **`status_label` from Close** is currently in lead's custom fields or a separate status object — need to check `closeGetLead` shape to know which field holds the human label. If it's missing, we render `status_id` and let Atom 4's later sweep enrich. (d) **Email body sanitization** — Close emails have both `body_text` and `body_html`. Verbatim file should render `body_text` (clean for LLM); raw JSON in `comms/` keeps both. Renderer must handle nulls cleanly (some emails have only HTML). (e) **No profile/discovery files** in this atom — those are LLM regen, Atom 7. Renderer just doesn't emit those keys. Test the absence as a deliberate behavior so Atom 7's additive layer doesn't surprise.

- **after [per-lead memory as files — Atom 2]:** Clean. Watch items: (a) **Worker pool implementation** — `enrichCalls` uses a shared cursor + N async workers competing for the index. Standard pattern, no race because the cursor is read+incremented synchronously per JS turn. Tested only via type/unit; first real exercise at Atom 4 sweeper. (b) **Per-call GET failures swallowed silently** — if Close 5xx's a call lookup, we fall back to the trimmed row with no log. Sweeper (Atom 4) will need a way to surface this in `client_ledger.md` ("3 calls had transcripts withheld due to upstream errors") rather than letting it disappear. Add an `enrichmentErrors[]` field to `LeadHydration` if Atom 4 needs it. (c) **`bucketActivities` exported as pure function** so the renderer in Atom 3 can re-bucket if it wants its own grouping; currently it'll just consume the buckets `hydrateLead` already produced. (d) **No test for `enrichCalls` worker pool** — pure unit testing requires mocking `closeGetCall`, which is straightforward but felt like over-engineering when Atom 4's smoke will exercise it end-to-end against real Close. If the worker behavior surfaces a bug we'll add a vi.mock test then.

- **before [per-lead memory as files — Atom 2]:** Risks. (a) **Per-call GETs amplify request count** — a lead with 30 calls = 30 extra GETs on top of the bulk fan-out. Mitigation: concurrency cap 5 + we only enrich activities where `_type === "Call"`. (b) **Recordings expire 30 days after creation** (saw `recording_expires_at` in Eliana's payload). If sweeper first runs against a lead with calls older than 30d, `recording_url` 404s but `recording_transcript` (text) still comes back from the call object. We persist the transcript text — that's the durable artifact. (c) **`closeGetLeadFull` caps activities at 500** — adequate for any single lead I'd expect at Comeketo's scale, but for a lead with massive history (100+ thread subscribers, etc.) we could miss older comms. Acceptable for v1; add `maxItems: 2000` override for archive sweeps if it bites. (d) **No retry on Close API 5xx** — Close occasionally 502s under load; current `closeFetch` doesn't retry. Atom 4's sweeper will need to handle that gracefully (skip-and-continue, log skip code) but that's not Atom 2's concern. (e) **`closeGetCall` exposes raw payload** — includes `note` / `note_html` / `outcome_autofill_reasoning` (Close's own AI summary) which is gold for the LLM but also includes operator metadata (`user_id`, `dialer_id`, etc.) that the renderer should keep in the verbatim file but not in the digest. Renderer's job to filter, not hydrator's.

- **after [per-lead memory as files — Atom 1]:** Clean execution. tsc green; 78/78 vitest green (13 new + 65 prior). One in-flight design decision: smoke test was promised as Atom 1 done criterion but **deferred to Atom 5** because it requires pushing `leads-data` to origin, which Jake explicitly deferred to "until everything's done." Atom 1 is therefore done at the type-and-unit-test boundary; first real GitHub round-trip happens when `/test` gets a sweep button. Watch items going forward: (a) **`writeLeadFile` retries on 409 AND 422** — GitHub returns 422 for SHA mismatch in some path shapes (the docs say 409 but observed 422 in production for create-or-update collisions); double-handled to be safe. (b) **`findLeadFolderPath` scans active then archive in series** — fine for ~50 leads but a portfolio aggregate (Atom 8 `/personal`) calling this 50× sequentially = 100 GitHub calls. Will rewrite as a one-shot top-level listing in Atom 8 if it bites. (c) **No request-scoped cache yet** — back-to-back `readLeadFile` calls for the same lead re-fetch the folder listing each time. Cheap fix later (memoize per-request via `react`'s `cache()`); not blocking. (d) **Decoder swallows non-base64 payloads silently** — `readLeadFile` returns null when GitHub responds with a different encoding. GitHub Contents API always returns base64 for files <1 MB so practical risk is zero, but if we ever store a >1 MB file the API switches to "this is too big, fetch via Git Data API" and our reader returns null misleadingly. Add explicit error path if Atom 4's renderer ever emits >1 MB output (unlikely for markdown).

- **before [per-lead memory as files — Atom 1]:** Risks. (a) **Smoke test requires `leads-data` to exist on remote** — Octokit's `getContent`/`createOrUpdateFileContents` with `ref: "leads-data"` will 404 if the branch isn't on origin. Resolution: push `leads-data` once before running the smoke. Acceptable scope creep for this atom since the branch is a new ref, never merging into main. (b) **`archiveLead` non-atomic** — N files = 2N commits (write to archive path, delete from active). Fine for low-frequency Won/Lost transitions; if a sweep crashes mid-archive, lead's files split across both states. Tolerable: next sweep will see two folders for one lead and can recover. Better fix is Git Data API tree-build, deferred to Atom 4 if it bites. (c) **GitHub API rate limit** — 5000 req/hr per token. Smoke test = ~3 calls (write, read, list), well under. Concern surfaces only if Atom 4's full sweep storms the API; mitigation = concurrency cap. (d) **Octokit lazy-load already proven** in `auxiliaries-runtime.ts:218`; copying the same pattern (`await import("octokit")` inside the function) is overkill here since Atom 4 will hot-path call this on every cron tick — top-level import is fine. Take the readability win.

- **after [per-lead memory as files — Atom 0]:** Stash dance worked clean: `git stash -u` → `git checkout --orphan leads-data` → `git rm -rf .` (clear orphan index) → scaffold 3 files → root commit `2bc00dd` → `git checkout -f main` → `git stash pop`. The `-f` was needed on the return because the orphan branch saw all `src/` files as untracked and git's safety check refused to clobber. No data loss — files were already at main HEAD content. Watch items: (a) **Pushing `leads-data` is pending Jake's call** — it's local-only. Without push, Jake can only verify via `git log leads-data` rather than github.com. (b) **`_leads/` is gitignored on main now** — if a future agent runs the sweeper while accidentally on main checkout, files write to disk but git won't see them. Cleanup if it ever happens: `rm -rf _leads/`. (c) **The `archon/thread-*` branches** visible in `git branch -a` are unrelated parallel-agent worktree state, not Atom 0 concerns.

- **before [per-lead memory as files — Atom 0]:** Risks. (a) **Working tree is dirty on main** — 12 modified + 2 untracked files from in-flight workflow polish. Atom 0 needs `git stash -u` → orphan branch → push → switch back → `stash pop` to keep that work intact. (b) **Orphan branch trap** — `git checkout --orphan` keeps the working tree, must `git rm -rf .` before scaffolding to get a truly empty `leads-data` tree. (c) **Pushing `leads-data` is a *new* remote branch** — not a violation of the "don't push main" rule, but Jake should see it appear on github.com to verify Atom 0 done. `.vercelignore` on main is local-only, no push. (d) **MCP 404 on Sakamoto** earlier this conversation confirms MCP can't be the sweeper read path (filters Not-Interested + ignores `lead_id` on activity_search). Direct Close REST only. (e) **Decision pending in Atom 9** — `lead_facts` migration file gets deleted; if any agent has been editing `journey-score.ts` or `lead-facts.ts` in parallel, conflicts surface there.

- **after [workflows — AI authors rich XHTML emails + asks clarifying questions]:** No bugs. tsc clean. Watch items: (a) **No XHTML validator yet** — the sub-LLM is trusted to follow Close's tag-subset rules. First time the agent emits malformed HTML, Close's template may render oddly; add a `parse5`-based linter pass before sending if it bites. (b) **Voice gate skips body_html** — `applyVoiceGate` runs only over plain-text fields. A perform-y phrase in an HTML paragraph won't be caught. Practical risk is low because the prompt rule is explicit, but watch real outputs. (c) **No "regenerate just the HTML" tool path** — operator wanting to richen ONE email step has to go through the full `propose_close_workflow` refinement loop, which may rewrite other steps. If this becomes friction, add a narrower `enrich_email_step({ step_index })` chat tool. (d) **Clarifying-questions behavior is prompt-only, no schema enforcement** — the model can still call the tool blind. We'll see how it behaves on Jake's first real demo run; the rule + good/bad examples should be enough but may need tightening. (e) **Token cost up** — the sub-LLM prompt grew from ~600 tokens to ~1300 tokens (XHTML rules + example + voice rules). Per workflow chat turn, this is fine; if Andre runs many refinements, it adds up. Acceptable; revisit if cost surfaces. (f) **`body_html` flows through `automation_drafts.workflow_json`** with no DB migration needed (the column is `jsonb`); existing drafts without `body_html` keep working — the field is just absent.

- **after [workflow chat polish — composer + slash + multi-choice]:** No bugs. tsc clean. Watch items: (a) **Slash command intent text vs. operator's preference** — slash commands inject INTENT prefixes (e.g. `/branded` → "Add a branded HTML email — multi-section, structured headings, our voice. ") that the operator can edit before sending. If they don't edit, the LLM authors based on the prefix alone, which may not match their actual ask. Acceptable for v1; if it produces generic results, we tighten the prefixes. (b) **Question card chips don't show the question content as the user message after click** — the chip TEXT becomes the user message, which is correct, but the operator visually loses the question once they answer. May be confusing if they want to verify which question they answered. Could keep the chip selected/highlighted as "answered with this" instead of just disabling. v1 acceptable. (c) **No XHTML validator** — sub-LLM might still emit invalid XHTML; surfaces only at publish time when Close 4xx's. Prior watch item from rich-XHTML move still active. (d) **No way to skip a question** — chips and freeform are the only paths; if the operator wants to ignore the question and ask for something different, they can just type it (the textarea remains unlocked) but the chips persist as a stale card. Tolerable. (e) **`+` button behavior** — clicks open slash mode (cycles input to `/`). If operator expected a file picker (Jake's nudge in the original ask), it's not that yet — file attach is the explicit follow-up move. (f) **Mid-session WorkflowAuthor.tsx revert during the previous Write** — appears Jake had the file open in his IDE during my earlier Write, and his auto-save reverted it. Lesson: for files Jake might be editing in parallel, prefer surgical Edits over full Write. Captured here in case it happens again.

- **before [workflow chat polish — composer + slash + multi-choice]:** Risk: medium. Touches the same prompt + tool surface as the just-shipped clarifying-questions move; risk is the new `ask_operator` tool creating ambiguity for the agent (when to ask in plain text vs invoke the tool). Mitigation: explicit prompt rule — clarifying questions ALWAYS go through `ask_operator`, never plain text. Slash commands risk pre-canned templates that read as "AI gave me canned options" instead of authored work — mitigation: slash commands prepend operator-INTENT (not template text) so the LLM still authors the body; operator can edit before sending. Composer redesign is pure CSS + JSX restructure inside the existing `cmk-wfa-*` namespace, low risk.

- **before [workflows — AI authors rich XHTML emails + asks clarifying questions]:** Risk: medium. Three files; behavior change but no schema migration. Risks: (a) **Sub-LLM may emit invalid XHTML** — broken tag nesting, unclosed `<img>`, disallowed tags; Close will accept the template but rendering may misbehave at email-delivery time. v1 mitigation: trust the sub-LLM's prompt rules; if it fails in practice, add an XHTML linter pass before sending to Close. (b) **NEPQ voice gate currently runs on `body_text` only** — `body_html` could contain perform-y language inside tags and slip through. v1 mitigation: prompt rule says "voice rules apply to body_html too"; if model violates, follow up with a stripped-text NEPQ pass over rendered text. (c) **Clarifying-question behavior depends on the outer agent reading the prompt rule** — the model may still call the tool blind for ambiguous instructions if context window is tight or it's pattern-matched on the prior turn. Watch first few real runs; tighten prompt if needed. (d) **The example email in the sub-LLM prompt could over-anchor** — if the model treats the 45-day timeline as a template and produces it for unrelated requests, that's bad. Prompt explicitly says "use as a reference for shape, not as boilerplate." Watch.

- **before [workflows list — bulk delete + per-card delete]:** Risk: low. Pure server-action additions + UI affordances. Watch: clicking a per-card × shouldn't navigate into the draft (the card is a `<Link>`); need to stop event propagation in the button handler. Bulk-delete sweeps any draft where the non-wait node count is 0, so a draft with only a wait node would also be cleared (acceptable — a wait-only workflow does nothing).
- **after [workflows list — bulk delete + per-card delete]:** No bugs. tsc clean. Watch items: (a) **Per-card × uses `e.preventDefault()` + `e.stopPropagation()`** — both required because the × is nested inside a `<Link>`; without them, click → navigate-to-draft + delete-the-draft race. Tested mentally; verified pattern is right. (b) **`confirm()` and `alert()` are native browser dialogs** — match what the authoring page already uses but feel a bit clunky vs the rest of the app. Future polish: swap to a small in-app confirm modal (the cockpit doesn't need one, but as the app grows it's an easy win). (c) **Optimistic local hide + router.refresh()** — if the server action fails, the card un-hides via `setHidden(false)` in the catch block; the user gets an alert. Failure mode is honest. (d) **`emptyDraftCount === 0` hides the Clear button entirely** — operator never sees a useless button, but it appears/disappears as drafts come and go. Subtle but desired. (e) **Bulk delete does NOT touch published drafts** (`.is("close_sequence_id", null)` filter on the SELECT) — protects against accidentally nuking published-but-zero-step drafts (shouldn't exist, but belt-and-suspenders).

- **before [workflows author — match cockpit aesthetic]:** Risk: low. CSS-only rewrite of an already-isolated `cmk-wfa-*` namespace + 1 small JSX addition. No data risk, no logic change. Watch: the `cmk-wfa-msg-system` rose tone uses the same idiom as cockpit error messages but the workflow surface might benefit from a lemon/peach tone instead (rose reads "danger" — system errors are more "heads up"). Defer until Jake sees one in the wild.
- **after [workflows author — match cockpit aesthetic]:** No bugs. tsc clean. Watch items: (a) **Mobile breakpoint untouched** — the existing `@media (max-width: 900px)` rule still stacks chat above graph at 50/50 vertical split. The new chat-head eyebrow strip will sit above the messages on mobile too; reads fine in head check but worth a real-device pass when Jake gets to that. (b) **`plan-btn-primary` removed from Send only** — the page header still uses `plan-btn-primary` for the "Publish to Close" button. Intentional: that's the operator-action button (matches the rest of the app's primary-CTA chrome). The composer Send is a chat-action button (matches cockpit). Two different intentions, two different chromes. (c) **Step count in eyebrow shows raw `wf.nodes.length`** — counts wait nodes too, so a 2-touch workflow with a wait node reads as "3 STEPS". Honest but slightly misleading; if Jake wants "actionable steps only," filter out `kind === "wait"` from the count. (d) **Eyebrow `WORKFLOW · DRAFT` may visually duplicate the header status pill** — the header has `Untitled workflow [DRAFT]` already. Two DRAFT signals in close proximity. Could drop the status pill since the eyebrow now carries it; deferring until Jake sees both side-by-side. (e) **The cockpit uses Fraunces serif on tool-result card bodies** for an editorial feel; the workflow author chat keeps sans 11.5px body to match user messages. If Jake wants the agent's responses to feel more "considered," swap the assistant body to serif 13px. Cheap one-line change.

- **before [workflows publish fix — templates + delay]:** Risk: medium. Touches the same file (`manifest-to-close.ts`) plus `close.ts` (new helpers) plus `actions.ts` (publisher becomes async-with-side-effects). Risks: (a) **Template name collisions** — if a template with the same name already exists, Close may 409 or return a different id; mitigation: append a short hash of the workflow id + step index to template names so they're always unique-per-publish. (b) **Orphaned templates on partial failure** — if we create email template successfully then SMS template fails, the email template lingers in Close. Acceptable for v1 (operator can clean up); v2 could rollback or re-use existing templates by name. (c) **Re-publish (`Re-publish` button on already-published draft)** — should it create new templates each time or reuse existing? v1: just create new (drafts after a re-publish leak templates). Note in watch.
- **after [workflows publish fix — templates + delay]:** No tsc errors. End-to-end Close API fire pending Jake clicking Publish. Watch items: (a) **Template name collisions still possible** — if Andre publishes the same draft twice without renaming it, the second publish appends a fresh suffix only via step index; the draft-id slug stays the same, so `"Untitled workflow · email 1 · a3f9b2-1"` would attempt to be re-created. If Close 409s on duplicate name, we'll see it on first re-publish and need to add a timestamp/random tail to the name. Cheap fix when it surfaces. (b) **Orphaned templates on partial publish failure** — if email template create succeeds for step 1 but step 2's sms create fails, step 1's template lingers in Close. v1 accepts this; operator can clean up via the Close template list. (c) **Re-publish leaks templates** — every re-publish creates fresh templates instead of overwriting. Templates accumulate. Real but unblocking. (d) **`is_shared: true` default** — templates show up in Andre's org-wide template picker. Intentional (so he can see them) but if any operator wants private-by-default, flip the default. (e) **Step text on call steps is inline (not a template)** — Close's call step accepts inline text, so this is correct, not an oversight.

- **before [workflows — implicit draft_id]:** LLM was asking for / fabricating draft_ids. Surfaces as: agent asking the operator for an id, then a Postgres `invalid input syntax for type uuid` when a Close `seq_…` id is pasted. Fix in-flight.
- **after [workflows — implicit draft_id]:** Resolved. The LLM no longer sees `draft_id` as a parameter, so it can't ask for it or fabricate one. Watch: the dispatcher still accepts a `draft_id` in args as a soft fallback (server-bound id always wins) — defensive belt-and-suspenders.

- **after [AI-built Close workflows]:** No bugs surfaced during build. tsc + build clean on first try. Watch items: (a) **Close step_type strings are unverified** — used `"email"` / `"sms"` / `"call"` based on the Close API docs page Jake screenshot'd, but the actual sequence step API may want different keys (e.g., `"create_call_task"` instead of `"call"`, or distinct step_type per delivery method). First publish attempt will reveal — if `closeCreateSequence` returns 4xx, tune `manifest-to-close.ts` step builders. (b) **Sub-LLM JSON parsing is best-effort** — extracts `{...}` between first `{` and last `}` then parses. If the model wraps output in code fences with text before/after, we still grab the JSON, but a malformed response shows up as "Sub-LLM returned no JSON object". Acceptable for v1; could harden with response_format JSON mode later. (c) **In-memory chat history** means a tab refresh loses the conversation — the draft persists, the chat doesn't. Per the locked default. (d) **Publish doesn't gate on operator session for Close-side scope** — it gates on the local operator cookie via `assertOperatorSession`, but the Close API call itself uses the API key from env, so any operator with the cookie can publish. Fine for our single-user threat model. (e) **No "save" affordance** — every chat turn auto-saves the draft. If operator wants to undo, they have to ask the agent to restore the previous shape (or re-author). Versioning is Phase 2. (f) **`closeListWorkflows` may return all sequences in the org** including legacy ones not authored here — the list page shows them all under "Published in Close" which is honest, but operators may want a filter (only ours / all / by-name).

- **before [AI-built Close workflows]:** Risk: medium. Sub-LLM call for structured-output workflow generation is the riskiest piece — needs hard JSON Schema validation on the way back to keep `workflow_json` shape sane. Close API step shape is partially-typed in our codebase (`CloseWorkflow.steps` are `Record<string, unknown>` minus a few common fields); the `workflowToCloseSequence` adapter has to get the step_type strings right per Close's API docs (email_account / sms_account / task_account types — confirm at first publish). Watch: `automation_drafts` table is assumed live (no migration in repo); if Supabase row insert errors on first `createWorkflowDraftAction`, the table may need a re-applied migration.

- **before [proposals — tabbed view]:** Risk: low. Pure presentation refactor. Existing forms / server actions unchanged.
- **after [proposals — tabbed view]:** No bugs. tsc clean. Watch: tab persistence uses `localStorage["cmk:proposals:tab"]` — survives reload but doesn't sync between tabs in the same browser; fine for single-operator usage. Mapping `not_ready` into the "Needs review" tab is opinionated — if Jake wants `not_ready` (drafts-not-yet-reviewed) to be its own tab later, add a fifth entry; cheap.

- **before [proposals — tile per job, not per action]:** Risk: low. Forms / server actions / DB unchanged. `ProposalReviewItem` was only used inside `ProposalReviewBoard.tsx` (verified via grep) so the type rename/restructure has no external blast radius.
- **after [proposals — tile per job, not per action]:** No bugs. tsc clean. Watch: (a) `summary_status` priority order is `needs_review > approved > not_ready > sent > skipped` — operationally matches "what should the operator look at first." (b) A plan whose every day is `skipped` lands in the SKIPPED bucket; today the page only renders columns for non-empty buckets beyond needs_review/approved, so it stays hidden — fine. (c) Status-counts chip rail capped to non-zero statuses so it stays compact even on long plans.

- **before [discovery map v1 — NEPQ sales-RPG layer]:** Risks. (a) **Atom 0 schema discovery is load-bearing** — the entire slot taxonomy depends on getting Andre's actual Close custom field names right; if I guess names, Discovery Map shows `unknown` for slots that are actually filled. Mitigation: read the live schema via `mcp__claude_ai_Close__find_lead_custom_fields` (or fetch sample leads), verify with Jake before atom 1 begins. (b) New `lead_facts` Supabase migration on the production Neon branch — coordinate with Jake before applying; fallback is dev branch first. (c) `/lead/[id]/discovery` page must look like a sibling tab, not bolted-on — visual regression check side-by-side with `/box` is part of verification. (d) LLM extraction tool runs against full Box (transcripts + emails) and writes `lead_facts` rows — if extraction over-claims (false positives), operator-source override always wins on re-render so corrections stick. (e) No auto-LLM-extract on Box hydrate (cost): manual `extract_discovery_facts` chat tool + per-page "Run discovery scan" button only. (f) Restraint score derived from rolling skip-breakdown over last N heartbeat runs — needs a sensible window (default 7d) and to not penalize legitimate skips like `OWNERSHIP` for foreign leads (those should be filtered out, not counted). (g) Boss-demo timing: full v1 is ~8 atoms; if any atom blows up, the slice fallback is "Discovery Map only" (atoms 0–2 + a stripped page) — surfaces are independently shippable so partial completion still demos.
- **after [discovery map v1 — NEPQ sales-RPG layer]:** No bugs surfaced; build + tsc + 39 unit tests all green. Watch items going forward: (a) **`lead_facts` migration not yet applied** — the SQL file is checked in at `supabase/migrations/20260505200000_lead_facts.sql` but Jake should apply it on Neon (dev branch first, then prod) before opening `/lead/[id]/discovery` against a real lead — without the table, the page will surface a friendly "Journey score unavailable" error pane (caught at journey-score level) rather than crashing. (b) **Atom 0 schema discovery worked clean** — `find_lead_custom_fields` returned the full org catalog and the 5 catering-discovery fields had clear matches; no field-name guessing happened. The `Tasting Event Party Size` field is included as a fallback hint for `guest_count` (proxy — actual event headcount often differs from tasting headcount); LLM extraction is the primary path. (c) **No auto-extraction on Box hydrate** — Run Discovery Scan button is the only path to write `llm_extraction` rows; cost-bounded but means Discovery Map starts empty for slots without canonical-Close fields until Andre clicks scan. Acceptable for v1; if it feels onerous, a future batch tool could scan all Andre leads on a slow cron. (d) **Operator-source rows always win on conflict** — `upsertLeadFact` short-circuits when an existing row is `operator` source and the incoming write is `llm_extraction`; corrections stick across rescan. Inverse: operator-source writes always overwrite llm_extraction (the correction case). (e) **Restraint window is hard-coded last 10 heartbeats** — meaningful for the page-load metric but if a lead has a single ancient heartbeat with all CLOSE_API_ERROR, that 10-run window will show 0% restraint forever until newer runs land. Could expand to time-bounded ("last 7d") if it bites in practice. (f) **Cockpit chip simplified** — plan called for a `Discovery 6/9` chip on `PlanDayCard` headers, but per-day cards don't carry lead-level data. Shipped as a `discovery →` link in the PlanDayStrip plan-summary card instead (lead-level, additive, doesn't break any existing layout). Numeric chip can be added later if Jake wants it; would need a small client-side fetch of the journey score on cockpit mount. (g) **Quest synth is rule-based, not LLM** — deterministic and predictable for the demo, but phrasing is templated. v2 swap-in is straightforward (same inputs → LLM call → richer language) when Jake is ready. (h) **`extract_discovery_facts` calls OpenAI even when all 4 LLM-only slots are blocked** — early-returns when `slotsToExtract.length === 0`, but if Box has no activity bodies, the model gets an empty prompt and may hallucinate. Mitigation: confidence ≥ 0.6 filter at the parse step plus the explicit "Do NOT invent" rule in instructions. Watch for hallucinated facts in the wild.

- **before [personal scoreboard]:** Risks. (a) **Page-load cost** — naively calling `journeyScoreForLead` per lead would do 50 Close API calls per page hit. Mitigation: dedicated lighter aggregator that uses `closeListLeadsByAssignee` (gets all leads with `custom.*` in one paginated call) + one `lead_facts` Supabase query + one `heartbeat_runs` 30-day query; no per-lead Box fetches. (b) **lead_facts table still not applied** — same migration as discovery v1. The aggregator should swallow Supabase errors gracefully and treat lead_facts as empty rather than crashing, so the page works even before the migration runs. (c) **Quest-theme aggregation needs care** — running `synthesizeQuest` per lead and counting current.slot_id is fine but `synthesizeQuest` is `O(slots × categories)` × N leads = manageable. Skip the full quest synth — just count unknown slots directly across leads (cheaper, same insight). (d) **Restraint over 30d is org-wide for Andre** — different signal than per-lead Restraint (which uses last 10 runs of one lead). Make the metric semantics clear in the UI label.
- **after [personal scoreboard]:** No bugs. Build green after `rm -rf .next` (stale `/api/lead/[id]/plan/simulate` route trace from earlier session needed cache nuke — standard `npm run fresh` situation per CLAUDE.md). Watch items: (a) **Cost discipline holds** — single page load is one Close `closeListLeadsByAssignee` call (paginated up to 200) + one `lead_facts` batch + one `heartbeat_runs` 30d query. With Andre's ~50 active leads this is fast; if pool grows past 200 the cap needs raising. (b) **Lead-facts errors swallowed** — try/catch around the Supabase select returns empty Map on any error, so the page renders pre-migration without crashing. Real Supabase errors (auth, RLS misconfig) won't surface to operator; acceptable since the Close-call failure path does surface a friendly "Scoreboard unavailable" pane. (c) **`top_leads` ranking heuristic** — sorts by 🟢 SCORE tag count desc → clarity desc → date_updated desc. Means leads with no hot-tags fall behind even at high clarity. If Jake wants a different sort (readiness desc, stage furthest-along), it's a one-line tweak. (d) **Pipeline funnel skips `lost`** — terminal stage filtered from display. Lost-lead count would need a separate metric if Jake wants it visible. (e) **`avg_clarity` skips zero-clarity leads** — averages only leads with at least one known slot, so empty Boxes don't drag the average to noise. Reflects "average clarity on leads where we know anything." Honest but worth noting in the UI tooltip if questions arise. (f) **Restraint 30d ≠ per-lead Restraint** — different metric: org-wide for Andre's leads (30d) vs single lead (last 10 runs). UI labels disambiguate (`Restraint · 30d` on personal vs `Restraint` on per-lead).

- **before [proposals plates + workbench overlay]:** Current `/proposals` page is too dense because every card shows full touch detail and controls inline. It becomes visually unmanageable at real data volume. Also modal/workbench patterns are inconsistent across app surfaces. Risk: avoid adding an inert AI chat box; bottom-right AI area must perform actual refine day/plan actions.

- **before [briefing page]:** Briefing concept is currently unbuilt and underspecified. Risk: overpromising “AI insight” without enough signals. Constraint: page must stay honest — use real Close lead recency, current proposal/plan counts, heartbeat truth/execution rows/webhook rows where available, and label any missing signal clearly.
- **after [briefing page]:** No TypeScript/build bugs. Watch: (a) "movement" currently means most recently updated Close leads, not semantic stage-change/reply classification; the webhook/event table can make this sharper later; (b) new leads use last 72h based on Close `date_created`; tune window if Andre thinks differently; (c) Refresh is a normal route reload query link, not client polling — good for at-will summaries, but a future auto-refresh toggle could reuse the existing AutoRefresh pattern.

- **before [proposals review page]:** Missing major product surface: top-right `proposals` is currently just text, but the app needs a daily review/approval workbench for outbound proposal/follow-up drafts. Risk: need to discover existing plan DB helpers before adding new data access. Keep scope to actual functionality: list, status change, edit touch, add touch, refine day, link to lead/graph.
- **after [proposals review page]:** No TypeScript/build bugs. Watch items: (a) first pass groups by day approval status, not by actual heartbeat eligibility/time window; if Andre wants “send today only,” add a today/eligible filter backed by simulator/heartbeat verdicts; (b) edit UI updates via server action + route refresh rather than optimistic local mutation; acceptable for review desk, but add optimistic behavior later if it feels sluggish; (c) broad plan list scans latest 100 active-ish plans, which is fine now but should move to a Supabase view/index if volume grows.

- **before [graph full-canvas + popover correction]:** Current graph still wastes width because the editor is always mounted as a right column. It also has redundant title copy (`scheduled_call cycle`). Risk: small, but the editor is tall; as overlay it needs sane viewport sizing and scroll containment.
- **after [graph full-canvas + popover correction]:** Resolved. Watch: overlay editor is right-aligned on desktop and centered below 1240px; if Jake wants the pop-up to originate visually from the clicked node later, add anchored positioning, but current version restores graph space without reintroducing giant full-screen modal behavior.

- **before [graph map correction]:** UX issue introduced by graph control-board pass: day nodes are too large and content-heavy, so the graph repeats the drawer and cannot be read as a whole topology. Need to restore graph purpose without reverting to tiny meaningless symbols. Risk low: CSS + `GraphDayCard` markup only; server-action/editor logic remains intact.
- **after [graph map correction]:** Resolved at first pass. Remaining design frontier: this is still a plan-day topology, not yet a true multi-node automation topology with Close-native workflow nodes, triggers, and external sequence blocks. The current compact node language leaves room for that next layer without faking non-existent workflow data.

- **before [cockpit Delegations eyebrow becomes the mode toggle]:** Risk: low. `cmk-mode-toggle*` CSS classes get orphaned but harmless if left in `globals.css` — keeping them in case a future surface wants them. `setMode` already exists and handles all the state plumbing.
- **after [cockpit Delegations eyebrow becomes the mode toggle]:** No bugs. Watch: the eyebrow-button has no per-mode color (current pattern: `cmk-eyebrow` is uniformly muted gray) — relying on the literal mode word + `⇄` glyph for state. If the operator has trouble telling at a glance, a tiny accent dot (sage in Andre mode, lavender in Lead mode) would fix without re-introducing the fat box.

- **before [cockpit rail header tighten]:** Cosmetic. No data risk. Both `cmk-mode-pill` usages live in `ChatPanel.tsx` only — confirmed via grep — so removing them is local.
- **after [cockpit rail header tighten]:** Resolved. Watch: if a future surface wants a state-pill labeling, the `.cmk-mode-pill` CSS class is still defined in globals.css — re-add `<span>` and it works. The icon-only "+" button still has `title="New thread"` + `aria-label` for screen readers + `⌘N` keyboard shortcut documented in the help modal — discoverability not regressed despite the visual minimalism.

- **before [lead heartbeat tab — timeline + recent-actions visual pass]:** Cosmetic only — components rendered every field, just without spacing or chrome. No data risk.
- **after [lead heartbeat tab — timeline + recent-actions visual pass]:** No bugs. Watch: `data-kind` dot tones are matched against the strings emitted by `activityLine()` (call/email/sms/note/task/meeting) plus the literal "thread" and "plan" added inline; if Close ever returns a new activity type, the dot falls back to the `.lead-timeline-dot` neutral base color — flag if that's noticeable in practice. Co-existence with the parallel graph control-board work: this move only touched `BoxTimeline.tsx` + appended to `globals.css` end-of-file; no overlap with graph file paths.

- **before [graph control-board implementation]:** Main risks: active dirty tree from another agent; `PlanDayCard.tsx` is load-bearing across Plan tab, graph inspector, and cockpit; `globals.css` is already large and recently edited for intake. Implementation should prefer a new graph-specific client component path over broad rewrites, and only refactor PlanDayCard if needed. Verification needs `npm run typecheck`; build/screenshot after if time and dev server cooperate.
- **after [graph control-board implementation]:** No TypeScript/build bugs. Watch items: (a) port 3000 already has a Node dev server running, so the browser may need `npm run fresh` to pick up the new client component/CSS instead of serving stale chunks; (b) the graph drawer owns duplicated edit UI for now rather than a fully extracted shared `PlanDayEditor` component — intentional to avoid broader collision while another agent is active; factor shared pieces later if the modal/drawer designs converge; (c) AI rewrite still requires server refresh to show the generated replacement day because we cannot predict the LLM output locally; status/touch edits update optimistically; (d) current graph remains horizontal-scroll for long horizons, which is acceptable for 7-day plans but may need zoom/fit controls for 30+ day cycles.

- **after [graph + day-editor redesign direction]:** Root cause identified in code: `PlanGraphView.tsx` converts each day into one `WorkflowNode` and forces plan data through generic `AutomationCanvas` shapes, while the actual editable content lives inside `PlanDayCard`'s large modal. Fix should not be "make the tiny SVG prettier"; it should split reusable editor content from `PlanDayCard`, render graph nodes as meaningful plan/touch cards, and make graph simulation/editor mutations update local state optimistically before `router.refresh()`. Modal height problem is real: current `PlanDayCard` stacks header, status toolbar, actions, add-touch, send-window, and AI-refine vertically; graph should use a drawer or tabbed/sectioned editor so primary controls remain visible at 100% zoom.

- **before [graph + day-editor redesign direction]:** Current graph page is visually underpowered and ergonomically wrong: nodes are tiny abstract shapes, empty canvas dominates, the inspector is cramped, and the day edit modal is too tall/wide for normal zoom. The modal also uses too much muddy brown, pushes core controls below the fold, and does not feel like the same artifact as the graph even though they edit the same plan state. Collision risk: another agent is actively changing the repo, so this pass should produce a design/implementation plan first and defer file edits until the tree is settled.

- **after [intake page — drop zone + card grid pass]:** No bugs. tsc clean after one type fix (`box.lead.display_name`, not `box.display_name` — `LeadBoxPageData.box` is a `CloseLeadFull` with the lead nested under `.lead`). Watch: (a) `cmk-intake-kind--*` tones rely on existing CSS custom properties (`--rose-deep` / `--sage-deep` / `--lavender-deep` / `--sky-deep` / `--peach-deep` / `--lemon-deep`) — fallback `var(--ink)` on each so an unset token still renders rather than blanking; (b) drag-leave fires when crossing into a child element, but we filter to `e.currentTarget === e.target` so the highlight doesn't flicker over the inner text — drop still works because `onDrop` is on the same element; (c) sequential multi-upload means a slow 50 MB PDF blocks the next file's start; if operators routinely drop 10+ files at once we should switch to a small-N concurrent pool; (d) the × delete is one click with no confirmation — fast for the operator, but easy to misclick; if Jake reports an accidental delete I'll add a confirm step with a 3s undo banner.

- **before [intake page — drop zone + card grid pass]:** Risk: low. New client component, isolated server action for delete. Watch: storage object delete uses Supabase Storage API (NOT a SQL DELETE — that's blocked by `protect_delete()` per the migration we just learned about). Removes both the DB row and the bucket file in one action.
- **before [intake uploader — await extract before refresh]:** Cosmetic UX bug. Extraction was working all along — operator just couldn't see it without a manual reload. No data risk.
- **after [intake uploader — await extract before refresh]:** Resolved. Watch: if extract takes longer than 30s on a giant PDF, the form unblocks with a "timed out" message but extraction may still complete server-side — operator hits refresh and sees it. If we ever ship really large PDFs in production, raise the timeout or move extract to a background worker.

- **after [Lead-scoped Intake — Phase 1]:** No bugs surfaced during build. tsc + next build clean on first try after `rm -rf .next`. Watch items: (a) **migration is staged but unapplied** — Jake needs to run `supabase/migrations/20260505140000_intake_extracted_text.sql` against the live DB; until then any read of `extracted_text` returns `undefined` (the type is `string | null` so `undefined` will widen — but Postgres won't error, the column just won't exist, so reads of the column WILL fail until applied; flagging clearly so Jake doesn't get caught). (b) **`pdf-parse` cold-load quirk** — using `await import("pdf-parse")` inside the PDF branch, so the test-fixture side-effect only fires on first PDF upload; if a fork-specific path issue surfaces, switch to `pdf-parse/lib/pdf-parse.js` direct import. (c) **Existing artifacts uploaded before the migration won't have `extracted_text`** — they keep their old 600-char `summary` only; chat won't include them in the intake block (filter is `extracted_text && trim().length > 0`). Re-upload or a backfill script would fix; not blocking. (d) **`text/markdown` MIME isn't standard from all browsers** — the uploader's `accept` includes both `text/markdown` and `.md`, and the extractor checks both filename suffix and MIME, so we should be covered, but flag if `.md` files come in as `application/octet-stream` and miss the text branch. (e) **15k-char total cap** is generous for typical lead intake but a single 200-page PDF will mostly truncate; the per-file 3k cap is the binding constraint. If operators hit this in practice, switch to a per-file summarizer pass (one OpenAI call per upload) before storing.

- **before [Lead-scoped Intake — Phase 1]:** Risk: low. Atom 4 (route deletion) may trigger Next.js stale `.next/types/` errors per CLAUDE.md (standard `rm -rf .next` recovery). Atom 1 will permanently delete any `intake_artifacts` row with `lead_id IS NULL` plus orphaned storage objects — Jake confirmed this is acceptable. Atom 2 adds `pdf-parse` (small, well-maintained) — known runtime quirk: `pdf-parse@1` looks for a test PDF at `./test/data/05-versions-space.pdf` on import in some forks; will dynamic-import inside the PDF branch to keep it cold-load until needed. Atom 5 hard caps total intake context at 15k chars to avoid blowing the system prompt. Watch: existing `/intake` route is the only place anywhere outside `/lead/[id]/intake` that uploads intake — once deleted, the only authoring surface is the per-lead route, which is the intended behavior.

## 2026-05-04

- **before [settings MCP auth row semantics]:** UX bug only — runtime `closeMcpStatus().auth_resolved` was already correct; the table row was keyed only to `CLOSE_MCP_AUTH_HEADER.set`.
- **after [settings MCP auth row semantics]:** Resolved. Operators with `CLOSE_API_KEY` set no longer see a false “missing” on the optional override row.

- **before [Close MCP env template]:** None — docs-only env append, no runtime wiring change.
- **after [Close MCP env template]:** `CLOSE_MCP_HEADER_*` vars are operator/reference only today; `close-mcp.ts` still uses `Authorization: Bearer ${CLOSE_API_KEY}` unless `CLOSE_MCP_AUTH_HEADER` is set — if Close's HTTP MCP requires literal `Close-API-Key` + `Close-Scope` headers for this app path, that's a separate code change.

## 2026-05-02

- **before [verify /settings MCP fallback panel]:** None. Read-only verification (typecheck + build) of a one-file edit Jake just made. Phantom `_document` cache risk noted — recovery is `rm -rf .next` once.
- **after [verify /settings MCP fallback panel]:** Nothing surfaced. Both checks clean on first run, no cache recovery. No new watch items beyond the Lane C carries (transport assumption, write-verb heuristic, advisory-not-enforced system prompt) already tracked above.

- **before [Lane C — Close MCP fallback]:** Reintroducing MCP into app code (was explicitly excluded per `project_comeketo_app_architecture.md` memory). Risk: drift back into "use MCP for everything" if the model overuses it. Mitigation in build: discoverable two-tool pattern (list + call) so the agent has to name what it's reaching for; system-prompt guidance preferring direct gated tools; write-detection heuristic stamps audit rows so we can see fallback usage in `/console`. Watch: Close MCP URL/auth shape unknown at code-time — `.env.local` blanks are fine until Jake fills them; runtime returns clean error if URL not set.
- **after [Lane C — Close MCP fallback]:** No bugs surfaced. tsc + next build clean alongside Lane B's parallel merge. Watch items going forward: (a) **MCP transport assumption** — client speaks plain JSON-RPC 2.0/HTTP and falls back to parsing SSE if `Content-Type` is `text/event-stream`; if Close's MCP requires modern Streamable HTTP with bidirectional sessions (`Mcp-Session-Id` header round-trip), this client won't drive it and we'll need to add session handling. First fire against a real URL will reveal which transport variant Close shipped. (b) **Audit verb heuristic** — `WRITE_VERB_RE` in [`close-mcp.ts`](src/lib/close-mcp.ts) flags `(create|update|delete|remove|patch|log|enroll|send|post|set|add|merge)` as writes. False-positives just over-log reads as writes — acceptable noise; false-negatives (a write tool whose name doesn't match) silently pass through as a read row. Worth a quick audit of the actual Close MCP tool catalog once configured to spot any verb pattern we missed. (c) **No `/test` ping yet** — the Test page didn't get an MCP health check this lane (avoiding contention with parallel surface work); a future operator-side button would fire `closeMcpListTools()` and report tool count + first-call latency. (d) **System prompt is advisory, not enforced** — the model is told to prefer direct tools but can technically reach for `close_mcp_call` for anything; if the audit log shows MCP overuse in practice, tighten by pre-filtering MCP from the tools array unless the operator opts in via a setting.

- **before [Lane C — `/test` MCP health check]:** None blocking. `/api/test/route.ts` and `/test/page.tsx` are operator-only surfaces, no parallel agent should be inside them right now (Lane A on heartbeat/settings, Lane B on chat/composite, Lane D on heartbeat dashboard). Pattern is well-established (4 modes already wired with the same shape). Risk: zero — adding a fifth row to a stable pattern.
- **after [Lane C — `/test` MCP health check]:** No bugs. Two new watch items: (a) **`testCloseMcp()` returns `ok: true` even when MCP is not configured** — by design, so the result pane shows the not-configured shape instead of throwing into the error path. The `configured: false` field on the output is the actual signal, and the hint string tells operators exactly what to set. If we ever make `/test` red-flag missing config, flip the not-configured branch to throw with the hint string. (b) **First-call latency reflects the full RTT to Close's MCP server** including any cold-start they have. If the first click feels slow but second click is fast, that's MCP server warmup, not our client. Worth noting in operator docs once Jake fills the URL.

- **before [Lane C — `/console` kind filter]:** Risk: low. The kind filter pattern already exists in spirit (`?trace=` filter is the same shape — query param, validation, alternate query, tweaked render). Validation has to handle stale clients linking with retired kinds — fall back to invalid-banner + full tail (same shape as `traceInvalid`). Watch: if Lane D's parallel work also added a kind filter, my param name would collide; the file as I'm reading it has no `kind` param yet, so I'm clear.
- **after [Lane C — `/console` kind filter]:** One issue surfaced and fixed in-flight: `isWriteFlag` const was inferred as `unknown` because the `&&` chain mixes a boolean (`showTargetToolSubLine`) with `unknown` operands (`r.payload`, `(r.payload as Record<string, unknown>).is_write`); JSX `{isWriteFlag && (...)}` then rejected the `unknown` value as not-`ReactNode`. Build-agent wrapped the chain in `Boolean(...)` to coerce. Lesson general enough to flag: any time we render `payload.X` from `execution_log` rows, the JSX expression needs `Boolean()` or an explicit type narrow because `payload` is `unknown` upstream. Watch: (a) **`payloadTargetTool()` is local to the console page** — if a second surface needs the same field extraction, hoist it to `execution-audit.ts` alongside the row type; (b) **`KIND_PILLS` is hardcoded** — fine while the curated set is stable, but if operators want to pin/unpin pills per their workflow, that's a future settings move; (c) **trace filter beats kind filter when both query params are set** — could feel surprising; documented in code comment but worth a "filtered by trace, kind ignored" hint if operators ask.

- **before [Lane C — `/settings` MCP fallback panel]:** *Recorded retroactively — move was announced in chat but not tapped before execution. Process violation flagged to Jake; ledger now honest.* Risk: low. Single file edit. No collision risk (settings page only edited by me in this session).
- **after [Lane C — `/settings` MCP fallback panel]:** No bugs. One process bug flagged: I ran the move without writing a `before:` tap into the ledger first. Direct violation of CLAUDE.md's top-priority rule. Fix going forward: tap the ledger BEFORE saying "going" in chat, not after. The chat announcement is not the tap. Watch items: (a) the `mcpState` discriminated union and pill colors are inline rather than promoted to a token — fine while only `/settings` renders the state; if `/console` or `/test` ever wants the same pill, hoist to a small `MCPStatusPill` component; (b) the panel uses `cmk-stack-panel--sky` to keep operator-info tone consistent with `/console` filter banner — if that token gets repurposed later for a different "info" semantic, this panel will follow whatever it becomes.

- **before [Lane C — `enable_mcp_fallback` setting toggle]:** Risk: small. Touches `api/chat/route.ts` which Lane B is parallel-editing — but the change is scoped to a single one-line `tools:` argument transform (filter helper from `close-tools.ts`). If Lane B has reshaped the tool-list assembly between my read and write, I'll need to merge by hand. Mitigation: do all the lib + settings work first, do the chat route edit last so the conflict window is narrowest. Watch: bool flag default behavior — when `CLOSE_MCP_URL` is blank, `enable_mcp_fallback=true` is meaningless (no tools to enable); display a subtle "URL not set" hint next to the toggle so operators don't think they're disabling something that's already off.
- **after [Lane C — `enable_mcp_fallback` setting toggle]:** No bugs. tsc + next build clean. The chat route merge worked — Lane B's `[...CLOSE_TOOLS, ...COMPOSITE_TOOLS]` shape was still in place, swapped cleanly to `[...getCloseToolsForSettings(...), ...COMPOSITE_TOOLS]`. Watch items: (a) **defensive gate runs both checks** — when MCP is disabled AND the model invokes a `close_mcp_*` tool (shouldn't be possible since they're filtered from the tools array, but a stale OpenAI conversation context could send a function_call for a name that's no longer in the catalog) — the dispatcher returns a clean error instead of running the call. Belt-and-suspenders by design. (b) **the toggle's "URL not set" hint is conditional on `mcpState === "off"`** — if URL is set but auth doesn't resolve (`partial`), the hint doesn't fire. Could refine but currently fine: in `partial` state, toggling the kill switch is meaningful even if config is incomplete. (c) **revalidating `/chat` on toggle save means the next chat turn picks up the new tools list immediately** — but an in-flight chat request (someone mid-typing) still sees the old shape. No fix needed.

- **before [operator nav + lock parity]:** Chat/book could load without cookie while `/lead` could not — inconsistent exposure if `OPERATOR_PASSWORD` set.
- **after [operator nav + lock parity]:** Aligned; `/chat`, `/leads`, `/intake`, `/api/chat`, `/api/threads` added to matcher.
- **before [operator API matcher sweep]:** `/api/leads/*`, `/api/test`, and `/test` UI still bypassed operator cookie when lock on.
- **after [operator API matcher sweep]:** Matcher adds `/api/leads`, `/api/test`, `/test`, `/analytics` (+ `:path*`); public remains `/api/auth/operator`, `/api/webhooks/:path*` (not matched).
- **before [operator lock on `/`]:** Marketing `/` leaked without cookie when lock on.
- **after [operator lock on `/`]:** Matcher includes `/`; `/operator-login` still open.
- **before [login land on console from `/`]:** Post-login `next=/` showed hero again — wrong first screen for ops.
- **after [login land on console from `/`]:** `postLoginDestination` maps root → `/console`.
- **before [console trace column]:** None — UX completeness.
- **after [console trace column]:** Clean.
- **before [trace → UI + console filter]:** Console could filter by lead/plan informally only; no `?trace=` contract.
- **after [trace → UI + console filter]:** UUID `trace` query validated; bad param falls back with error banner.
- **before [delegations → execution_log]:** No `trace_id` on `/api/chat` response; console could not correlate a send with log rows without reading thread content.
- **after [delegations → execution_log]:** Resolved for audited tools + plan gen; `trace_id` returned when `tools_used` non-empty.
- **before [operator console]:** Approvals page used `TabNav active="heartbeat"` (misleading); `/console` not in operator middleware matcher when lock on.
- **after [operator console]:** Approvals `TabNav` matches surface (was heartbeat); `/console` behind operator middleware.
- **before [Lead Box multi-tab routes]:** None — IA/density issue on one page.
- **after [Lead Box multi-tab routes]:** Deep links: `#cycle-plan` still on Plan tab; server actions revalidate lead layout segment; approvals links unchanged.
- **before [Close leads assignee search]:** None beyond UX — root cause was API misuse (`user_id` not an advanced-filter field on `lead`).
- **after [Close leads assignee search]:** Watch: assignee tabs scan `GET /lead/` pages until matches or `has_more` ends — large orgs may hit `LEAD_SCAN_MAX_SKIP` cap (see `scanLeadsMatching` in `close.ts`); raise if operators need full-org certainty.
- **before [automation UX sweep — Rodbot + inspector + drafts]:** Draft detail had no in-chat interpret wiring; inspector rail was placeholder copy.
- **after [automation UX sweep — Rodbot + inspector + drafts]:** Watch: draft **save** + inline **generateCloseSteps** UI removed — steps must reach DB via another path (e.g. future chat tool / manual) or publish flow stalls without existing `close_steps_json`.

- **before [PR #1 auto-fix]:** 3 HIGHs blocking merge (cohort heatmap visually inverts retention vs raw count → demo risk; funnel `cmk-funnel-delta--up` shows red when value drops, green when value rises; consolidated comment + analytics page reference path `_reference/CC Agent (legacy)/...` that no longer exists in repo).
- **after [PR #1 auto-fix]:** All 3 HIGHs resolved on the PR branch. Watch: HIGH 4 (no runtime validation that snapshot JSON matches TS shapes — `as unknown as T` casts on 7 imports) is **deferred to a follow-up sprint** per review verdict; risk is unchanged today (snapshots all carry the typed keys). MEDIUM 5/6 (extract `fmtCents`/`pct`/`safeMax`/`fmtDate` and `buildIntelSignals` to `src/lib/`) also deferred. LOW 2 (5 of 9 chart sections lack `length === 0` empty-state guard) not addressed this pass — auto-fix step intentionally limited to HIGH+MEDIUM scope per review.
- **before [workflow studio route]:** None — discoverability-only gap (demo graph lived under dev-only `<details>`).
- **after [workflow studio route]:** Clean — demo graph no longer dev-only.
- **before [automation empty states + matcher doc]:** Sequences pill filter could imply demo graph was “their” filtered data; operators adding `/api/foo` could forget matcher.
- **after [automation empty states + matcher doc]:** Filter-mismatch panel + middleware inventory comment; audit: all current `page.tsx` / `route.ts` roots covered or intentionally public.
- **before [intake route + TabNav + attachment types]:** `tsc` / build red: missing `@/app/intake/*`, `TabKey` mismatch, `Attachment` union.
- **after [intake route + TabNav + attachment types]:** Intake recreated.
- **before [intake download error surfacing]:** Failed signed URL / mismatch redirected to Box with no explanation.
- **after [intake download error surfacing]:** [`redirectIntakeArtifactDownload`](src/app/intake/actions.ts) sets `?intake_dl=bad_request|not_found|signed_url`; [`box/page.tsx`](src/app/lead/[id]/box/page.tsx) + [`IntakeArtifactsPanel`](src/app/lead/[id]/IntakeArtifactsPanel.tsx) show dismissible message.
- **before [ledger / agent discipline — anti-churn]:** User friction: **delete ↔ restore loops** when agents green-build by bringing back files Jake removed; taps were the missing guardrail in practice.
- **after [ledger / agent discipline — anti-churn]:** Rule in Problems: missing modules → **assume intentional deletion until proven otherwise**; strip imports / routes / matcher first; **`before:` tap must state that**; never bulk-restore without Jake OK.
- **before [panel + icon-hover UI pass]:** Flat forms + default inputs; nav icons had no hover affordance.
- **after [panel + icon-hover UI pass]:** None blocking — cosmetic IA; watch select arrow custom style on quirky browsers.
- **before [panel pass — console + leads + approvals]:** Operator index pages visually disjoint from the tinted panel system used on settings/intake.
- **after [panel pass — console + leads + approvals]:** Clean — same `cmk-stack-panel` language; watch double-wrap if a future row adds its own full-bleed tint.
- **before [OpenAI breadth — 5.5 + media + background]:** GPT‑5.5 not in picker; media APIs absent; background Responses not exposed (operator perception = “only chat completions”).
- **after [OpenAI breadth — 5.5 + media + background]:** Watch: **Deep think** disables Close tools for that turn; Vercel **hobby** may kill long polls before OpenAI finishes — Pro/`maxDuration` or a future async job table may be needed; transcribe upload MIME/extension quirks across browsers; new model strings still **runtime-validated by OpenAI** only.

- **before [Lane D — heartbeat dashboard tells the truth]:** None blocking. Four-agent fork in flight; coordination risk: Lane A may be touching `lib/heartbeat.ts` for the `auto_approve_clean_days` consumption — I'm scoped to the read/aggregate path (new helper file) + presentation, NOT the verdict path, to avoid collision. If Lane A also reshapes `runHeartbeatForPlan` return shape, my aggregator may need a follow-up to absorb new fields. Watch: `heartbeat-summary.ts` is client-safe and avoids bundling `heartbeat.ts` — keep my new aggregator on the same side of that line so the page stays cleanly server/client split.
- **after [Lane D — heartbeat dashboard tells the truth]:** No collisions with Lane A/B/C verified — only files touched were the new `heartbeat-truth.ts`, `heartbeat/page.tsx`, and an additive `globals.css` block (after `.hb-error`, before `/* HEARTBEAT REPORT (modal) */`). tsc clean. Watch items: (a) **lead names are not surfaced** — rows show truncated `lead_xxx…` only because `heartbeat_runs` doesn't store `display_name` and a per-lead Close GET would 30× the dashboard load; future polish is either a batched `closeListLeadsByAssignee` map or a `display_name` column on `heartbeat_runs.report` writes; (b) `WAITING_ON_OPERATOR_CODES` includes `EXECUTION_DISABLED` so draft-mode actions surface as "Waiting on you · flip mode" — operator-correct interpretation but assumes the operator wants those days fired; if `manual_send_only` mode is intended as "I'll send by hand," this entry will read as a false-alarm; (c) `today_eligible` and `waiting_count` are derived from latest-per-lead runs; if the same lead has two runs in 24h with different verdicts, only the latest counts (correct for "current state" but means a stale run from 22h ago can't appear); (d) `fired_today` reads every fire in the 24h window (correct — each is a real Close write) and dedupes only on `(lead_id, day_index, fired_at)` tuple inside the row, so the same action fired in two different runs would appear twice — in practice `DAY_ALREADY_SENT` blocks this, but if Lane A reshapes day-status promotion, watch for double-counts; (e) the forensics `<details>` is collapsed by default — operators digging "why didn't this fire" need to know it's there (the chevron + meta line should make it discoverable); (f) `heartbeat-truth.ts` is server-only (uses `getSupabaseServer`) — do not import from any "use client" file; the page is already a Server Component so this is a non-issue today.

- **before [Lane A — pipeline-state tool + chat widget]:** Jake reassigned Lane A from heartbeat-verdict to the chat-side mirror of Lane D's reframe. Coordination risks: (a) Lane B also extends `close-tools.ts` — append-only on my side, narrow merge surface; (b) Lane C also extends `RichToolResult.tsx` — different switch branches, isolated; (c) Lane D builds an aggregator on the page-side that's structurally identical to mine — if their aggregator lands first I'll consume it server-side rather than fork (no DB schema change, both pure-read); (d) tool-result summary in chat is hard-truncated at 600 chars per existing `cmk:tools` block contract — must design payload compact (counts + capped example arrays, not full lists). Watch: if any agent reshapes `runHeartbeatForPlan` return type, my aggregator's read of `heartbeat_runs` rows may need adjustment — pure-read so risk is just type drift, not data corruption.

- **after [Lane A — pipeline-state tool + chat widget]:** No bugs. tsc clean. Three concurrent agents (B/C/D) all wrote to `close-tools.ts` and the chat surface; merges landed cleanly because each lane added isolated tool entries + dispatch cases. Watch items: (a) **two aggregators in the codebase** — Lane D's `aggregateOperatorTruth()` in [`heartbeat-truth.ts`](src/lib/heartbeat-truth.ts) and my `pipelineStateForOwner()` in [`pipeline-state.ts`](src/lib/pipeline-state.ts) walk the same source tables but return different shapes for different consumers (page vs chat). Both pure-read so no data risk, but DRY pressure could justify unifying after demo settles. (b) **600-char widget summary truncation** — my payload sizes to ~700 chars in the worst case (5 waiting + 5 fired + 6 gated). When all arrays are full and lead names are long (Holloway Events ~14 chars), the JSON.stringify can exceed 600 and the widget falls back to raw JSON pre. The fallback path is what the existing `ToolPanel` does for unknown tools — functional but less pretty. Mitigation if it bites in practice: trim each example list to 3 items, or cut intent length to 40 chars. (c) **Close API call on every invocation** — `closeListLeadsByAssignee(owner, 200)` is called once per `pipeline_state_for_owner` call to build the name map. With 200 leads that's one Close API hit (Andre has ~50 leads in the practice org, so it's fast). Could cache the name map in Supabase if rate becomes an issue. Owner='all' path skips this call (no name resolution, falls back to `…<lead_id_suffix>`). (d) **owner='all' has no name resolution** — by design (we don't know which user_ids to query); if Jake wants 'all' to show real names, walk both Andre + Jake assignee lists and union. (e) **Lane D's heartbeat-truth lacks lead-name resolution** while mine has it for owner-scoped — when we unify aggregators, port the closeListLeadsByAssignee step into the shared layer so /heartbeat also gets real names. (f) **`solo_mode` field included in payload** so the agent can interpret which gates are active when explaining "why didn't this fire" — not consumed by the widget yet but useful for the model's reasoning.

- **before [Lane D follow-up — lead names on the truth strip]:** None blocking. Edge case: heartbeat sweeps ALL plans regardless of owner, so single owner-scoped `closeListLeadsByAssignee(ANDRE)` could miss Jake-owned leads that show up in waiting/fired. Using `closeListLeads({limit: 250})` instead (returns recent 250 across all owners; practice org has 101 leads total). If a lead falls outside that window, fallback renders truncated id — same as today. One Close API hit added to page load (~150ms). No DB or aggregator changes.
- **after [Lane D follow-up — lead names on the truth strip]:** No bugs. tsc clean. Watch items: (a) **`closeListLeads({limit: 250})` returns the 250 most recent leads** — if a lead with an active plan is older than the 250th most recent, the row will fall back to the truncated id. Practice org (101 leads) has zero risk; production orgs with hundreds of older active plans would. Future hardening if needed: split into multiple paged calls or only resolve names for the lead_ids actually referenced in `truth.waiting_on_approval` + `truth.fired_today` (per-id `closeGetLead` parallel — N calls instead of one batch). (b) **Close API hit on every page render** — wrapped in defensive try/catch so a Close outage doesn't break the page (falls back to truncated ids), and joined the existing Promise.all so it parallelizes with the Supabase queries. No noticeable latency in practice. If Close rate-limits become a concern, a 60s in-memory cache of the name map (keyed by org) would absorb most of it. (c) **Server Component + service-role keys** — `closeListLeads` uses the operator-side Close key, not a per-user one; that's already the convention for everything in `lib/close.ts`, no auth-shape change here. (d) **Lane A still has its own owner-scoped name map** in `pipeline-state.ts` — both implementations work, future DRY-pass should consolidate into a shared `lib/lead-names.ts` helper.

- **before [Lane D — auto-refresh the truth strip]:** None blocking. Watch: `setInterval` + `router.refresh()` runs the full Server Component re-render path including the `closeListLeads({limit: 250})` call from the previous atom — that's one extra Close API hit per 30s while the tab is foreground. Cap mitigation: pause via `visibilitychange` when tab hidden. If that's still too chatty in prod, a settings-driven refresh interval (30s / 60s / 120s / off) is a clean follow-up. No DB or aggregator changes.
- **after [Lane D — auto-refresh the truth strip]:** No bugs. tsc clean. Watch items: (a) **30s interval is hardcoded** — when `auto_refresh_interval_ms` lands in settings (or env), thread it as a prop to `<HeartbeatAutoRefresh intervalMs={...} />`; (b) **Each refresh fires `closeListLeads({limit: 250})`** — at 30s cadence with one operator that's 120 calls/hour for the dashboard alone. Practice org has no rate concern; production should consider a 60s in-memory cache of the lead-name map (the names rarely change in 30s anyway). (c) **No "live" indicator on the page** — operators won't know it's auto-refreshing unless they watch the numbers tick; could add a tiny pulsing dot near the "last 24h" badge in the toolbar as a future polish. (d) **`router.refresh()` does NOT bypass Next.js fetch cache** — if any of the data calls inside the Server Component are cached by Next, refresh won't see new data. The page is `dynamic = "force-dynamic"` so all reads are uncached — verified, no issue today; flag if anyone changes that directive. (e) **Visibility hook fires on tab-switch but NOT on minimize-window** — minimized window still counts as `visible`. Acceptable trade; full window-blur detection adds complexity without clear value.

## 2026-05-01

- **before [pre-break ledger sweep]:** Consolidate open watches + ops reminders into Problems for Jake’s return.
- **handoff [pre-break] — active watches & ops:**
  - **Supabase:** Keep local/prod in sync with repo migrations in timestamp order — notably `20260501180000_automation_drafts`, `20260502120000_close_webhook_events`, `20260503120000_intake_artifacts`, `20260504100000_rls_publishable_denied`, `20260505120000_execution_audit`. `/automation/drafts` 404s or table-missing errors usually mean an unapplied migration.
  - **Close API:** `POST /sequence/` response shape in prod may differ from typings — publish flow throws with export fallback; confirm whether `id` is top-level on create.
  - **Dev hygiene:** After sprints touching Server Actions / routes, Jake runs `npm run fresh` if chunk/cache weirdness (per [`CLAUDE.md`](CLAUDE.md)).
  - **Historic / non-repo:** Customer scheduled tasks were disabled earlier — not fixed by this codebase; revisit if live follow-ups matter.
- **after [pre-break ledger sweep]:** Handoff watches grouped above; epic-specific bullets kept below for traceability.
- **before [automation IA polish]:** None — small UX-only change.
- **after [automation IA polish]:** Clean.
- **before [automation + multi-touch epic]:** None known; large surface area (Close write paths + AI) may expose integration gaps during verification.
- **after [automation + multi-touch epic]:** Watch Close `POST /sequence/` response shape in the wild (id may be nested); publish path errors should fall back to JSON export per UI. Confirm Supabase migration applied for `automation_drafts`.
- **inception:** None yet. Workspace was empty. Build has not started.
- **watch:** Scheduled tasks were disabled, not deleted — if the new functionality doesn't supersede them, the live customer follow-ups (Hugo, Steve, Daphney, Dawn, Eliana, Elizabeth) are now silent until re-enabled.
- **after inception:** No new problems surfaced setting up the ledgers. Clean.
- **before [foundation alignment]:** Three forks blocking foundation kickoff — (1) stack choice, (2) project location, (3) credential + MCP-hosting posture. None of these are recoverable without churn if I guess wrong, so I'm asking Jake to lock them.
- **architectural watch:** OpenAI Responses API needs *remote/HTTP* MCP servers, not local stdio. GitHub has an official hosted MCP. Supabase MCP options are local-only or self-hosted as of last I checked. This may need verification before Settings page can offer real fields.
- **resolved 2026-05-01:** Supabase ships a hosted HTTP MCP at `https://mcp.supabase.com/mcp?project_ref=...`. My earlier "trickier" call was wrong. Both MCPs can be remote-attached to Responses calls.
- **new watch [auth wiring]:** Supabase + GitHub MCPs both expect OAuth flows in the Claude/IDE world. OpenAI Responses API tool config takes static `headers` instead. Need to confirm at Test-page time whether bearer tokens (PATs / service role keys) work in headers, or whether we need a pre-auth bridge.
- **security flag 2026-05-01:** OpenAI API key was pasted in chat. I am NOT saving it to memory and I am NOT writing it to disk yet (no scaffold exists). Will land in `.env.local` (gitignored) once project is scaffolded. Jake should consider this key exposed — recommend rotation post-scaffold once we have a path to receive a fresh one safely.
- **resolved 2026-05-01:** Supabase key naming was wrong in my head — current format is `sb_publishable_*` and `sb_secret_*`, not legacy anon/service_role. Updating env wiring to match.
- **resolved 2026-05-01:** Operator-key-handling policy clarified — when Jake provides a key, use it. He owns rotation. No more security lectures.
- **before [scaffold] watch:** GitHub MCP auth value not in the .env. Stubbing as empty in `.env.local`; Settings page will let Jake fill it once we know the auth shape (PAT vs OAuth bearer).
- **after [scaffold]:** Scaffold completed without surfacing new bugs. Watch items still open: (a) GitHub MCP auth empty — first Test-page fire will tell us what the server demands, (b) Supabase MCP auth shape — best-effort fallback in `/api/test` tries `SUPABASE_SECRET_KEY` as bearer; may need adjustment, (c) Settings page is read-only — write-back lands once auth shape is confirmed.
- **note:** OpenAI Responses MCP tool spec uses `headers` for static auth. Casting in route handler because OpenAI Node SDK type defs may lag the MCP tool-type literal. Working as-is at runtime; revisit if SDK update tightens types.
- **resolved 2026-05-01:** MCP auth-shape watch items are no longer blocking — pivot to direct APIs eliminates them from the primary path. Auth is now plain PAT (GitHub) + secret key (Supabase). Both well-documented. MCP-attach question deferred to agent-side prototyping.
- **before [pivot]:** Need GitHub PAT from Jake (or generated by him) for the Octokit path. Settings page will surface a `GITHUB_PAT` row showing whether it's set.
- **after [pivot]:** Pivot landed cleanly. One real outstanding: `GITHUB_PAT` in `.env.local` is empty until Jake generates one. The Settings page will show `GITHUB_PAT: missing` and the GitHub Test button will throw a clear error message until then.
- **resolved 2026-05-01:** GitHub PAT is now in `.env.local` per Jake. All three test buttons should now go green when the dev server is running.
- **before [strip MCP]:** No real problems — just dead code/env to remove for tightness.
- **after [strip MCP]:** Clean. Nothing surfaced. The watch item about MCP-auth shapes (Supabase secret as bearer? GitHub PAT in headers?) is now closed by deletion — the question can't be asked of code that doesn't exist.
- **discovered 2026-05-01 [env file disconnect]:** Jake's uploaded `.env` (in `~/Library/.../uploads/`) and the project's `.env.local` (in `app/`) are separate files. Jake's been editing the uploaded one; the app reads the other. This is going to keep biting until we set the convention: `app/.env.local` is the only file the dev server reads. Fixing now via sync, but worth flagging that the uploaded `.env` should probably be treated as a one-time bootstrap, not an ongoing config surface.
- **after [sync env]:** Resolved by sync + convention. Going forward: edits go in `app/.env.local` directly. If Jake prefers to update the uploaded `.env`, he just has to ping me for a re-sync.
- **before [hero page]:** Need to identify the exact serif typeface from the screenshot. Will inspect Jake's template; if no font is specified there, default to a close Google Fonts match (likely Newsreader or similar). Google sign-in button is a visual stub only — do not wire it. Nav in screenshot reads `about / changelog / request invite` — those routes don't exist yet; rendering them as inert links for now.
- **after [hero page]:** Font identified — Fraunces (Google Fonts), with Inter for sans and JetBrains Mono for mono. All three loaded via `@import` at the top of globals.css. No problems landed. Watch items: (a) `about` / `changelog` / `request invite` are inert anchors — when those pages exist, swap to `<Link>`; (b) Google button is wired as a no-op `onClick={(e) => e.preventDefault()}` — flip to real OAuth when auth is wired.
- **bug 2026-05-01 [server component event handler]:** Hero page throws Next.js Server Component serialization error (`Object.toJSON` recursion on something un-serializable). Cause: `onClick` prop on the GoogleBtn — Server Components can't ship event handlers. Fixing by removing the onClick (button is visual-only anyway). Going forward: any element that needs interactivity goes in a "use client" file. tsc DOES NOT catch this — must verify via `next build`.
- **after [hero hotfix]:** Resolved by removing the onClick. Rule going forward: tsc clean is necessary but not sufficient. For UI moves, also run `next build` (or hot-reload the dev server) before declaring done. Adding to architecture memory.
- **before [layout rules]:** Hero currently overflows the viewport because `hero-split` has `min-height: 70vh` and the counter strip pushes below the fold. Auth card has a thin border but no shadow, so dimensionality is muted. No global scroll-hide utility yet. No swim-lane palette tokens yet. Fixing all of these in one move so the rules are real, not aspirational.
- **after [layout rules]:** Resolved. `min-height: 70vh` removed from `.hero-split`; vertical centering inside `hero-main` does the work. tsc clean. Watch item: very short viewports (< ~700px tall) will still trigger the safety-net scroll inside `hero-main` — that's intentional and the scrollbar stays hidden, so it reads as "tight" not "broken."
- **before [chat page]:** Jake's chat HTML uses slightly different specific colors than my existing tokens (e.g. sage `#6B8E5A` vs my `--sage-deep` `#8FA078`, gold `#C4923D` not in my palette at all). Decision: port his colors literally for chat fidelity rather than forcing token mapping that loses the look. If a wider palette audit comes up later, we can reconcile then.
- **after [chat page]:** No bugs surfaced. Watch items: (a) chat is pure static — input bar doesn't actually accept text yet, all rail items are inert, no real "3 turns" tracking. When we wire the OpenAI Responses backbone, this all needs to flip to a `"use client"` shell with state. (b) Tab nav (people/activity/intake/etc.) routes to nowhere yet — those become real `<Link>`s when the matching pages exist. (c) Color palette duplication (sage `#6B8E5A` vs `--sage-deep` `#8FA078`) is real but deferred. Will surface again if a palette audit move comes up.
- **before [unify with chat]:** Hero/admin pages still carry mono on editorial elements (eyebrow, fine-print, stats labels, breadcrumb), and the paper bg is the honey `#EFEBE3`. Both diverge from the chat page Jake wants as the standard. Fix in one move so divergence doesn't bake in further.
- **after [unify with chat]:** Resolved. Mono survives only in true code/data spots (admin Settings/Test fingerprint columns, env var names) where it earns its place. Watch item: `--paper` change ripples through every page; if anything looks too pale somewhere, easy to dial back. Settings + Test pages now have an active utility nav (`chat / settings / test`) which makes /chat reachable from anywhere in the dev shell.
- **before [functional foundation]:** Three watch items: (a) The model strings (`gpt-5.4-nano-2026-03-17`, `gpt-5.4-mini-2026-03-17`, `gpt-5.4-2026-03-05`) are Jake-supplied — if the OpenAI Responses API rejects any of them at runtime, error surfaces in the chat reply and we adjust. (b) `.cmk-settings.json` is gitignored — fresh clones won't have one; the lib will return defaults until first save. (c) Chat is now mixed Server + Client (panel is "use client") — boundary is clean (the panel is its own file) but worth noting for future refactors.
- **after [functional foundation]:** No new bugs. Watch items still active: model-string validity is runtime-only. New: intake's "Ask Rodbot" bar and "Try one" chips are still inert (no onClick yet — they're Server Components). Wiring those to a `/api/intake/ask` endpoint is a clean next move when you want intake-as-RAG to actually run. Chat input is fully wired and live.
- **before [add vercel cli]:** Linking creates `app/.vercel/` (gitignored by default by the CLI). If Jake isn't logged in, `vercel link` will trigger a browser auth flow — flag and pause if so. Mild lock-in nudge to watch: `vercel dev` reads `vercel.json` and treats Next.js as serverless functions; behavior may diverge slightly from `next dev`.
- **before [intake rename + research]:** "Rodbot" is dead naming. Easy rename. The harder concern: Jake feels the intake page concept itself is confusing — the polished HTML he sent leans toward a "drop docs and ask questions" RAG surface, but the original CC Agent likely defines intake differently. Need to find that source of truth before changing structure.
- **after [intake rename + research]:** Original CC Agent intake JSONs confirm the concept matches what the HTML shows — drop docs, structured extraction, Q&A. Two real gaps in our current page: (a) the `← reports` button doesn't go anywhere because we don't have a reports list view; the original implies intake is plural — many reports per workspace. (b) Each doc panel currently shows just `name` + `meta` — the original schema gives us classification kind, confidence, structured fields (theme, metrics, concepts) we could surface for richer cards. Both of these are deferred until Jake decides the move.
- **before [chat depth]:** No live blockers. Watch items: (a) the gpt-5.4-* models are still untested in production — first real /api/chat fire will reveal whether the strings work or we need to fall back; (b) auth not wired — no user_id on threads/messages yet (single-user assumption); (c) image base64 round-trips can be heavy in a JSON body — fine for MVP, would move to direct upload later if it starts feeling slow.
- **after [chat depth]:** No bugs. Watch items moved forward: model-string validity is still runtime-only (errors will surface in the chat as `error: ...` and the message persists either way). Tab nav `delegations` link still goes to `/chat` — the page is functionally the chat workspace now. Future polish hooks: (a) rich assistant blocks (tool / automation card / needs-your-call) by detecting structured output or with function calling — currently all assistant output renders as markdown; (b) thread rename, archive, delete — backend has helpers for these but no UI yet; (c) initial sample seeds in DemoEmptyState are hardcoded — could come from settings.
- **after [add vercel cli]:** No problems. Login was already in place, so no browser auth interrupt. One watch item: `vercel env pull` overwrites `app/.env.local` — never run it without checking with Jake first, since the canonical env values currently live in `app/.env.local` (not yet pushed to Vercel project env).
- **after [push to RodbotCC]:** Push clean. Stale nested `ComeketoAgent/` folder on remote was the botched output of an earlier external push — deleted in a follow-up commit. Watch item: anyone re-cloning the repo before the cleanup commit will see the duplicate files in history; not a problem, just a footnote.
- **vercel team auth still missing:** `vercel git connect` keeps returning 400 because the Vercel team `expressedais-projects` (formerly belonging to ExpressedAi GitHub user) hasn't been granted access to RodbotCC's repos yet. CLI cannot drive this — must be completed in the Vercel dashboard's GitHub-app config dialog. Once Jake clicks through, retry will succeed.
- **after [flatten + re-nest]:** No problems. Killed leftover `app/.next/` cache — regenerable. Watch item: any future references in conversation/memory to `app/.env.local` are now stale — env file is at `./.env.local`. Memory note may need to be updated next time it surfaces.
- **resolved [vercel auth rathole 2026-05-01]:** Hours lost to Vercel + GitHub auth confusion. Root cause: Jake was already signed into Vercel in browser as `tech-4536` the entire time — the CLI flow was a red herring. Two projects exist under his account: `comeketo-agent-alpha` (stale, leftover) and `comeketo-agent-ra8h` (live at https://comeketo-agent-ra8h.vercel.app, building from f1d8b57). GitHub→Vercel auto-deploy wired and working. Lesson: before chasing CLI auth flows on any service, verify the browser session first.
- **resolved [phantom app/ dir blocking dev 2026-05-01]:** After the earlier flatten move (Next.js source moved from `app/` → repo root, and source files moved into `src/`), an empty `app/` directory was left behind containing only `.DS_Store` + stale `.next/` cache. Next.js prioritizes a root-level `app/` over `src/app/` when detecting App Router root, so it tried to serve from the empty dir and 404'd every route. Fix: `rm -rf app/ .next/`. Lesson: when flattening a Next project, verify NO `app/` or `pages/` dir survives at the new root unless it's the canonical one.
- **recurring [phantom app/ dir keeps regenerating 2026-05-01]:** Same phantom showed up AGAIN ~3 hours after first removal. Found at 15:23 with just an `app/.env.local` file inside (identical to root `.env.local`). When present, Next builds with empty App Router output (`.next/server/app/` doesn't even get created, route table shows only Pages 404). Source unknown — possibly Vercel CLI (`vercel pull` / `vercel env pull` defaults to `app/.env.local`?), possibly an editor / file-sync. Fix again: `rm -rf app/`. Watch item: if it returns a third time, add `app/` to `.gitignore` AND inspect what tool created it. Could also add a precommit/preinstall hook that errors on its presence.
- **dev-server stale chunk cache 2026-05-01:** After adding Server Actions + new dynamic routes ([id]) underneath a running `next dev`, the dev server's in-memory webpack state desyncs from disk. Symptoms: random pages start 404'ing (e.g. `/chat`) and dynamic routes throw "Cannot find module '../XXX.js'" errors pointing at non-existent chunks. `next build` from a clean tree is fine — only the running dev server is broken. Fix: Ctrl-C the dev server and `npm run dev` again. Repro pattern: any move that adds a `[param]` segment + Server Actions in the same commit. Worth a heads-up to Jake whenever we ship that combo.
- **resolved [server→client function-prop boundary 2026-05-01]:** Server-rendered page.tsx was passing `{kind:"item", label:"Open in Close", onSelect: () => window.open(...)}` arrow functions as the `menu` prop to `<BoxPanel>` (a "use client" component). Next 14 throws `Error: Event handlers cannot be passed to Client Component props.` because functions can't be serialized across the boundary. Fix: redefined `BoxPanelMenuAction` as a fully serializable discriminated union (`{type: "open_expanded"}` | `{type: "open_url", url}` | `{type: "copy", text}`) and resolve to live handlers inside the client component. Lesson: any prop a server component hands to a client component must be JSON-serializable. When designing reusable client primitives, the public API has to accept declarative descriptors, not callbacks.
- **resolved [FAB cluster overlapped page header 2026-05-01]:** PlanCardClient rendered the `.plan-card-fab` as a sibling of the children (not inside the .plan-card div). With `position: absolute`, it anchored to the nearest positioned ancestor — which was way up the DOM, effectively the viewport. Result: Approve & run / Approve all / Refine plan buttons floated at the top-right of the page, on top of the AppHeader's wordmark + utility links. Fix: wrap children + FAB + toast in a `.plan-card-shell` div with `position: relative`. The FAB now anchors to the shell which is the same width as the plan card. Lesson: when wrapping server-component children with absolute-positioned siblings, always wrap them in a shared positioned ancestor.

- **before [practice lead seed + Close comms probe]:** None — purely additive against Openera practice org; script is idempotent only if re-run manually (would duplicate leads unless we add guards later).
- **after [practice lead seed + Close comms probe]:** No new product bugs. Watch: re-running the seed script will create another 50 leads and reshuffle **all** leads again — add a dry-run or `[Comeketo practice seed]` filter before re-execution if that becomes painful.

## 2026-05-02

- **🔒 RULE LOCK [no demo, ever]:** Recorded permanently in Goals + Global. Surfacing here because it's a working-culture rule on the same shelf as the anti-churn rule (2026-05-01) — both prevent specific failure modes that recur if the agents get sloppy. **Failure mode this prevents:** scoping work down to "just enough to demo," shipping stubs that look done but aren't, leaving "wire it later" comments that calcify into tech debt. **How to apply:** when an atom feels like it should be deferred, defer it cleanly with a Watch entry — don't ship a half-version that has to be torn out. When tempted to use the word "demo" in tap entries / prompts / comments, replace with what the work actually is (the build, the run, the loop, the test, the verification, the operator flow).

- **before [Lane B — composite chat tools]:** Two known frictions going in: (1) `approveAllDaysInternal` lives inside `src/app/lead/[id]/actions.ts` as a private helper — composite path needs the same logic but I'm avoiding edits to `actions.ts` (Lane A may also touch it for `auto_approve_clean_days` consumption). Strategy: duplicate the loop in `composite-tools.ts` with a clear "fold-back" comment so when Lane A lands the toggle, the two paths converge in one helper. (2) Tool-trace UI in `ChatPanel` extracts `lead_id` (singular) from args/result for the lead-pin pointer — composite tools take `lead_ids[]`/`plan_ids[]`, so trace cards for batch calls won't auto-pin. Acceptable for first cut; widget-side improvement is Lane A's territory.
- **after [Lane B — composite chat tools]:** No new bugs. tsc clean. Watch items: **(a)** `approveCleanDaysForPlan` in composite-tools.ts is a faithful duplicate of `approveAllDaysInternal` in `actions.ts` — when Lane A lands the `auto_approve_clean_days` toggle, fold both into a shared lib helper (likely `lib/plan-approval.ts`) so the toggle takes effect everywhere; **(b)** "hot" today = `date_updated` desc — first proxy until LATTICE arrives. Operator who says "hot" expecting reply-likelihood or last-Andre-touch will get most-recently-updated instead. Document the proxy in any operator-facing copy until LATTICE replaces it; **(c)** batch caps are hard at 10 ids per call — if Andre's reflexively-top-10 demo wants 15+, raise both caps in lockstep (find_top_n cap 25 ↔ batch cap 10 today); **(d)** ChatPanel tool-trace cards show `lead_id?: string` (singular) — batch tools won't auto-pin; the tool result JSON contains the lead list but the UI ignores arrays today. Lane A widget work should add a `lead_ids?: string[]` recognition path to RichToolResult; **(e)** parallel-agent collision tonight: Lane C also edited `api/chat/route.ts` (added MCP-fallback prompt section + may still be pending their imports + dispatch branch). My Batch section sits ABOVE the MCP fallback section in the prompt — order is intentional (batch is the operator's normal flow, MCP is the escape hatch). If Lane C re-orders, fold accordingly; **(f)** OpenAI tool-payload size: composite adds 3 tools (32 total now). Still under any practical cap, but worth tracking — the per-turn prompt is now ~4kb of tool defs alone.
- **before [error-handling review · PR #1]:** Risk of missing real silent-failure paths because the diff has no try/catch/await — easy to skim past the genuine concerns (NaN propagation through cast-bypass, empty-state inconsistency).
- **after [error-handling review · PR #1]:** Two real issues filed in artifact: (1) `fmtCents` (page.tsx:51) accepts NaN via `typeof === "number"`; combined with `wl._meta.active_count * rv.deal_size_percentiles.mean_cents` (line 100) and the funnel `valueCents` proxies (lines 138, 146), any missing operand renders as literal `"$NaN"`. Fix: `if (typeof cents !== "number" || !Number.isFinite(cents)) return "—"`. Same pattern would tighten `pct` (page.tsx:58) for free, though no NaN-producing call site exists for `pct` today. (2) 5 of 9 chart sections (`source_channels`/`revenue_concentration`/`bltByEventType`/`conversionCurves`/`cohortGrid`) lack the `length === 0` guard that owners/monthlyTrend/histogram/WinLossSubChart already use — empty arrays render bare-section instead of `cmk-an-empty` placeholder. Watch: the cast-bypass pattern (`as unknown as T`) is itself a runtime-validation gap; out of sprint scope, but worth a future "snapshot validator" sprint with zod/valibot at module load.

- **before [Close API automation + surface expansion]:** `close.ts` covered list/get workflows + enroll + opps + activities but not full sequence definition fetch, subscription CRUD, telephony/templates/config — friction for automation editor + SMS debugging.
- **after [Close API automation + surface expansion]:** Addressed for the automation slice + operator debugging (templates, statuses, phone inventory). Watch: **creating/updating/deleting sequences via API** not wired yet (destructive — needs explicit approval UX). **Webhook subscription CRUD via our app** still not wired (operators use Close UI or raw REST; ingress endpoint exists). Chat tool surface growing — watch OpenAI tool payload size if we add 20+ more tools (may need grouping or lazy tools).

- **before [Close webhook ingress]:** Inbound path missing — could not durably record Close-driven state changes for Box freshness / future Realtime.
- **after [Close webhook ingress]:** `/api/webhooks/close` live + `close_webhook_events` schema committed. Watch: **subscription create/update/delete via API** still manual in Close UI or raw REST for now. **Table must exist** before POST succeeds (migration). **Replay / ordering:** Close does not guarantee order — consumers must tolerate; duplicate `event_id` returns 200 idempotent.

- **watch [Close webhook go-live deferred]:** Jake table-applied + parking lot — full **ops** checklist (public URL, Vercel env, Close subscription, `CLOSE_WEBHOOK_SIGNATURE_KEY`) lives under **“Things to do — Close webhooks”** in `_scaffold/Global.md`. **Not** app-login auth; can stay queued without blocking other build momentum.

- **watch [snapshot bump post-comms-depth]:** New `snapshotIdForBox` — existing `lead_plans` may show stale vs live Box until operators regenerate (expected).

- **after [Delegations page magic]:** No bugs surfaced. tsc + next build clean. Watch items: (a) `ContextMenu` is imported from `lead/[id]/ContextMenu.tsx` — minor cross-route coupling, fine until a third surface needs it, then move to `src/components/`; (b) coordinate with the parallel Close-API agent — this sprint only added to `lib/threads.ts` (no `lib/close.ts` edits), collision risk low but verify before merging; (c) **`npm run fresh` recommended** — added `/api/threads/[id]` route and reshaped /chat client surface significantly, dev server may carry stale chunks; (d) rail row's button-inside-ContextMenu pattern: right-click also bubbles to `onClick` in some browsers — observed OK in Next 14 build, watch for double-fire reports.

- **after [Delegations round 2]:** Build threw `PageNotFoundError` for `/_document` and `/settings` mid-collect — caused by stale `.next/server/pages/_document.js` references from a previous incremental build. Fixed by `rm -rf .next && npx next build`. Recurring symptom — `next dev` and partial cache states keep regenerating phantom pages-router artifacts in `.next/`. Fully resolved with the manual clear; ran clean afterward. Watch items: (a) `Modal` and `ContextMenu` are still imported from `src/app/lead/[id]/` — third surface now uses them, cross-route coupling has been accepted; if a fourth shows up, move them under `src/components/`; (b) localStorage layout key versioned `cmk-chat-layout-v1` so future shape changes can migrate without breaking persisted prefs; (c) the parallel Close-API agent edited `api/chat/route.ts` between rounds — merged cleanly with my round-2 edits (added `summary` field), no conflicts; (d) `npm run fresh` strongly recommended — modal shape, animation keyframes, and grid transition fight stale dev-server chunks; (e) tool result `summary` is hard-truncated at 600 chars — long Close payloads will show `…` suffix in the modal, which is fine for preview but not full audit; (f) the fully-static phrase list for animated thinking will get repetitive — move to settings or compute from current state when it gets old.

- **before [variable horizon + practice /leads]:** Cycle plans fixed at 7 days; practice rows not flagged on /leads.

- **after [variable horizon + practice /leads]:** No bugs in tsc/next build. Watch: (a) `closeListLeads` `_fields`; (b) practice counts from 200-cap snapshot.

- **before [/automation Close sequences]:** Automation route still demo-only vs CRM.

- **after [/automation Close sequences]:** Live sequence list + `close:list-sequences` script. Watch: Close app URL pattern may differ by org — verify "Open in Close" links.

- **after [Delegations round 3]:** No bugs surfaced. tsc + next build clean (/chat 52→54.4 kB). The `PageNotFoundError` on `/api/leads/[id]/summary` showed up again on the first incremental build and resolved with `rm -rf .next && next build` — same recurring Next 14 dev-cache phantom we've had before, not introduced by this sprint. Watch items: (a) `Toast` (in `src/components/`), `Modal` + `ContextMenu` (still under `src/app/lead/[id]/`) are all load-bearing across multiple routes — if a fourth surface needs Modal/ContextMenu, hoist them into `src/components/`; (b) auto-grow textarea triggers a layout pass on every keystroke — fine at message length, watch for jank if input grows multi-paragraph; (c) shortcut "?" listener guards against editable focus but `⌘\` may collide with browser dev-tools on some Linux setups and `⌘⇧N` competes with private-window in Firefox; (d) `prefers-reduced-motion` is not yet honored — could add a `@media` block to flatten the keyframes if a motion-sensitive operator asks; (e) drag-drop overlay backdrop-blur is GPU-cheap on modern browsers but degrades to translucent fill on older ones; (f) `relativeTime()` doesn't auto-refresh on the rail, so a thread that was "now" at mount stays "now" until next reload — could add a 60s interval if it gets noticed; (g) hydration: `useChatLayout()` initializes with DEFAULT_LAYOUT then updates after first effect, so a brief flash is possible if the user previously hid both panes — observed OK, not a server/client mismatch.

- **after [Delegations round 4]:** No bugs. tsc + next build clean (/chat 54.4→55.8 kB). Resolved the round-3 watch item (f) — `relativeTime` now auto-ticks every 60s via `nowTick` state. Watch items: (a) Esc-to-cancel intercepts the keydown only when `abortRef.current` is set, but it does so before any other Esc handler — Modal's own Esc-to-close still works because Modals close via their own handler before this listener (different elements); if we ever stack a Modal during a request, Esc will cancel the request first then a second Esc will close the modal — that's the correct hierarchy IMO; (b) the slash palette closes if the user types past the matched prefix without space — by design (it disappears when no match, no need to dismiss), but worth confirming feels right at use; (c) `/lead <id>` smart-routes by `lead_xxx` prefix — if Close ever changes the prefix, update the slash command; (d) thread filter is purely client-side substring match against `title` only — no fuzzy match, no body content; fine for current scale; (e) code-copy reads `innerText` which means whatever the browser rendered — if we add a syntax highlighter later that injects markup tokens, copy may include them; non-issue today; (f) the abort fetch returns synthesized error message `"_(canceled)_"` rendered as italic markdown — operator might prefer a more visible "canceled" pill; can revisit if it gets confusing.

- **before [Close surface sprint]:** None blocking — REST-only read surfaces + scripts; auth stays out of scope. Risk to watch: Advanced Filtering `reference_type` for lead status must match Close (`status.lead`); 200-cap + `?q=` client-side status/assignee trim same as other list views.

- **after [Close surface sprint]:** No new bugs from tsc/build. Watch: if Close rejects `status.lead` on `/data/search/`, adjust `reference_type` per current Advanced Filtering docs; text search + filters still bounded by `_limit` 200.

- **before [big moves roadmap]:** `use server` files cannot export arbitrary values — Settings build was failing until state moved out of `actions.ts`; Supabase migrations are in-repo but must be applied on the remote project.
- **after [big moves roadmap]:** Resolved. Watch: `intake` bucket + `intake_artifacts` table must exist for uploads; operator lock is optional (empty `OPERATOR_PASSWORD` leaves actions open).

---

## 2026-05-01

- **watch [post-auth epic ops]:** Ensure migration `20260505120000_execution_audit.sql` is applied on the Supabase project so `execution_log` / `approval_audit` / `lead_activity_touches` exist. Playwright smoke remains optional (not added to package.json).

- **after [polish plan rounds 5→9 autopilot]:** No new bugs. Two unblocker fixes to parallel-agent code: (1) `heartbeat/[run_id]/page.tsx` `SkipBreakdownSection`: removed explicit `: ReactNode` return type so TS infers JSX.Element (Server Component constraint); (2) `PlanDayCard.tsx`: `as const` on every `kind:` field of the items array. Both 1-line, logic untouched. Watch items: (a) `useFormState` is now a load-bearing pattern via `SettingsForm` — if Next.js bumps to App Router strict mode this needs revisit; (b) toast dedup uses `bumpKey` to force React key change — works across the existing render but if a future toast wants persistent stacking, add a `dedup: false` option; (c) responsive breakpoints add `!important` overrides to padding tokens — fragile if those token values change, fine for now; (d) `prefers-reduced-motion` rule uses `*` and `!important` to enforce — heavy-handed but appropriate for a one-line accessibility commitment; (e) recurring `.next` phantom cache still appears on first incremental build — workaround stays `rm -rf .next && next build`.

- **after [Round 11 — chat-first widgets]:** No bugs. tsc + next build clean. Watch items: (a) `pinFromCall` knows only `close_get_lead_full`, `close_get_lead`, `generate_seven_day_plan`; everything else falls to a generic `tool-result` pin — extend the switch as new tool shapes matter; (b) auto-pin guard via `lastAutoPinned.current` keyed `${msgId}:${callIdx}` — fine while message IDs stay stable; (c) `RichToolResult` reads `parsed.lead`, `parsed.contacts`, etc. from the JSON-stringified summary which is hard-truncated at 600 chars — long contact lists will be clipped at the API layer, the renderer doesn't fix that; (d) the empty-pinboard state still talks about the old "scope" mental model — copy could be tightened; (e) backspace lead-picker, `/day <id> <N>` command, and Lead Box widget extraction are all queued for next rounds; (f) the parallel agent's heartbeat/[run_id] page still has a TS error elsewhere that doesn't affect /chat — Jake's other agent owns it.

- **before [Lane A R2 — payload tighten + parser fallback + /morning slash]:** Coordination risk: Lane B may also be touching `ChatPanel.tsx` slash commands. Mitigation: I'm appending one new entry (`/morning`) into the existing slash palette array — narrow merge surface. Watch: (a) trimming intent to 40 chars may chop mid-word — acceptable for a chat preview, sees fuller intent on click-through to the lead; (b) counts-only fallback uses regex extraction not JSON parse, so it'll work on any payload shape that puts the count fields first — but if the field-order convention drifts, the regex needs an update; (c) `/morning` and `/today` both map to the same prefilled text — `/today` may already exist as Lane B's command for "today's calls" or similar; check before adding to avoid collision.

- **after [Lane A R2 — payload tighten + parser fallback + /morning slash]:** No bugs. tsc clean. Watch items: (a) **regex fallback is order-agnostic** — patterns match `"plans_active":N` anywhere in the truncated string, so even if the output field order drifts the counts still come back. The only fragile bit is the field NAMES — if anyone renames `today_eligible` → `today` or similar in `pipeline-state.ts`, update the fallback regexes in `parsePipelineSummary` to match. (b) **`solo_mode` regex defaults to false** when truncated past it — fine because the indicator is purely advisory; the worst case is the "solo mode" pill is hidden when it should be shown. (c) **The partial banner shows but the lists themselves are gone** — operators wanting the full list can click "Raw arguments + result" in the tool modal to see the truncated JSON, OR re-fire the briefing if a new heartbeat run has arrived. (d) **EXAMPLE_CAP at 3 may feel sparse** when the operator wants to see a longer waiting queue at a glance — solution if it bites is a separate "show full queue" tool/command that returns the full list, fetched freshly on demand (no widget summary truncation). (e) **`/morning` aliases** `/morn`, `/brief` — `/brief` may collide with future "brief me on lead X" patterns; if Lane B or C wants `/brief`, mine is happy to drop the alias.

### 2026-05-04
- **before [fix disappearing plan]** — confirmed two compounding bugs in /lead/[id]: (1) AutoRefresh.tsx polls every 30s, (2) load-lead-box.ts:76 swallows getLatestPlanForLead errors silently. Combined: any transient Supabase read failure during a tick → plan disappears.
- **after [fix disappearing plan]** — closed via 4-file surgical edit (AutoRefresh.tsx, load-lead-box.ts, PlanSection.tsx, page.tsx). Underlying *cause* of transient Supabase failures still unknown — now visible via console.error '[load-lead-box] getLatestPlanForLead(...) failed' and a user-visible 'Plan fetch failed' card. If Jake sees that card recurring, follow-up: add retry-with-backoff to getLatestPlanForLead, or investigate Supabase rate-limit/connection-pool behavior.
- **before [site-wide lead names]** — bare lead IDs rendered in /heartbeat (Recent runs, Fired today) and /console (Recent execution log). Disorienting — no human remembers a lead ID. Possible others undiscovered.
- **after [site-wide lead names]** — closed. Note: top-250 cap means leads outside recent activity window fall back to shortLeadId (lead_xxxx…) — acceptable for current scale. If Jake hits a lead he can't see by name, that's the trigger to upgrade resolver (e.g. per-lead fetch on miss, or supabase-backed cache table).
- **[site-wide lead names] follow-up** — resolver only checked display_name; many leads have empty display_name and only `name` set, so they fell back to shortLeadId silently. Fixed: resolver now tries `display_name || name` (matches /leads page pattern). Added console.info/warn/error logging so future failures surface in dev terminal.
- **before [group MCP-fallback panels]** — false-failure UX: API call fails → LLM retries via close_mcp_call → MCP succeeds → answer correct. But current ToolPanel renders flat per-call, so the failed step still shows red. Diagnostics already work via /console mcp_fallback filter — only presentation needs a fix.
- **after [group MCP-fallback panels]** — closed. False-failure UX gone for the dominant case (failure → close_mcp_call recovery). Heuristic stays tight: only close_mcp_call after a failure groups as recovery — anything else falls back to today's flat rendering. /console mcp_fallback diagnostics untouched.
- **[group MCP-fallback panels] follow-up** — initial heuristic only treated close_mcp_call after failure as recovery. Real LLM behavior also recovers via dropping to simpler API tools (e.g. failed Search → succeed via list_leads + plain query). Loosened: any failure(s) followed by next success → recovered group. Pill text changed 'ok via MCP fallback' → 'ok after retry'.
- **before [search-tool fix + honest MCP labels]** — root cause of red panels in chat: closeSearchLeads sends a malformed Advanced Filtering body. Was masked by LLM working around it via list_leads. Fix the call, then mask is unnecessary. Also: 'MCP fallback' UI/code language is misleading because MCP isn't even configured.
- **after [search-tool fix + honest MCP labels]** — closed. Red panels in chat should now be ~gone for plain-text searches. If structured Klaus queries are ever needed (status:Customer, has:email, etc.), build a proper /data/search/ payload — note left in close.ts. MCP code paths (close-mcp.ts, etc.) untouched — still wired for future enablement.
- **before [bake MCP tool catalog into system prompt]** — agent flying blind on MCP capabilities. Jake provided full MCP doc; baking it into the prompt makes ~50 new tools first-class in the LLM's mental model. Token cost ~1000 extra/turn — minor, amortized across the agent's now-better tool selection.
- **after [bake MCP tool catalog into system prompt]** — closed. Token cost ~+1500 chars per system prompt; minor tradeoff for the LLM picking the right MCP tool first try.
- **before [Phase 1 WorkflowManifest foundation]** — adds src/lib/workflow-manifest.ts (+ tests), wires inspector + mermaid view into existing /automation/drafts/[id] editor, opt-in onSelectNode prop on AutomationCanvas. No data migration, no heartbeat change, no /chat or cockpit work.
- **after [Phase 1 WorkflowManifest foundation]** — closed. Plans + automations now share one canonical shape on the read side; heartbeat + Close-publish-flow untouched. Phase 2 (cockpit + Studio convergence + edit affordances) and Phase 3 (multi-touch UX uplift) and Phase 4 (simulator + runtime unification) remain ahead. Discovery during build: AutomationCanvas already had external-inspector hooks the original plan assumed it lacked — reduced Change 3 to nothing.
- **before [Phase 2 cockpit + automation cleanup]** — biggest /chat edit yet (3070 LOC file). Risk areas: localStorage v1→v2 migration, lead_id propagation through send paths, system prompt token budget when lead context loaded. Half B is straight deletion; verify no orphan imports first.
- **after [Phase 2 cockpit + automation cleanup]** — closed. Heartbeat untouched. lead_plans / automation_drafts data unchanged. Per-thread lead_id persistence DEFERRED (Phase 3+) — lead_id is global cockpit state for v1. localStorage migration v1→v2 in place; old layout gracefully migrates. /chat bundle 62.8→64.1 kB.
- **before [cockpit polish: deeper lead context + editable day card]** — Lead-mode lead-context block is too friendly: pre-loaded summary makes the agent skip the deeper tool calls it would naturally make on a cold prompt. Fix: tell the block to encourage close_get_lead_full / close_list_activities / close_list_email_threads explicitly. Day-card modal needs inline edit (channel select + intent + draft_seed) + an Approve button on the modal itself.
- **after [cockpit polish: deeper lead context + editable day card]** — closed. Both issues caught in cockpit smoke test now resolved. Worth re-testing the depth fix specifically — ask the same Lead-mode question that produced terse output before, expect richness now.
- **after [day modal width + chip picker styling]** — closed. The modal action buttons (Approve / Move back / Mark sent / Skip) were left as-is — they live in a paper-2 toolbar so context already provides separation; if Jake still wants them more distinct, easy follow-up. ChannelChips component is generic — could be promoted to /components if reused elsewhere later.
- **before [Andre rail names + remove deep think]** — both small UI/UX cleanups. Lead-name fix uses already-built /api/leads/search route. Deep think removal touches ChatPanel.tsx state + UI + send body, and chat route's polling-background branch.
- **after [Andre rail names + remove deep think]** — closed. The lead-names cache is client-side and fetches the recent 200 leads once per session. Pins for older leads beyond that window will still show the captured label (which falls back to lead_xxx if it was an auto-pin). Acceptable for now; could upgrade later to a per-id lazy fetch.
- **before [task #8 API tool dictionary audit]** — dictionary structurally sound (32+3 tools all present, no orphans). Only 4 real bugs in prompt vs tool defs. Real risk: close_enroll_in_workflow LLM calls today with workflow_id (broken). Surgical fix.
- **after [task #8 API tool dictionary audit]** — closed. Audit caught 4 'flagged' issues + Phase 2 surfaced 6 more by reading every entry against the tool def. The agent now has accurate signatures for every CLOSE_TOOL it can call. Pattern section also corrected. /openai-background.ts still orphan from earlier deep-think removal — separate cleanup.
- **before [P4 v0 heartbeat dry-run simulator]** — operator approves blind today. After: see SKIP_CODE / would-fire per touch before firing. Read-only path; no Close writes. Pure projection of existing heartbeat verdict logic.
- **after [P4 v0 heartbeat dry-run simulator]** — closed. Operator can now preview heartbeat verdicts before clicking Approve & run. Zero side effects: confirmed by reusing the verdict-only path from the existing runner without inserting heartbeat_runs / executing actions / updating plan status. If a stale plan would have been auto-paused by the real runner, simulator REPORTS plan_was_stale: true but doesn't actually pause.
- **before [plan-as-graph view + simulate-driven coloring]** — extends AutomationCanvas with nodeStates prop (currently has pulseNodeId for one-node highlight; nodeStates is many-node). Adds CSS for fire/skip tints. New PlanGraphView client component (~150 LOC). New page route + sub-nav entry. No schema, no migrations.
- **after [plan-as-graph view + simulate-driven coloring]** — closed. The 'beautiful graph' is back, lit up by the simulator. Reuses Phase 1 manifest adapter + the simulator we just shipped. AutomationCanvas got a small extension (nodeStates prop) with no regressions to existing /automation/drafts/[id] usage.
- **before [graph polish + remove /automation]** — /automation removal kills the publish-to-Close drafts pipeline. Operator chose simplification over capability. AutomationCanvas component preserved (moved to /components/) since PlanGraphView still uses it. workflow-manifest.ts type imports updated.
- **after [graph polish + remove /automation]** — closed. /automation removal sacrificed the publish-to-Close drafts pipeline; operator chose simplification. AutomationCanvas relocation tidied the conceptual debt of an 'automation' component used outside an automation page.
- **before [task #9 tool-panel UI coverage]** — small file edit to ChatPanel.tsx: extend TOOL_CATEGORY map + add close_mcp_call special-case in toolHeadline. No risk.
- **after [task #9 tool-panel UI coverage]** — closed. Tool panels now read accurately for every tool in the dictionary + every MCP tool the agent might fall through to. No more falsely-categorized 'read' panels for writes.
- **before [plan state sync across surfaces]** — PlanDayStrip useEffect deps only on leadId; never re-fetches when plan changes elsewhere. PlanGraphView is a server component prop passthrough; no client refresh. Chat tool calls that create/refine plans never notify the cockpit's right rail.
- **after [plan state sync across surfaces]** — closed. Three surfaces now stay in sync within the session via window events; cross-tab still works via visibilitychange refresh on focus. NOT YET cross-device — that'd need realtime (Supabase realtime / SSE) and isn't worth it yet. Action callers from /lead/[id]/PlanSection (server-rendered form action) don't dispatch events directly because they're server actions, but the visibility-refresh handles cross-tab → cockpit propagation.
- **before [PlanPageBeacon]** — server actions can't reach the client event bus directly. Beacon-on-render approach catches every revalidatePath round-trip generically without per-form wiring.
- **after [PlanPageBeacon]** — closed. The 'caveat worth knowing' from the previous fix is gone. Plan-state synchronization is now complete in-window. Cross-device still requires page refresh — that's Supabase realtime / SSE territory and out of scope.
- **before [analytics buildout — render 7 missing chart views]:** Open friction for this sprint: (1) **Conversation intel has NO snapshot** — port-manifest lists `build_conversation_intelligence.py` as path-only with no output wired into `src/data/analytics/`. CLAUDE.md "no building without functionality" rule says omit unless we can derive a rollup from snapshots already on disk. May need to drop or redefine the view; flag for confirm step. (2) `cohort_snapshot.json` is rich (`cohort_grid` + `conversion_curves` + `best/worst_cohorts`) — one chart can't capture it; pick the right primitive(s). (3) Aesthetic constraint — existing `cmk-an-channels` / `cmk-an-channel-bar` / `cmk-an-fam-*` family-tinted bar primitives should be reused, not duplicated per view. (4) Branch mismatch — plan asks for `analytics-buildout`, worktree is on `archon/thread-b7dbe237`; isolation rule wins, branch stays as-is.

- **after [analytics buildout — plan setup]:** No new bugs (setup only — no code edits). Risks captured in `plan-context.md`: (a) Conversation Intel feasibility — must resolve in confirm step or drop; (b) cohort snapshot is wide — pick conversion_curves + recent_cohort_health, don't try to render everything; (c) win_loss has 7 `by_*` cuts — pick one primary + chip summary, not 7 sub-charts; (d) verify `cmk-an-channel-bar` works for non-channel data or needs neutral palette; (e) `npm run fresh` may be needed after the sprint. Watch items going into implementation: (i) keep `force-static` — JSON imports tree-shaken at build; (ii) `force-static` requires no runtime `fs.readFile`, so any new chart must continue importing snapshots as JSON modules at the top of the file (or accept module-level imports passed in via props from the page); (iii) the parallel agent owns `/console`, `/approvals`, `/heartbeat/*`, `/operator-login` — analytics doesn't touch them; (iv) Jake's "kept artifact pile small" preference (round 12 watch (e)) — bundling new snapshots into the JS bundle stays acceptable for static data, but we're not adding new ones this sprint anyway.

- **before [analytics buildout — plan confirm]:** Risk that pattern lines in `page.tsx` had drifted, or that snapshot shapes changed since plan was written. Conversation Intel feasibility unresolved — could become a "what to do with this view" blocker at implementation if not decided now.
- **after [analytics buildout — plan confirm]:** No new bugs (read-only verification). Watch items: (a) **Conversation Intel decision is final for this sprint — OMIT.** When implementing, drop the entry from `missingViews` at `page.tsx:132` (currently `["Pipeline funnel", "Conversation intel"]` → after sprint becomes `[]`); do NOT add a stub component, an inert tab, or a "coming soon" placeholder per CLAUDE.md hard rule. (b) Plan referenced `bun run` but project is `npm run` — implementer must use npm scripts. (c) `cmk-an-fam-*` classes are source-family-specific (sky/lavender/peach/lemon/sage/grey); for owner / stage / cohort dimensions, fall back to bare `cmk-an-channel-bar` (default fill in globals.css) or `cmk-an-card-{tone}` tinting — visual confirmation needed in dev. (d) `Upcoming events` snapshot at `page.tsx:124` has `has_view: false` but is not on the chart-list; leave as-is — out of scope this sprint. (e) Inline vs. extracted file shape stays a judgment call; default inline, extract only if any single chart grows past ~80 lines.

- **before [analytics buildout — implement]:** Going into the build with Conversation Intel dropped (no source data); win/loss has 7 `by_*` cuts to choose between (plan picks 4: by_event_type, by_source_family, by_owner, by_value_bucket); cohort_snapshot is wide (rendering conversion_curves + cohort_grid heatmap only); some `by_source_family` keys may not match existing `cmk-an-fam-*` tokens (will fall back to `cmk-an-fam-other` or add tokens in CSS task); page.tsx is currently ~365 lines and will balloon — keep inline unless any chart exceeds ~80 lines.

- **after [analytics buildout — implement]:** No new bugs introduced by analytics work. **Pre-existing baseline carried into this sprint, NOT introduced here, NOT this sprint's scope to fix:** (1) `next lint` script in package.json is a no-op since Next 16 dropped the command — should be migrated to a direct ESLint invocation in a future tooling sprint; (2) `vitest` is not installed in this worktree's `node_modules` — `npm test` fails with "command not found"; need a `npm install` round to restore; (3) `npm run build` fails on `SettingsActionState` not being exported from `src/app/settings/actions.ts` — Server Action wiring issue on the parallel agent's settings surface, unrelated to analytics; (4) 11 typecheck errors in baseline (`react-dom` types, `vitest` types, `operator-guard` cookies type) — none in analytics code, all rooted in missing devDeps. **Watch items unique to this sprint:** (a) Conversation Intel surface is now silently absent — bottom blurb explains why, but if Andre/Rodrigo asks "where did it go", point at this entry + plan-confirmation.md for the OMIT rationale; (b) Owner Performance bar uses `cmk-an-bar-sage` instead of family-tinted bars — chosen because owners aren't a source-family dimension; visual cohesion check needed in browser; (c) Win/Loss `by_value_bucket` chart uses `cmk-an-bar-lemon` — bucket order is preserved from snapshot (which is amount-ascending for the JSON we have); (d) Win/Loss `by_event_type` and `by_owner` are top-6, others (`by_source_channel`, `by_customer_type`, `by_guest_bucket`) intentionally excluded to avoid the "7 sub-charts in one section" anti-pattern flagged in plan; can add a chip-summary follow-up if Rodrigo wants the missing cuts; (e) `Upcoming events` snapshot still has `has_view: false` (surfaced via metric strip + intel signals only, not a dedicated chart) — out of plan scope this round; (f) Cohort summary line correctly handles edge case where best === worst (rare on small samples); (g) Monthly-trend bars use linear scaling on `won_value_cents` — if one month dwarfs others, smaller months may render at sub-pixel height; mitigated by `min-height: 1px` floor on the bar.

- **before [analytics buildout — validate]:** Implement-after-tap flagged 4 baseline tooling gaps to verify (no node_modules in worktree, vitest missing, lint no-op, `SettingsActionState` build error). None should be analytics-caused; validation must confirm.
- **after [analytics buildout — validate]:** No new bugs. **Resolved during validation:** (1) all 11 baseline typecheck errors — fixed by `npm install` (worktree had no node_modules; @types/react-dom, vitest, @types/node now resolve) + `rm -rf .next` (stale `.next/types/validator.ts` was generated under a Next 15+ build, using `Promise<{id}>` params, while installed Next is 14.2.35 with sync params); (2) `SettingsActionState` build error did NOT reproduce — was a stale-cache artifact; build is clean. **Confirmed unfixed (deferred, not this sprint's scope):** (a) `npm run lint` — project has no ESLint config (no `.eslintrc*`, no eslint deps); `next lint` enters interactive setup. Pre-existing baseline; will need a dedicated tooling sprint. **New watch items:** (i) `.next/types/validator.ts` drift — if a parallel agent runs Next 15 against the same worktree, the regenerated validator breaks tsc against Next 14 route handlers; workaround `rm -rf .next && npm run build`; (ii) `npm install` adds 32 vulnerabilities (1 low, 8 mod, 23 high) — run `npm audit` if/when security tightening is on the menu, but per CLAUDE.md priority rule it stays low until Jake says otherwise; (iii) Node engine warning: required `>=20.16.0`, current `v20.14.0` — non-blocking but worth noting if Vercel deploys start failing.

- **before [analytics buildout — finalize PR]:** Risk that GitHub PR creation fails on first push (no upstream) or that the repo has branch protection blocking direct push. No CI config in repo (verified — no `.github/workflows/`), so no auto-checks should run on PR creation.
- **after [analytics buildout — finalize PR]:** No new bugs. PR #1 OPEN successfully — first push set upstream cleanly (`origin/archon/thread-b7dbe237` created with tracking), GitHub returned no branch-protection block. **Watch items:** (a) lint baseline still red — if Jake or a reviewer expects PR checks to be green, they'll see no checks at all (no `.github/workflows/` configured) which is honest but worth knowing; (b) the PR body cites "12/12 tests pass" + "build clean" from validation phase — a fresh-clone reviewer must run `npm install && rm -rf .next && npm run build` to reproduce (the `.next` clear is non-obvious without context); (c) Vercel deploy on this PR will NOT happen (deploys only fire on push to `main` per CLAUDE.md hard rule + Vercel project config); merging the PR will trigger a Vercel deploy from `main` after merge — that's the demo-update gate; (d) PR is #1 in the repo — first one. Reviewer flow / merge conventions for this repo aren't established yet; surface this to Jake if he wants a CONTRIBUTING.md or a default merge strategy locked in.

- **before [PR #1 comment-quality review]** — 2026-05-02: comment-quality agent auditing comments in changed files; artifact only, no product code touched.

- **before [PR #1 docs-impact review]** — 2026-05-02: read-only review move — risk of over-flagging is the main concern (e.g. recommending a README for a repo that intentionally has none, or proposing CLAUDE.md additions for conventions with only one consumer).
- **after [PR #1 docs-impact review]** — 2026-05-02: no problems surfaced and none introduced. **Watch items:** (a) repo has no `README.md`, no `docs/`, no `.github/PULL_REQUEST_TEMPLATE.md` — intentional pre-launch state per CLAUDE.md "ship over ceremony" posture, but worth knowing if Jake later wants a contributor-facing surface; (b) the chart-primitive CSS palette (`cmk-an-bar-{sage,sky,peach,lavender,lemon,neutral}`) and the typed-snapshot pattern in `src/lib/analytics-types.ts` are convention-shaped but uncodified — they'll need a short note once a second consumer touches them; (c) `_scaffold/` ledgers continue to absorb sprint memory in lieu of formal docs, which is per CLAUDE.md design — no change needed.
- **before [PR #1 code review]** — 2026-05-02: read-only review move — risk of false positives on snapshot type shapes (JSON shape may not exactly match strict types).
- **after [PR #1 code review]** — 2026-05-02: 2 real bugs surfaced in PR #1 — both worth fixing before merge. (a) `cohort_grid.slice(0, 12)` in `src/app/analytics/page.tsx:198` shows the OLDEST 12 cohorts, not "most recent 12" as the heading promises (JSON is ordered chronologically oldest-first; verified in `src/data/analytics/cohort_snapshot.json`). (b) Pipeline funnel `↓ X% drop` arrows between Active/Won/Lost rows model parallel disposition buckets as if they were sequential funnel stages — when Lost > Won, this even renders the contradictory `↓ +513.0%`. Two smaller items: a duplicate type (`UrgencyCardSeg` vs `UrgencySegment`) and inconsistent `window_days` typing (required on SourceChannel, optional on WinLoss/Revenue). **Watch items:** (i) once these get applied, retest the `/analytics` page after `npm run fresh` since the fix touches both a slice direction and a render condition; (ii) the funnel arrow logic is the kind of thing future analytics views might copy — fix it cleanly now or it propagates; (iii) baseline tooling gap from prior sprint (`vitest` not installed, `next lint` not configured) was NOT introduced by this PR but remains worth solving before more analytics work lands.
- **after [PR #1 comment-quality review]** — 2026-05-02: artifact written to `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/review/comment-quality-findings.md`. Verdict: APPROVE w/ 1 HIGH (stale path in page.tsx header) + 1 MEDIUM (sprint-tied temporal framing) nit-fixes.
- **before [PR #1 test-coverage review]** — 2026-05-02: read-only — risk is over-flagging (this is presentation-only per scope; vitest tooling gap is preexisting and out of scope to fix in this sprint).
- **after [PR #1 test-coverage review]** — 2026-05-02: one ongoing-risk item surfaced and none introduced. **Real risk:** snapshot JSON ↔ TS shape drift is unguarded — the seven `as unknown as Type` casts at `src/app/analytics/page.tsx:86-92` will silently degrade to NaN/0% bars if Python rebuilders rename fields upstream. No exception, no skip code, just empty-looking charts. Mitigation is cheap (a one-snapshot-one-test pattern under `src/data/analytics/*.test.ts`, ~7 × 30 lines, zero new deps) but **out of scope for PR #1** by author's note. **Watch items:** (i) before merging the next analytics sprint, decide whether to absorb the snapshot-shape test sprint as a precondition or accept the drift risk knowingly; (ii) the page-private `fmtCents`/`pct`/`safeMax`/`fmtDate` helpers are duplicated-by-pattern — a future agent porting analytics elsewhere may re-implement instead of importing them, so the extraction (Finding 2) has a half-life — earlier is cheaper; (iii) `vitest` tooling baseline still needs `npm install` to actually run tests — not introduced by this PR, mentioned for completeness.

- **before [PR #1 review synthesis]:** Five separate findings artifacts; risk of (a) double-counting a finding two agents flagged the same way, (b) losing nuance when collapsing into a single PR comment, (c) over-blocking on items that scope explicitly excluded.
- **after [PR #1 review synthesis]:** No conflicts between agents — all five converged cleanly. **Aggregate state of PR #1: 4 HIGH (2 real bugs + 1 stale comment path + 1 follow-up snapshot-validation sprint), 6 MEDIUM (4 auto-fixable here, 2 deferred to extraction sprint), 4 LOW (1 moot once HIGH 2 fixes, 3 polish).** **Watch items:** (a) the duplicate `_reference/CC Agent (legacy)/...` path appears as **on-page rendered text** at `src/app/analytics/page.tsx:737-738` too — auto-fix must scrub both the comment AND the JSX citation, easy to miss; (b) HIGH 2 funnel-arrow fix removes the only inter-row arrow that exists today — visual cohesion check needed in browser; (c) cohort grid sort fix changes which 12 rows render in the heatmap — `bestCohort`/`worstCohort` summary line was already correct (server-side), so it should now agree with the table; visually verify post-fix; (d) snapshot-validation tests (HIGH 4) belong to a dedicated sprint — flag explicitly when scoping the next analytics work that the drift risk is accepted until then.
- **before [MCP auth fix]** — root cause of 'MCP not working': wrong Authorization scheme. Bearer vs Basic. CLOSE_MCP_AUTH_HEADER was set to empty string in .env.local so the fallback logic ran with the wrong format.
- **after [MCP auth fix]** — closed. Close's MCP server uses HTTP Basic auth (same as REST), not Bearer. The system prompt's MCP tool catalog is now actually invokable end-to-end.

- **after [proposals plates + workbench overlay]** — 2026-05-05: fixed the Proposals page overload problem where each column card repeated the whole proposal/editing surface inline. The board now separates scanning from editing: plates are lightweight, and deep work happens inside one modal/workbench pattern. Remaining product debt: Graph and Delegations still use their own overlay layouts; the Proposals workbench is now the clearer pattern to extract/unify across those surfaces.

- **before [plan-discovery split]** — 2026-05-05: Current Plan tab is overloaded: strategy brief, known/unknowns, stop conditions, best question, and 7-day sequence all compete in one panel. Discovery exists now and is the correct home for the dossier. Need to avoid breaking approve/refine/kill flow while moving display-only context.
- **after [plan-discovery split]** — 2026-05-05: Overload resolved for the lead Plan tab. Watch item: Discovery Profile currently reads the latest Supabase plan fields; when the `leads-data` branch/folder architecture reaches Atom 8, this same visual surface should swap to `06_discovery.md` reads while structured plan state stays in Supabase.

- **before [lead assets + rich-text capability design]** — 2026-05-05: Current agents mostly design text touches. Missing capability: approved assets and rich-text bodies that Andre/agents can attach or reference in SMS/email/note decisions. Risk: fake upload/attachment controls, unsafe HTML, unclear source-of-truth between Supabase assets and future leads-data folders.

- **after [lead assets + rich-text capability design]** — 2026-05-05: Main risks identified: SMS is not rich text and Close Workflow SMS attachments are constrained; HTML must be sanitized to Close XHTML subset; assets need approval/source labels so enrichment/customer-facing boundaries stay intact; do not let LLMs invent image URLs or attach unapproved files. Implementation should start with storage/metadata + picker, then wire outbound surfaces.

- **before [asset library atom 1 — storage + Lead Box assets]** — 2026-05-05: Risks: broad cross-system surface, possible overlap with intake upload, and Close attachment semantics not fully wired. Keep this atom honest: upload/store/list/delete assets only, mark customer approval, and expose in Lead Box. Do not imply assets are being sent yet.

- **after [asset library atom 1 — storage + Lead Box assets]** — 2026-05-05: No TypeScript/build bugs. Watch items: (a) migration is staged but not applied; (b) assets are private and only preview/download with signed URLs, so future send/inline-image work needs an explicit publish/attach step; (c) outbound surfaces still do not reference asset IDs yet — next atom should add an AssetPicker to proposal/plan/delegation editors without claiming assets are sent.

- **before [lead-scoped delegations subtab]** — 2026-05-05: Current friction: from Lead Box there is no direct path into Delegations chat with the current lead selected. Opening global delegations/chat can preserve stale last state, causing the operator to talk about the wrong Box.
- **after [lead-scoped delegations subtab]** — 2026-05-05: Friction closed. Watch item: the deep link currently carries the lead id only, so the first paint may show `lead_xxx` until the chat lead-name cache/scope widget resolves display context. Behavior is correct: API calls receive `lead_id` and the chat no longer inherits a stale Delegations lead/thread.

- **before [GPT lead research folder + prompt]** — 2026-05-05: Risk: external GPT agents may write unstructured dumps, pull non-Andre leads, miss transcripts, overwrite client folders, or treat this as live app state. Need a strict folder contract + prompt with explicit ownership, selection, transcript, and GitHub write rules.
- **after [GPT lead research folder + prompt]** — 2026-05-05: Risk reduced by explicit rules in README + prompt: Close mutations forbidden, customer messages forbidden, non-Andre leads skipped, transcripts must be attempted, missing data labeled, and GitHub writes limited to `lead-research/`. Watch item: GPT/GitHub MCP may not create empty directories, so `.gitkeep` placeholders preserve `clients/` and `sweeps/`; per-client `raw/` folders are created when the first payload file is written.
