# Global

Running snapshot of project state. Tapped before & after every move.

---

## 2026-05-01

- Workspace `/Users/jakeaaron/ComeketoAgent/` initialized.
- Three-ledger workflow established: `Goals.md`, `Problems.md`, `Global.md`.
- `CLAUDE.md` written; tap rule set as top priority with announcement requirement (before + after).
- Scheduled-task deck cleared earlier today — only `atlas-tldr-index` remains active. 8 tasks disabled, 13 already-fired tasks left as-is on disk.
- Jake mentioned new functionality coming that motivated the wipe — details pending.
- **after inception:** All four files written (CLAUDE.md + 3 ledgers). Tap rule is now the single most important behavior in this directory.
- **architecture decided by Jake:** New app pivots off ChatGPT 5.5 + agents + Close CRM connection (just released). GitHub MCP becomes the coordination layer — Comeketo agent designs automations conceptually → posts to GitHub → ChatGPT agents poll on schedule → trigger when conditions met. Unlimited agents, unlimited schedules.
- **client-facing constraint:** Must read as an evolution of the existing app, not a rebuild. Aesthetic Asset Kit at `/Users/jakeaaron/Downloads/CC Agent/CCAgentindex/boxes/assets/Aesthetic Asset Kit` is the visual contract. Old `/Users/jakeaaron/Downloads/CC Agent/` is reference only — no wholesale cloning.
- **before [foundation alignment]:** Standing by for Jake's call on stack/location/creds before laying any brick.
- **alignment finalized:** OpenAI key received (treated as exposed, will land in gitignored `.env.local` at scaffold time). Supabase MCP confirmed as hosted HTTP. Defaults locked unless Jake vetoes: Next.js 14 + TS + App Router, project root at `/Users/jakeaaron/ComeketoAgent/app/`, GitHub hosted MCP (`https://api.githubcopilot.com/mcp/`), Supabase hosted MCP. Auth via static headers in Responses API tool config.
- **before [scaffold]:** Operator dropped a full `.env` covering OpenAI, Supabase (URL + publishable + secret), Close CRM, ClickUp. GitHub MCP auth still blank. Project layout: `app/` at root, Next.js routes under `app/src/app/`, lib helpers under `app/src/lib/`.
- **after [scaffold]:** App skeleton live at `/Users/jakeaaron/ComeketoAgent/app/`. Stack: Next.js 14.2 + React 18 + TS 5.6, with `openai@^4.77` and `@supabase/supabase-js@^2.46` installed. Three pages (Home, Settings, Test) and one API route (`/api/test`). Verified: `npm install` clean, `tsc --noEmit` zero errors. To launch: `cd app && npm run dev` → http://localhost:3000.
- **architecture clarified 2026-05-01:** MCP is for the *agent side* (scheduled ChatGPT agents that poll), not our *app side*. Our backend uses direct APIs: Octokit for GitHub, Supabase JS SDK for Supabase, OpenAI Responses for OpenAI. The coordination flow is: app writes plans → GitHub repo → external ChatGPT agents poll repo via their own MCP attachment → trigger. We never invoke MCP from our Next.js backend.
- **before [pivot]:** Jake updated package.json (added `@supabase/ssr@^0.10.2`, bumped `@supabase/supabase-js` to `^2.105.1`). Adding `octokit` for GitHub. Test page becomes three buttons: OpenAI / Supabase / GitHub.
- **after [pivot]:** App is now on the direct-API architecture. Stack: Next.js 14.2, React 18, TS 5.6, `openai@^4.77`, `@supabase/supabase-js@^2.105`, `@supabase/ssr@^0.10`, `octokit@^4`. Three test modes wired through `/api/test`. MCP env vars retained but unused by primary path. To validate end-to-end: `cd app && npm run dev` → `/test` → click each button.
- **before [strip MCP]:** GitHub PAT now present in `.env.local`. About to delete all MCP env vars and references — no side-door retained. Tightness > optionality.
- **after [strip MCP]:** App is now bare-metal direct-API. `.env.local` carries only what the app actually uses (OpenAI, Supabase URL+keys, GitHub PAT, Close + ClickUp reserved). No MCP cruft anywhere. Source of truth for the architecture rule lives in `project_comeketo_app_architecture.md` memory: no MCP in this repo, ever.
- **before [sync env]:** Convention being established now — `app/.env.local` is THE env file. The uploaded `.env` was a bootstrap, not an ongoing edit target. Syncing latest values over (GitHub PAT + NEXT_PUBLIC_ prefixed Supabase URL/publishable key) and updating env.ts read paths.
- **after [sync env]:** Locked. `app/.env.local` carries 10 vars (OpenAI, Supabase URL + publishable + secret, GitHub PAT, Close × 2, ClickUp × 3). env.ts reads them by plain names. Three buttons should now go green on `npm run dev`.
- **github repo locked:** Project repo is `https://github.com/RodbotCC/ComeketoAgent`. All git pushes target this remote. Not yet wired locally — `git init` and `git remote add` will land when we're ready to push the first commit.
- **before [hero page]:** Designing the entry-point hero. Jake's new template is at `/Users/jakeaaron/ComeketoAgent/Chat page/`. Aesthetic: warm cream/beige background, serif headline (regular + italic mix), 24h-dial graphic, bottom stats row. Auth not wired — Google button is visual stub.
- **after [hero page]:** Hero is live at `/`. Aesthetic locked: Fraunces + Inter + JetBrains Mono, paper palette (#EFEBE3 bg, #1A1815 ink), soft pastel accent dots on the daily clock. Three pages now: `/` (hero), `/settings` (admin status), `/test` (three connection checks). Each page renders its own `cme-shell` (header + main + footer). Layout.tsx is reduced to html/body + globals.css. tsc clean. `npm run dev` should render the screenshot exactly.
- **after [hero hotfix]:** GoogleBtn no longer carries an event handler. Save dev server, browser auto-reloads, error is gone.
- **before [layout rules]:** Establishing three Jake-locked design rules for every page in this app: pages fit the viewport, scrollbars hidden when they're inside widgets, widgets have visible dimensionality (border + soft shadow + cream card on pastel swim-lane bg). Identity reference: the four-box "Constitutional / Global Ledgers / Domain Boxes / Leaf Boxes" image.
- **after [layout rules]:** Three rules live in `globals.css` and in long-term memory (`feedback_layout_rules.md`). Hero now fits 100vh on a normal desktop. The `.widget`, `.lane`, `.lane-*`, `.scroll-hide`, `--shadow-sm/-md/-lg` primitives are ready to be used by every page from here on. Next move: absorb the new chat HTML into a `/chat` route, applying the rules and the four-box-palette identity.
- **before [chat page]:** Adding `/chat` route. Reuses the `cme-shell` 100vh frame plus chat-specific classes (`.cmk-*`) ported from Jake's HTML. Internal scroll is on the chat message column with hidden scrollbar — page itself stays fixed.
- **after [chat page]:** `/chat` is live. Routes in the app are now: `/` (hero), `/chat` (delegations chat), `/settings`, `/test`. Four colored title dots (#8B7355, #A89968, #6B8E5A, #9B8FB8) anchor the chat header — same identity language Jake wants across pages. Visual identity locked: cream paper bg + cream cards + thin 0.5px borders + colored 2px left-ribbons for tool/system/call-out blocks + subtle shadows. Static content; ready to be wired when OpenAI Responses backbone goes in.
- **before [unify with chat]:** Bringing every page in line with the chat. Paper bg lightens to `#F5F3EE`. Editorial mono is killed (replaced by Inter sans + small caps via letter-spacing + weight). Four title dots land in front of every wordmark. Hero wordmark navigates to `/chat`.
- **after [unify with chat]:** Visual identity is now consistent across `/`, `/chat`, `/settings`, `/test`. Four-dot identity + lighter paper + sans small-caps eyebrows + tighter type. Navigation loop: hero logo → /chat. Chat / settings / test logos → /. Settings + test utility nav exposes all three admin destinations. Hero is the marketing page; chat is the app.
- **before [functional foundation]:** Adding `/intake` route. Extracting `AppHeader` + `TabNav` + `icons` to `src/components/` for reuse across chat / intake / future authenticated pages. Adding `lib/settings.ts` (file-backed JSON), Server Action for model picker, `/api/chat` Responses endpoint, client-side ChatPanel with real input.
- **after [functional foundation]:** Five routes now wired: `/`, `/chat`, `/intake`, `/settings`, `/test`. The chat input is functional — types message → POST `/api/chat` → Responses API call with the model picked in `/settings` → reply renders in-line. Demo turns show on first load, vanish once real conversation begins. Shared `AppHeader` + `TabNav` keep visual consistency across `/chat` and `/intake`. Three `gpt-5.4-*` models exposed in the picker. `.cmk-settings.json` writes happen via Server Action. Stack still: Next 14 + React 18 + TS, openai, octokit, supabase-js. App is now functionally alive.
- **before [add vercel cli]:** Adding Vercel CLI to the toolchain. Goal: `vercel dev` for edge/serverless parity, `vercel env pull` for env sync, preview deploys per branch. App still Next.js 14 / App Router; nothing changes in code yet.
- **before [intake rename + research]:** Renaming "Rodbot" → "Comeketo Agent". Sourcing intake's true concept from the previous build at `/Users/jakeaaron/Downloads/CC Agent/`.
- **after [intake rename + research]:** Rodbot strings purged. Intake concept locked: a workspace of named **intake reports**, each one is `documents[] + conversation[]` over those docs. Schema lives in `CC Agent/CCAgentindex/intake_reports/*.json` (three live examples: test, check_thsi, soliton_collision_theory). Each document is OCR'd into `raw_text`, summarized, structured (theme/metrics/concepts), classified (kind/confidence). The `← reports` button implies a list-view route that doesn't exist yet. Our current `/intake` is one report; we'd want `/intake` (list) + `/intake/[slug]` (single report) for full alignment.
- **before [chat depth]:** Pivoting full attention to `/chat` per Jake. Adding Supabase persistence (threads + messages), vision (image attach), markdown rendering (react-markdown), and a working "+ NEW" thread flow. Chat becomes the central surface of the app.
- **after [chat depth]:** `/chat` is now a functional, persistent, multimodal surface. Schema in Supabase: `threads(id, title, created_at, updated_at, archived_at)` + `messages(id, thread_id fk, role, content, attachments jsonb, created_at)`, RLS on with service_role full access. Thread list loads from Supabase, "+ new" button starts a fresh thread (created on first send so empty threads don't pile up), clicking any thread switches context. Image attach via file picker → base64 → packed into OpenAI Responses input as `input_image` content blocks alongside `input_text`. Assistant replies render as markdown (headings / lists / code / tables / blockquotes / links). Stack now includes `react-markdown@^9` + `remark-gfm@^4`. App is meaningfully alive — type a message, attach a screenshot, get a real reply, threads persist across page reloads.
- **after [add vercel cli]:** App is now Vercel-linked. Project: `expressedais-projects/comeketo-agent`. Toolchain additions: `vercel dev` (serverless-parity local), `vercel` (preview deploy from current branch), `vercel --prod`. No code or behavior change yet. Env vars on Vercel side are empty — pushing them up is a separate, deliberate move (`vercel env add` per var, or `vercel env pull` after seeding the dashboard).
- **after [push to RodbotCC]:** Code is now on GitHub at `RodbotCC/ComeketoAgent` (public, main branch). Local git origin set, RodbotCC user identity in repo config. Working-dir root is the git root; `app/` is a subdirectory (so Vercel will need `Root Directory = app` once the repo is connected on the dashboard side).
