# Goals

Running ledger of what we're building toward. Tapped before & after every move.

---

## 2026-05-05

- **after [harness/ Phase 3+4+5: ledger + plan mirror + audit ledgers]:** Done. **Phase 3** — new [`src/lib/harness-ledger.ts`](src/lib/harness-ledger.ts): `appendLedger`, `readLedgerDay`, `readRecentLedger`, `filterLedgerByLead`. Direct read-modify-write to `harness/ledger/YYYY-MM-DD.jsonl` via Octokit with SHA-retry × 3 (80/240/600ms). [`execution-audit.ts`](src/lib/execution-audit.ts) `logExecution` now dual-writes — Supabase first, then `void appendLedger(...)` (fire-and-forget). 10 tests in [`harness-ledger.test.ts`](src/lib/harness-ledger.test.ts) covering UTC date paths, JSONL parse robustness (skips blank lines + malformed lines), per-lead filter. **Phase 4** — new [`src/lib/lead-plan-fs.ts`](src/lib/lead-plan-fs.ts): `mirrorPlanToFile(plan)` writes `plan.json` to lead's harness folder via `writeLeadFile`. Wired into all 9 plan mutators in [`plans-db.ts`](src/lib/plans-db.ts) (savePlan, approvePlan, killPlan, pausePlan, appendRequiredActionToPlanDay, editPlanDayTouch, deletePlanDayTouch, updatePlanDay, replacePlanDays, setDayStatus). New helper `refreshAndMirrorPlan(planId)` for the mutators that don't have the full plan in scope. Supabase remains canonical during dual-write — Phase 6 flips. **Phase 5** — new [`src/lib/harness-approvals.ts`](src/lib/harness-approvals.ts): `appendApprovalAudit` writes to `harness/approvals/YYYY-MM.jsonl` (monthly partition). New [`src/lib/harness-heartbeat.ts`](src/lib/harness-heartbeat.ts): `writeHeartbeatRunSnapshot` writes one JSON file per run to `harness/heartbeat/YYYY-MM-DD/{run_id}.json`. Wired into both `heartbeat_runs` insert sites in [`heartbeat.ts`](src/lib/heartbeat.ts) (per-lead `runHeartbeatForPlan` + sweep `runHeartbeatForAll`). [`execution-audit.ts`](src/lib/execution-audit.ts) `logApprovalChange` now dual-writes via `appendApprovalAudit`. **Suite 120/120 green; tsc clean.** **Phase 6** (drop Supabase dual-writes — the lock-in) deferred to overnight soak per the plan; doing it the same session as the writers ship would couple the new infra's reliability to the old observability. After 24h of clean operation, Phase 6 is a small commit removing the Supabase insert lines.

- **before [harness/ Phase 3+4+5: ledger dual-write + plan mirror + audit ledgers]:** Continuing the transfer per Jake. Phase 3: new `src/lib/harness-ledger.ts` writes every `logExecution` row to `harness/ledger/YYYY-MM-DD.jsonl` via Octokit (dual-write — Supabase still canonical). Phase 4: plans mirror to `harness/leads/{id}__{slug}/plan.json` after Supabase writes (file is read-replica for the chat agent; flipping canonicality deferred to Phase 6). Phase 5: `logApprovalChange` writes to `harness/approvals/YYYY-MM.jsonl`; heartbeat run snapshots write to `harness/heartbeat/YYYY-MM-DD/{run_id}.json`. Phase 6 (drop Supabase dual-writes) deferred to overnight soak — too risky to ship same session.

- **after [harness/ collapse to single-branch]:** Done. `harness/` now lives on `main` alongside `src/`. `GITHUB_LEADS_BRANCH` defaults to `main` (was `leads-data`). `.vercelignore` excludes `harness/` from deploy bundle. CLAUDE.md + `harness/README.md` + `harness/leads/README.md` updated to drop "parallel histories" framing. Tests updated for new branch default. **Suite 110/110 green; tsc clean.** Pending Jake: (1) set Vercel "Ignored Build Step" command to `git diff --quiet HEAD^ HEAD -- ':!harness'` so cron sweep commits don't rebuild; (2) optional `git push origin --delete leads-data` after this commit lands on origin.

- **before [harness/ collapse to single-branch]:** Per Jake — two-branch architecture was creating cognitive friction. Pulling `harness/` from `leads-data` onto `main`. Vercel rebuild concern handled via Vercel "Ignored Build Step" command — skips builds when only `harness/**` changed. Single branch, single mental model. ~30 min.

- **after [harness/ overhaul — Phase 1+2]:** Done. **`leads-data` branch** (commit `3eed57f` already on origin): renamed `_leads/` → `harness/leads/`; scaffolded 11 new top-level harness dirs each with `.gitkeep` + `README.md` (`ledger/`, `approvals/`, `heartbeat/`, `automations/`, `catalog/`, `staff/`, `venues/`, `people/`, `intelligence/`, `summaries/`, `catalog-content/`); new top-level `harness/README.md` documents the architecture, the file-shape contract, the writer/batching strategy, and what stays in Supabase as auxiliary memory. **`main` branch (this commit)**: `lead-folder.ts` `LEADS_ROOT` constant flipped to `harness/leads`; `findLeadFolderPath` now dual-probes `harness/leads/` first then falls back to legacy `_leads/` with a one-time warn (fallback removed one phase from now); test fixtures updated (110/110 green). New file `src/lib/intake-fs.ts` (~250 lines) with `writeIntakeArtifact`, `listIntakeArtifactsForLeadFs`, `getIntakeExtractedTextFs`, `deleteIntakeArtifactFs`, and `intakeArtifactPath` helper. New `src/lib/intake-extract.ts` factored out from the old extract route — pure text extraction (PDF/JSON/HTML/plain text) with image+audio/video deferred to Phase 2 (Gemini). Rewrote `/api/intake/upload/route.ts` to extract inline + write directly to `harness/leads/{id}__{slug}/intake/{intake_id}/{meta.json,extracted.md}` via Octokit. Stubbed `/api/intake/extract/route.ts` as a deprecated no-op (returns `{ok:true, deprecated:true}`) so legacy callers (ChatPanel attachment-extract fallback) don't 404. Modified `intake-artifacts.ts` so the existing `IntakeArtifactRow` shape is preserved but reads now go through `intake-fs.ts`. Added new `loadIntakeArtifactsWithText(leadId, limit)` which the chat agent now uses to inject extracted intake text into the system prompt (replaces the old `listIntakeArtifactsForLead` + extracted_text-on-row path). Removed the separate "extracting" fetch from `LeadIntakeBoard.tsx` since extraction is now part of upload. **What stays in Supabase**: `close_webhook_events`, `threads`+`messages`, `lead_activity_touches`, storage buckets for >1MB binaries — all per the plan's auxiliary list. **Phases 3-6** (global ledger dual-write, plans-fs, audit cutover, full wind-down) are the following week's work; the harness scaffolding is in place ready for them.

- **before [harness/ overhaul — Phase 1+2 of 6]:** Most-important-overhaul-of-the-build per Jake. Plan at `~/.claude/plans/this-is-going-to-cryptic-turing.md` approved. This session ships intake migration + `_leads/` → `harness/leads/` rename + scaffolds the broader harness top-level dirs (ledger, approvals, heartbeat, automations, catalog, staff, venues, people, intelligence, summaries, catalog-content). Code changes: new `src/lib/intake-fs.ts`, modified `lead-folder.ts` with dual-probe fallback, modified intake upload + read paths. Branch ops: rename `_leads/` → `harness/leads/` on `leads-data` via Octokit. ~90 min target. Phases 3-6 (ledger dual-write, plans-fs, audit cutover, Supabase wind-down) are the following week's work.

- **after [per-lead memory as files — Atom 4: `lead-folder-sweeper.ts` orchestrator]:** Done. New file [`src/lib/lead-folder-sweeper.ts`](src/lib/lead-folder-sweeper.ts) (~140 lines) exports `sweepLead(leadId): SweepLeadResult` (hydrate → render → diff against existing folder via byte comparison → `writeLeadFile` only the deltas; idempotent — re-running with no Close-side changes = zero writes) and `sweepActiveLeads(opts?): SweepActiveLeadsSummary` (Andre-owned + non-terminal status filter, 3-lane worker pool, per-lead errors caught into `errors[]`, structured summary with `considered`, `in_scope`, `swept[]`, `errors[]`, `started_at`/`finished_at`). Plus `isLeadInScope(lead)` exported for testing the filter rule. Terminal status set: `["✅ Won", "🔴 Lost", "🔴 Not Interested"]` — exact strings to be confirmed at smoke. Added [`listLeadFolderFiles(leadId)`](src/lib/lead-folder.ts) as the diff input source (returns `Map<relPath, {sha, content}>` of existing folder state, or null if folder missing). Companion test [`src/lib/lead-folder-sweeper.test.ts`](src/lib/lead-folder-sweeper.test.ts) — 6 tests for `isLeadInScope` covering Andre vs other-owned, all three terminal labels, undefined status (in-scope by default), positive-case in-progress labels. **Full suite 105/105 green; tsc clean.** Smoke against real Close + GitHub deferred to Atom 5 (manual sweep button on `/test`). Next: Atom 5 — Vercel cron route + manual trigger button on `/test`. This is where `leads-data` finally gets pushed to origin so the GitHub round-trip works.

- **after [Sales Playbook v2.0 integration — agent fluency in Andre's process]:** Done. Jake flagged that the agent had NEPQ *voice* wired (regex linter blocks fake-warmth phrases) but NOT the actual playbook content (mindset, call control rules, NEPQ progression structure, inbound script architecture, objection idioms, the 5-day cadence, sales stages). New file [`_reference/sales-playbook.md`](_reference/sales-playbook.md) — full 2,254-line playbook saved as durable reference. New file [`src/lib/sales-playbook.ts`](src/lib/sales-playbook.ts) exports `SALES_PLAYBOOK_CONDENSED` (~250-line distillation of mindset + 3 call control rules + Mirror→Sounds-Like→Open loop + NEPQ progression A-G + objection idioms + tonality + 5-day cadence + stage definitions + voice rules) and `getSalesPlaybook({ tight })` accessor (tight version drops idioms + scripts to ~80 lines for token-tight prompts). Injected into: (a) `src/lib/lead-folder-llm.ts` PROFILE_SYSTEM and DISCOVERY_SYSTEM (tight version) — the per-lead profile and discovery doc generators now write playbook-aware prose. (b) `src/app/api/chat/route.ts` DEFAULT_INSTRUCTIONS (full version) — the chat agent reads the framework on every turn. Cost: adds ~1500 tokens to chat system prompt and ~600 tokens to each LLM regen call. Worth it. tsc clean; 110/110 tests green. **Now ready to push.**

- **after [per-lead memory as files — Atoms 9 + 10: cleanup + architecture record]:** Done bundled. **Atom 9** — moved deprecated migration `20260505200000_lead_facts.sql` from `supabase/migrations/` to `supabase/_deprecated/` (Supabase CLI ignores files outside the migrations dir). New [`supabase/_deprecated/README.md`](supabase/_deprecated/README.md) explains why and includes the safe `DROP TABLE IF EXISTS public.lead_facts CASCADE;` Jake can run when he's ready. **Did NOT** delete `lead-facts.ts` / `journey-score.ts` — they're load-bearing for the Discovery page's existing SQL-backed surface (which Atom 8 left in place). Both are wrapped in try/catch so a missing/empty `lead_facts` table returns empty Map without breaking the page. Future cleanup pass can rip them out once Jake confirms the file-backed surface reads better in practice. **Atom 10** — added a new "Per-lead memory architecture (locked 2026-05-05)" section to [`CLAUDE.md`](CLAUDE.md). Names the file layout, the direct-REST-not-MCP hydration rule, what stays in Supabase (execution_log, heartbeat_runs, close_webhook_events, automation_drafts, intake_artifacts, plans, lead_assets), what moved to files (profile, discovery slots, NEPQ openers, comms history, transcripts), and the rule that future agents shouldn't add new Supabase tables for lead-scoped narrative content. **All 11 atoms (0-10) of the per-lead-memory build are now complete.** Suite 110/110 green; tsc clean.

- **after [per-lead memory as files — Atom 8: Discovery page reads from folder] — additive variant:** Done. Took the additive path rather than ripping out the existing SQL-backed surface — Andre demo imminent, can't risk regressions on a live page. New helpers in [`src/lib/lead-folder.ts`](src/lib/lead-folder.ts): `stripFrontmatter(md)`, `readLeadProfileBody(leadId)`, `readLeadDiscoveryBody(leadId)`. `/lead/[id]/discovery` now reads `04_profile.md` + `06_discovery.md` in parallel with `journeyScoreForLead`, displays both as a new lavender panel at the bottom (`📄 Profile & Discovery — generated`) with each as an open `<details>` rendering the markdown body in `pre` (real markdown rendering deferred — operator can read raw md fine for v1). Empty state when neither is generated yet shows a quiet pointer to `/test` → Regenerate. Original SQL-backed surface (slot grid, scores, quest, restraint) untouched. Personal page deferred — different aggregate shape, not load-bearing for demo. **Full swap (delete SQL surface entirely) is a later atom** once Jake confirms the file-backed surface reads better in practice. tsc clean; 110/110 tests green.

- **after [per-lead memory as files — Atom 7: LLM regen for `04_profile.md` + `06_discovery.md`]:** Done. New file [`src/lib/lead-folder-llm.ts`](src/lib/lead-folder-llm.ts) (~230 lines) exports `regenerateLeadProfile(leadId)`, `regenerateLeadDiscovery(leadId)`, and `regenerateAllLeadDocs()` orchestrator. Each regen: reads `00_meta.json` for `comms_content_hash` + lead name → reads existing target file, parses frontmatter `from_hash:` → if matches, **skip LLM call entirely** (returns `{regenerated: false, reason: "hash_match"}`). On miss: reads `01b_comms_verbatim.md` (capped at 24k chars), runs OpenAI Responses with hand-tuned system prompt (NEPQ voice rules, conservative-don't-invent rule), wraps output in YAML frontmatter `{close_lead_id, lead_name, generated_at, from_hash}`, writes via `writeLeadFile`. Two distinct system prompts: profile = prose with Snapshot/Identity/Win Angles/Risks/NEPQ-Openers sections; discovery = slot evidence table + Current Quest + NEPQ ask. Orchestrator runs concurrency-1 against OpenAI (cost predictability over speed). [`/test` page](src/app/test/page.tsx) gains 7th mode "Regenerate lead docs" wired through [`/api/test/route.ts`](src/app/api/test/route.ts) `testLeadRegen()`. Companion test [`src/lib/lead-folder-llm.test.ts`](src/lib/lead-folder-llm.test.ts) — 5 tests on the frontmatter-hash extraction primitive (the cost-control linchpin). **Suite 110/110 green; tsc clean.** Cron does NOT yet auto-trigger regen — operator manual only for now until smoke confirms quality. **Next: Atom 8** — repoint `/lead/[id]/discovery` and `/personal` to read from these files instead of `lead_facts` Supabase table.

- **after [per-lead memory as files — Atom 6: webhook → comms_dirty flip]:** Done. New helper `markLeadCommsDirty(leadId)` in [`src/lib/lead-folder.ts`](src/lib/lead-folder.ts): reads `00_meta.json`, sets `comms_dirty: true` + `last_dirty_at: <iso>`, writes back. Returns `"flipped" | "already_dirty" | "no_folder"` so the caller can log honestly. [`src/app/api/webhooks/close/route.ts`](src/app/api/webhooks/close/route.ts) now calls it whenever an event arrives with `lead_id`, wrapped in try/catch so GitHub failures don't break the webhook ack (Close would otherwise retry → duplicate event noise). Logs the flip result via `logStructured`. tsc clean; suite 105/105 green. **Next: Atom 7** — LLM regen for `04_profile.md` and `06_discovery.md`.

- **before [per-lead memory as files — Atom 6: webhook → comms_dirty flip]:** Add `markLeadCommsDirty(leadId)` to `src/lib/lead-folder.ts` — reads existing `00_meta.json`, sets `comms_dirty: true`, writes back via `writeLeadFile`. No-op if folder doesn't exist yet (sweeper creates on next pass). Call it from `/api/webhooks/close/route.ts` whenever an event arrives with `lead_id`. Triggers on ANY event for the lead (activity, status change, opportunity, custom field, contact) — broad on purpose; sweeper's byte-diff means "spurious" dirty writes are cheap. Wrapped in try/catch so GitHub failures don't break webhook (which would cause Close to retry). Synchronous Octokit call ~250ms = acceptable latency for a webhook ack.

- **after [ship everything to main — Andre demo prep]:** Live. `318a301..746feee main -> main`. Vercel rebuilding now. Lead-memory cron + sweep button + workflow polish + lead-assets all on prod within ~2 minutes.

- **before [ship everything to main — Andre demo prep]:** Pushing all three streams as-is per Jake's call: lead-memory Atoms 0-5, workflow chat polish (parallel), lead-assets feature (parallel). tsc clean. Single commit on main, will trigger Vercel rebuild + activate the 2-hour sweep cron.

- **after [per-lead memory as files — Atom 5: Vercel cron + manual `/test` button] — code phase:** Code shipped; smoke pending Jake's push of `leads-data`. Files: new [`src/app/api/cron/sweep-leads/route.ts`](src/app/api/cron/sweep-leads/route.ts) (GET+POST, `maxDuration=300`, single `lead_id` query param for targeted re-sweep). Auth via existing `src/middleware.ts` Bearer-only check on `/api/cron/*` — no operator cookie path needed because the manual button takes a different route. [`src/app/api/test/route.ts`](src/app/api/test/route.ts) gains `testLeadSweep()` mode (compacts `swept[]` to top-25 entries to keep result pane readable) + `maxDuration=300`. [`src/app/test/page.tsx`](src/app/test/page.tsx) gains 6th MODES entry "Run lead sweep". [`vercel.json`](vercel.json) cron entry `/api/cron/sweep-leads @ 0 */2 * * *`. [`src/lib/env.ts`](src/lib/env.ts) gains `CRON_SECRET` key. **Full suite 105/105 green; tsc clean.** **Pending:** push `leads-data` to origin (one-time, separate from main, no Vercel rebuild) → set `CRON_SECRET` in `.env.local` (and Vercel env when ready) → run `npm run dev` + click "Run lead sweep" on `/test` → verify summary + spot-check folders on github.com/RodbotCC/ComeketoAgent/tree/leads-data/_leads/active. Currently asking Jake for the go-ahead on the push since CLAUDE.md says "always ask before pushing."

- **before [per-lead memory as files — Atom 5: Vercel cron + manual `/test` button]:** New route `src/app/api/cron/sweep-leads/route.ts` — handles GET (Vercel Cron's default) AND POST (manual button), gated by `CRON_SECRET` header. Calls `sweepActiveLeads()`, returns the structured summary as JSON. New `vercel.json` cron entry — every 2h. `/test` page gets 6th mode "Run lead sweep" — POSTs the cron route with the secret, renders the summary in the test result pane. Adds `CRON_SECRET` env var. Pre-flight: push `leads-data` to origin one-time (separate from main, no Vercel rebuild) so Octokit reads/writes resolve. Smoke: click button → summary JSON → visit github.com/RodbotCC/ComeketoAgent/tree/leads-data/_leads/active → spot-check one lead's `01b_comms_verbatim.md` + `comms/*.json` for transcript content.

- **before [per-lead memory as files — Atom 4: `lead-folder-sweeper.ts` orchestrator]:** Glue layer between hydrate (Atom 2), render (Atom 3), and write (Atom 1). New file `src/lib/lead-folder-sweeper.ts` exports `sweepLead(leadId)` and `sweepActiveLeads()`. `sweepLead`: hydrate → render → list current folder state via new `listLeadFolderFiles` helper → for each rendered file, decode existing base64 and compare to new content; skip if identical, else `writeLeadFile`. Idempotent — re-running with no changes = zero commits. **Never deletes** files (operator overrides + historical comms preserved). `sweepActiveLeads`: `closeListLeadsByAssignee(CLOSE_USER_ID_ANDRE)` filtered to status not in (Won/Lost/Not-Interested), 3-lane worker pool over `sweepLead`, returns `{ swept, skipped_unchanged, errors }`. Adds `listLeadFolderFiles(leadId)` to `lead-folder.ts` for the diff step. Per-file commits for now (one per changed file, ~1-3 per active lead per sweep). One-commit-per-lead via Git Data API tree-build is a watch-item.

- **after [per-lead memory as files — Atom 3: `lead-folder-renderer.ts`]:** Done. New file [`src/lib/lead-folder-renderer.ts`](src/lib/lead-folder-renderer.ts) (~340 lines, pure function) exports `renderLeadFolder(hydration): FileMap`, plus `contentHash` (sha256 of sorted `{activity_id}:{date_updated}` tuples — stable across re-renders unless something material changed) and `activityFilename` (matches Eliana's convention: `comms/{kind}_{YYYY-MM-DD}_{lastEightCharsOfId}.json`, lowercase). Produces:
  - `00_meta.json` — lead identity, contacts derivation, status, counts, content hash, `comms_dirty=false`, `last_sweep_at`.
  - `01_comms_digest.md` — counts strip, recent fires (us → lead) table, inbound activity (lead → us) table, latest call transcript snippet (1.2k char excerpt) when one exists.
  - `01b_comms_verbatim.md` — Eliana-style block-per-activity with emoji headers (📧/💬/☎️/📅/📝), Close link, activity_id anchors, blockquoted bodies. Calls render operator note + transcript + Close auto-summary as separate quoted sections.
  - `client_ledger.md` — state table (lead, status, owner, last sweep, activity total, active subscriptions), recent fires table, plan-cadence placeholder pointing at Atom 10 plan-mirror.
  - `comms/{kind}_{date}_{shortid}.json` — one file per activity, full Close payload (incl. `recording_transcript`).
  Companion test [`src/lib/lead-folder-renderer.test.ts`](src/lib/lead-folder-renderer.test.ts) — 16 tests across file-map shape (always 4 canonical files; never emits 04/06/10 — those belong to LLM regen), `comms/` filename convention (validates `acti_GOY8LzV84GHpZPylbkjUcMohj3LjS0581XgaSnaTj4B` → `comms/call_2026-04-23_asnatj4b.json` exactly per Eliana's payload), `00_meta.json` field population, `contentHash` stability/order-independence/change-on-add, verbatim email/call/no-transcript rendering, digest outbound/inbound grouping. **Full suite 99/99 green; tsc clean.** Helper functions (`stripHtml`, `blockquote`, `escMd`, `previewOf`) are private but battle-tested by the rendering tests. Next: Atom 4 — `src/lib/lead-folder-sweeper.ts` — orchestrator that hydrates → renders → diffs against the `leads-data` branch → writes only changed files in one commit per lead.

- **before [per-lead memory as files — Atom 3: `lead-folder-renderer.ts`]:** Pure function `renderLeadFolder(hydration: LeadHydration): Map<filepath, content>`. Produces (matching Eliana's shape, minus profile/discovery deferred to Atom 7):
  - `00_meta.json` — small machine-readable header (lead_id, name, slug, contact_id, primary_phone/email, status_label, custom-field-derived event_date, plan_day_1_date placeholder, comms_dirty=false, last_sweep_at=fetched_at, comms_content_hash=sha256 of verbatim content).
  - `01_comms_digest.md` — narrative digest: counts strip, recent-fires table (Andre→lead), inbound activity table (lead→Andre), latest call transcript snippet if any.
  - `01b_comms_verbatim.md` — every email/SMS/call/thread rendered to markdown with activity_id anchors back to Close URL. Mirrors Eliana's section style ("📧 Email (outgoing) — date · Subject ... > Body blockquote").
  - `client_ledger.md` — cadence position table (placeholder until Atom 10 plan-mirror lands). Recent-fires table here for now.
  - `comms/{kind}_{YYYY-MM-DD}_{shortid}.json` — one file per activity carrying the FULL Close payload (calls include transcript). `shortid` = last 8 chars of activity_id, lowercase.
  Pure function — no side effects, no Octokit. Unit tested with a fixture `LeadHydration`. Done = renderer output for Eliana fixture matches the hand-built folder shape.

- **after [per-lead memory as files — Atom 2: `close-hydrate.ts` direct-REST puller]:** Done. Two files. New [`src/lib/close-hydrate.ts`](src/lib/close-hydrate.ts) exports `hydrateLead(leadId): Promise<LeadHydration>` returning the bucketed shape `{ lead, calls[enriched], emails, smses, meetings, notes, tasks, threads, subscriptions, unknown_activities, fetched_at, activity_total }`. Bulk fetch uses existing `closeGetLeadFull` (lead + 500-cap paginated activity feed + email threads + subscriptions in parallel). Per-call enrichment: `enrichCalls()` runs a 5-lane worker pool over `closeGetCall(activityId)` to grab `recording_transcript` (list endpoint trims it). Failures (expired recording, transient 5xx) fall back to the trimmed list row so the lead still hydrates with the call present. `bucketActivities()` exported separately as a pure function for the renderer (Atom 3) to reuse. [`src/lib/close.ts`](src/lib/close.ts) gains `closeGetCall(activityId)` doing GET `/activity/call/{id}/`. Companion test [`src/lib/close-hydrate.test.ts`](src/lib/close-hydrate.test.ts) — 5 tests covering canonical bucketing, `TaskCompleted` co-location with `Task`, fidelity preservation in `unknown_activities` for unrecognized types like `LeadStatusChange`/`Created`, within-bucket order preservation. **Full suite 83/83 green; tsc clean.** No real Close API call exercised yet — that happens at Atom 4 sweeper smoke. Next: Atom 3 — `src/lib/lead-folder-renderer.ts` — pure function that turns a `LeadHydration` into the `Map<filepath, content>` matching Eliana's folder layout.

- **before [per-lead memory as files — Atom 2: `close-hydrate.ts` direct-REST puller]:** New file `src/lib/close-hydrate.ts` exporting `hydrateLead(leadId)` returning enriched shape `{ lead, calls[], emails[], smses[], threads[], meetings[], notes[], tasks[], subscriptions[], fetched_at }` where each `calls[]` entry is the FULL Close call payload including `recording_transcript` (the list endpoint trims it; per-call GET is required). Reuses `closeGetLeadFull` for the bulk fan-out (already does lead + paginated activities + email_threads + subscriptions in parallel) and adds a new lightweight `closeGetCall(activityId)` helper to `close.ts` for the per-call enrichment. Concurrency cap = 5 on the per-call GETs to stay polite to Close's rate limit. Returns raw payloads — pure data, no rendering. Renderer (Atom 3) turns this into the file map.

- **after [per-lead memory as files — Atom 1: lead-folder.ts pure helpers]:** Done. New file [`src/lib/lead-folder.ts`](src/lib/lead-folder.ts) (~270 lines) exports: `slugify(name)`, `leadFolderPath(leadId, name, state)`, `findLeadFolderPath(leadId)` (scans active then archive for `{leadId}__` prefix match), `readLeadFile(leadId, fileName)` (returns null on absent file/folder, base64-decoded otherwise), `writeLeadFile(leadId, leadName, fileName, content, opts)` with optimistic-SHA retry on 409/422 (3 attempts at 50/150/400ms backoff = <1s total), and `archiveLead(leadId, leadName)` (per-file copy + delete, returns `{moved}` or `{skipped}`). All Octokit calls target `env.GITHUB_LEADS_BRANCH` (default `leads-data`), `env.GITHUB_LEADS_OWNER/REPO` (default `RodbotCC/ComeketoAgent`). Companion test [`src/lib/lead-folder.test.ts`](src/lib/lead-folder.test.ts) — 13 tests across slugify edge cases (Eliana, Sakamoto, Sunny ☼☼☼ → "unnamed", accented names, length cap, apostrophe handling), path composition, retry pacing, env defaults. Full suite **78/78 green**, tsc clean. **Smoke test against real GitHub deferred to Atom 5** — needs `leads-data` pushed to origin and the manual-sweep `/test` button as the trigger surface; doing it standalone now would require pushing the branch outside the rest of the work, which contradicts Jake's "wait until everything's done" call. Per-file `archiveLead` is non-atomic (N files = 2N commits); Git Data API tree-build can replace it in Atom 4 if the volume bites. Next: Atom 2 — `src/lib/close-hydrate.ts` (direct-REST per-lead pull including per-call `recording_transcript`).

- **before [per-lead memory as files — Atom 1: lead-folder.ts pure helpers]:** New file `src/lib/lead-folder.ts` — slugify, path helpers, Octokit read/write with optimistic-SHA retry × 3, archiveLead. All targeting `branch: "leads-data"`. Env additions `GITHUB_LEADS_OWNER` + `GITHUB_LEADS_REPO` with sensible defaults. Unit tests for pure parts (slugify, leadFolderPath, retry pacing) with Octokit mocked. Smoke verification: small script writes a test file to `leads-data` branch, reads it back. Push-to-remote of `leads-data` may happen as part of this atom's smoke test (writeLeadFile call requires the remote branch to exist) — Jake aware, not a violation since it's a new branch never merging into main.

- **after [per-lead memory as files — Atom 0: layout lock + repo scaffold]:** Done locally. Orphan branch `leads-data` exists at root commit `2bc00dd` with `_leads/README.md` (~140 lines documenting layout, source-of-truth table, slug rules, sweeper contract, parallel-history rule), `_leads/active/.gitkeep`, `_leads/archive/.gitkeep` — three files, no shared history with `main`. On `main`: new `.vercelignore` (`_leads/`) + `.gitignore` adds `_leads/` so accidental sweep writes during `main` checkout don't land in code history. Both `main` changes are LOCAL-ONLY per the "don't push main without ask" rule. The `leads-data` branch is also LOCAL-ONLY pending Jake's call on whether to push it now (it's the deliverable Andre would need to verify the layout in browser, but Atom 0 is complete either way). Workflow-polish work-in-progress (12 modified + 2 untracked files) preserved via stash → orphan → stash pop dance. Next: Atom 1 — `src/lib/lead-folder.ts` pure helpers (slugify, paths, Octokit read/write with SHA retry, archiveLead). Will tap before Atom 1.

- **before/after [proposals workbench — scale down so 100% looks like 75%]:** Jake — workbench overlay was sized for ~85vh + 1560px max width with 26-42px serif headline, 18px overview body, 96px panel-tab min-height + 14px tab strong / 12px small. Felt right only at 75% browser zoom; at 100% it ate the viewport. Scaled the whole widget down ~25% in one CSS pass: `proposal-workbench` width 1560 → 1180px, height 850 → 680px; overlay top padding 86 → 64px; head padding 24/28/21 → 16/22/14; head h2 clamp(26,2.7vw,42) → clamp(20,1.9vw,28); head p 14 → 12px; nav padding 22/20/24/24 → 16/14/18/18 + gap 11 → 8; panel-tab min-height 96 → 70px + padding 14/16/14/18 → 10/12/10/14 + border-left 5 → 4px + radius 8 → 7px + gap 6 → 4; panel-tab strong 14 → 12px, small 12 → 10.5px; right-pane padding 22/24/24/20 → 16/18/18/14 + gap 14 → 10 + grid-template min 260/250 → 200/190 + overflow hidden → auto (so right pane scrolls when overflowing); active/ai panel padding 18 → 13/14; active gap 14 → 10; ai gap 12 → 9; panel-heading gap/padding 12/12 → 10/9; panel-heading small 12 → 10.5px; overview-copy gap 12 → 9 + p 18 → 14px line-height 1.36 → 1.4; overview-copy blockquote padding 12/14 → 9/12 border-left 4 → 3px + font-size 14 → 12px; ai-form textarea min-height 120 → 90px + 12px font. tsc clean. Operator can use the workbench at 100% zoom now.

- **before [plan-tab day cards open the proposals workbench, with intake assets pluggable]:** Jake reviewed the Plan sub-tab on `/lead/[id]` and the proposals workbench overlay side-by-side. Proposals workbench (left-side day nav + right-side overview/touch-edit/add-touch/regenerate panels + AI change request textarea) is the good widget. Plan tab's day cards expose the same actions but as separate per-card modals — inconsistent. He wants: clicking any Day box on Plan tab opens the SAME workbench. Plus: intake artifacts (PDFs, images, HTML uploaded to `/lead/[id]/intake`) should be pluggable inside the widget so operator can drop them into AI change requests / touch bodies.
- **after [plan-tab day cards open the proposals workbench, with intake assets pluggable]:** Done. **`src/app/proposals/ProposalReviewBoard.tsx`**: `ProposalWorkbench` exported (was local); new optional `assetsSlot?: React.ReactNode` prop renders inside the right pane below the AI change request. Proposals page passes nothing → behavior unchanged. **`src/app/lead/[id]/PlanDaysWorkbench.tsx`** (new client component): converts `SevenDayPlan + leadName` → `ProposalPlanItem` via `planToProposalPlanItem` (maps each `SevenDayPlanDay.required_actions` → `ProposalDayItem.touches`, derives `summary_status` and `status_counts` the same way the proposals board does). Renders an auto-fill grid of `DayTile` cards (Day N kicker / status pill / serif objective clamped to 3 lines / touch count) using the existing 7-color pastel cycle (`lavender / sky / sage / lemon / peach / rose / blue`). Click → opens `ProposalWorkbench` overlay with the converted plan; `assetsSlot` carries an `AssetsPanel` that lists the lead's intake artifacts as a copy-on-click grid. Each artifact shows a kind glyph (🖼 image / 📄 PDF / ⟨/⟩ html / ▦ csv / {} json / ▶ video / ♪ audio / ◆ unknown), filename, byte size + extracted-char count, and a "Copy ref" affordance. Click → copies an asset reference to clipboard: for images `<img src="{ASSET:<id>}" alt="<filename>" />` (placeholder URL the agent will resolve at send time), for everything else `[asset: <filename> · <N> chars extracted]`. Brief "Copied ✓" confirmation. **`src/app/lead/[id]/PlanSection.tsx`**: replaced the `plan.days.map(<PlanDayCard>)` loop with `<PlanDaysWorkbench>`; old `DAY_TONES` const inlined into the new component. Stale-plan warning now renders as a small footnote under the workbench grid instead of inside each day card. New props `leadName: string` + `intakeArtifacts: IntakeArtifactRow[]` plumbed through. **`src/app/lead/[id]/page.tsx`**: extracts `leadName = box?.lead?.display_name` + `intakeArtifacts` from `loadLeadBoxPageData` and passes both to PlanSection. **CSS** (~150 lines appended): `.cmk-pdw-grid` (auto-fill grid), `.cmk-pdw-tile` + 7 pastel-tinted variants matching DAY_TONES, `.cmk-pdw-assets` panel + `.cmk-pdw-asset` clickable grid item with grid-row layout (glyph spans 2 rows). tsc clean.

- **before [per-lead memory as files — Atom 0: layout lock + repo scaffold]:** Pivoting from `lead_facts` Supabase table to per-lead Markdown folders mirroring Jake's old `Eliana Lopes/` shape (`00_meta.json`, `01_comms_digest.md`, `01b_comms_verbatim.md`, `04_profile.md`, `06_discovery.md`, `client_ledger.md`, `comms/{kind}_{date}_{shortid}.json`). Storage = subdir of THIS repo on a separate `leads-data` branch (sweeper commits there; Vercel only watches `main` so no rebuild thrash). Reads via existing Octokit client (`mirrorToGitHub` pattern in `auxiliaries-runtime.ts:203` is proven). Sweeper hydrates via direct Close REST (not MCP — MCP returned 404 / ignored `lead_id` filter on Sakamoto practice lead). Cron every 2h, manual sweep button on `/test`. Scope = Andre-owned, status not in (Won/Lost/Not-Interested). On Won/Lost/Not-Interested transition: one final sweep then `mv` to `_leads/archive/`. Atom 0 = orphan `leads-data` branch + scaffold (`.gitkeep` + `_leads/README.md`) + `.vercelignore` entry on main (local-only, not pushed). 10-atom plan.

- **after [workflow chat polish — composer + slash + multi-choice]:** Done across 4 files. **`src/lib/workflow-author-tool.ts`**: new `ask_operator` tool with required `question` + `choices` (2-5 items) and optional `allow_freeform` (default true); dispatcher branches to handle `ask_operator` separately and returns a discriminated `{ ok, kind: "operator_question", question, choices, allow_freeform }` shape; the `propose_close_workflow` happy path now returns `{ ok, kind: "workflow_proposed", … }` for symmetry. Tool name union widened, `isWorkflowAuthorTool` accepts both. **`src/app/api/workflows/chat/route.ts`**: outer system prompt rewritten with a hard rule — clarifying questions ALWAYS go through `ask_operator`, never plain text; one question per call, 2-5 concrete chip choices, no "Other" (use `allow_freeform`); MUST NOT call `propose_close_workflow` on the same turn; includes good ask_operator examples (sections / tone) and bad ones (you-already-have-the-answer questions). Route handler captures the `operator_question` payload from the tool result and ships it back to the client as a top-level field on the JSON response. **`src/app/workflows/WorkflowAuthor.tsx`**: composer rewritten as a pill — `cmk-wfa-composer-pill` wraps a `+` button (cycle to slash mode), a transparent textarea, and an inline send-arrow icon. Slash palette pops up above the composer when input starts with `/` (matches cockpit `cmk-slash-palette` shape, ↑↓ navigate, Enter or Tab runs, Esc dismisses). 8 slash commands: `/branded`, `/timeline`, `/html`, `/sms`, `/wait`, `/task`, `/rewrite`, `/help` — each prepends operator-INTENT (not template text) into the composer so the LLM still authors. Question card renders inside an assistant message: serif question + vertical chip list + dashed-border "Type my own answer →" freeform fallback that focuses the textarea. Chips disable after answer to prevent double-submit. Click chip → text becomes user message, chat continues. **`src/app/globals.css`**: replaced the old `.cmk-wfa-composer/.cmk-wfa-input/.cmk-wfa-send-btn` block with the new pill chrome (`cmk-wfa-composer-pill` / `-add` / `-input` / `-send`), slash palette tokens (`cmk-wfa-slash-palette/-row/-key/-hint/-foot`), and question card tokens (`cmk-wfa-q/-question/-choices/-chip/-chip-freeform`). All built from existing pastel + lane tokens, no new colors. tsc clean. Pending end-to-end fire from Jake.

- **before [workflow chat polish — composer + slash + multi-choice]:** Jake reviewed the workflow chat post-aesthetic-pass and flagged three real gaps: (1) the composer is janky — Send button sits as a separate dark block to the right of the textarea instead of being inline. The cockpit (`/chat`) wraps everything in one pill: + button left → transparent textarea center → small send-arrow icon right. (2) When the agent asks clarifying questions, they're plain text — should be multiple-choice chips with a "fill in your own" option so operator answers in 1 click. (3) Slash commands missing — `/html`, `/png`, `/branded`, `/sms`, `/wait` should give the operator structured intent shortcuts, plus the "+" should suggest file attach affordance. Files to touch: `src/app/workflows/WorkflowAuthor.tsx` (composer redesign + slash palette + question-card render), `src/lib/workflow-author-tool.ts` (new `ask_operator` tool with question/choices/freeform), `src/app/api/workflows/chat/route.ts` (route the new tool + tighten prompt to use it for clarifying), `globals.css` (composer pill + slash palette + question-card styles). File upload defers to a follow-up move (needs Supabase Storage public bucket).

- **before [workflows — AI authors rich XHTML emails + asks clarifying questions]:** Reframe from Jake — building a parallel WYSIWYG editor would be a Close-clone, not a product. The real move: teach the sub-LLM behind `propose_close_workflow` to (a) generate Close-compatible XHTML bodies (`<body>…</body>` wrapper + supported tag subset: h1-h6, p, ul/ol/li, strong/em/b/i/u/s, code, a, img, hr, span style="color:..."), (b) ask 2-3 clarifying questions before authoring rich content when intent is ambiguous (sections? tone? assets?), (c) keep generating plain bodies when the operator asks for something quick. Adapter (`manifest-to-close.ts`) is one line away from supporting this since `closeCreateEmailTemplate` already accepts `body_html`. Files to touch: `src/lib/workflow-author-tool.ts` (sub-LLM schema + system prompt), `src/lib/manifest-to-close.ts` (read body_html from node config). Image attach in chat is a separate later atom (needs Supabase Storage public bucket).
- **after [workflows — AI authors rich XHTML emails + asks clarifying questions]:** Done. **Sub-LLM** ([`src/lib/workflow-author-tool.ts`](src/lib/workflow-author-tool.ts)): `EmailStep` config type now includes `body_html?: string`; validator reads `body_html` from sub-LLM output, trims, and only adds it to the step config if non-empty (so plain-text-only steps stay clean). System prompt rewritten to teach Close's XHTML subset exactly (`<body>` wrapper, allowed tags + restrictions, self-closing `<img />` and `<hr />`, no scripts/styles/tables/inline-style-other-than-color), the dual-body pattern (always emit `body_text`, optionally `body_html` when content is structured/branded), and includes a real Rhonna-style 45-day-pre-event email as a reference example showing branded structure + NEPQ voice coexisting. NEPQ voice rules called out as applying to both `body_text` and the visible text inside `body_html`. **Outer agent** ([`src/app/api/workflows/chat/route.ts`](src/app/api/workflows/chat/route.ts) system prompt): new "Ask before authoring rich/branded content" section. When operator asks for something with real shape ("Rhonna's 45-day pre-event email", "the inbound 5-email drip with branded blocks") the agent must ask 2-3 targeted clarifying questions BEFORE calling the tool — sections, embeds, tone, audience. Includes good-vs-bad question examples (good: concrete and small; bad: open-ended and lazy that disrespect what the operator already said). Short conversational touches still go straight to the tool with no questions. New "Rich HTML email bodies" section explains the dual-body shape so the agent knows the tool produces both formats automatically. **Adapter** ([`src/lib/manifest-to-close.ts`](src/lib/manifest-to-close.ts) `emailStep`): reads `cfg.body_html` from the node config, trims, and passes it to `closeCreateEmailTemplate` only when non-empty (the helper already accepted `body_html`). Plain-text steps continue to land with just `body_text`. tsc clean. Pending end-to-end: Jake asks the agent to build "Rhonna's 45-day pre-event email" → agent asks 2-3 clarifying questions → operator answers → agent generates both `body_text` and `body_html` → publish → branded HTML lands in Close's email template.

- **future direction — Perplexity (or equivalent) MCP for real enrichment (deferred, captured 2026-05-05):** Jake will pitch this to Rodrigo today. Reframe of the prior enrichment thinking: the enrichment we were doing in `box.ts`/discovery was over-engineered AND the default fallback is unsatisfactory — neither is what Andre actually needs. Real fix is a web-grounded MCP server (Perplexity is the obvious candidate; could also be Tavily, Exa, or hosted Brave Search) so the agent can pull live web context on a lead's company / venue / event before drafting touches. Not "scrape the lead's homepage once at intake" — actual on-demand web search the agent reaches for when reasoning about a specific lead ("what does this venue's site say about catering preferences?", "what's this couple's wedding hashtag turning up?"). MCP fits because OpenAI Responses API already accepts remote MCP `headers` config (we already wired Close's MCP with the same shape, so the auth path is known). Open questions: (a) **which provider** — Perplexity has the strongest "answer with citations" shape for sales context; Tavily is cheaper per-query; Exa is the rawest. (b) **rate-limit + cost discipline** — every chat turn could hit web search if the agent over-reaches; per-tool-call cap or a settings toggle. (c) **citation surfacing in chat** — when the agent quotes the web, the URL chips need to show up in the RichToolResult so the operator sees what was sourced. (d) **how it composes with existing discovery** — Discovery Map slots stay as ground-truth-from-Close; web context is ephemeral reasoning material, not a `lead_facts` write. **Status:** future move; capturing the intent here so when Jake comes back from Rodrigo's, scoping starts from this entry instead of zero.

- **before [workflows list — bulk delete + per-card delete]:** 22 junk drafts on `/workflows` (mostly "Untitled workflow" / 0 steps from May 5 testing). Currently the only way to delete a draft is to enter it and click the × in the header — tedious × 22. Adding: (a) per-card × delete on each draft card (with confirm); (b) a "Clear empty drafts" header button that sweeps all 0-step drafts in one server action. Both wire to existing `deleteWorkflowDraftAction` pattern.
- **after [workflows list — bulk delete + per-card delete]:** Done. New server action `deleteEmptyDraftsAction` in [`src/app/workflows/actions.ts`](src/app/workflows/actions.ts) selects all unpublished drafts, computes per-row non-wait step count, deletes via `.in("id", emptyIds)`, returns `{ deleted: N }`. `deleteWorkflowDraftAction` gained a `stay_on_list` form flag so per-card delete doesn't redirect (default behavior preserved for the authoring-page × button). New client component [`WorkflowDraftCard.tsx`](src/app/workflows/WorkflowDraftCard.tsx) wraps each draft card with an absolute-positioned × button (top-right, hover-to-show) that runs delete + `router.refresh()` and locally hides the card optimistically; `confirm()` guard before delete. New client component [`ClearEmptyDraftsButton.tsx`](src/app/workflows/ClearEmptyDraftsButton.tsx) renders inside the section header next to the count when `emptyDraftCount > 0`; click → confirm → server action → alert with deleted count → refresh. CSS in [`globals.css`](src/app/globals.css): `cmk-workflows-card` got `position: relative`; new `.cmk-workflows-card-x` (hover-revealed, rose tone on its own hover) and `.cmk-workflows-clear-btn` (small ghost button in the section head, rose tone on hover). tsc clean.

- **before [workflows author — match cockpit aesthetic]:** Jake compared the workflow authoring page chat side to the delegations cockpit chat. Cockpit uses flat white cards with a sage left-rule on agent messages, mono uppercase eyebrow meta, serif body, neutral card chrome instead of colored bubbles. Workflow author was using rounded 10px bubbles with lavender user-msg bg — reads disjoint. Fix: rewrite `.cmk-wfa-msg-*` block to mirror `chat-msg-*` (8px radius card, 0.5px border, 2px sage left-rule on agent, mono 9.5px eyebrow), drop the lavender user bg, add an eyebrow header to the chat rail (`WORKFLOW · DRAFT  ·  N STEPS`), restyle starter-prompt chips as vertical card list, swap Send button to match cockpit ink-on-paper chrome.
- **after [workflows author — match cockpit aesthetic]:** Done. CSS rewrite in [`src/app/globals.css`](src/app/globals.css): `.cmk-wfa-msg` now matches the cockpit `chat-msg-user` / `chat-msg-card` shape — 8px radius, 0.5px rgba(0,0,0,0.10) border, white bg, 11.5px line-height 1.6 body. Agent messages get a 2px sage `#6B8E5A` left-rule + 0 1px 2px rgba shadow (mirrors `.chat-msg-card` exactly). User messages right-align (`align-self: flex-end`) at 78% max-width. System (error) messages use the `.chat-msg-call` pattern with rose left-rule. Meta eyebrow tightened to 9.5px / 0.14em / uppercase mono. Empty state restyled: starter chips became a vertical 6px-gap stack at 260px max-width with left-aligned 11.5px body (reads as "options" not "tags"). New `.cmk-wfa-chat-head` slim eyebrow strip at the top of the rail showing `WORKFLOW · DRAFT` (or PUBLISHED) on the left + `N STEPS` on the right — mirrors the cockpit's `cmk-eyebrow` chrome. Composer flattened: 12px font, 6px radius input, paper-2 bg consistent with `chat-input-bar`. New `.cmk-wfa-send-btn` ink-on-paper button replaces the `plan-btn-primary` to match the cockpit chat composer language. JSX changes in [`src/app/workflows/WorkflowAuthor.tsx`](src/app/workflows/WorkflowAuthor.tsx): added `<div className="cmk-wfa-chat-head">` above the chat scroll with the two-eyebrow strip; swapped Send button class. Page now reads as the same product as `/chat`. tsc clean.

- **before [workflows publish fix — templates + delay]:** First end-to-end publish revealed Close `/sequence/` API rejects our payload with 3 specific errors: (a) `email_template_id` is required — Close stores email bodies as named templates, not inline subject/body on the step; (b) `sms_template_id` is required — same shape; (c) `delay` field can't be a string ("Could not resolve timedelta from type '<class 'str'>'"). Fix: add `closeCreateEmailTemplate` + `closeCreateSmsTemplate` helpers in close.ts; rework `workflowToCloseSequence` (or wrap in a publisher fn) to be async and create templates first, then build steps that reference template ids; drop the string `delay` field, keep only `delay_in_seconds`.
- **after [workflows publish fix — templates + delay]:** Done. Two new helpers in [`src/lib/close.ts`](src/lib/close.ts): `closeCreateEmailTemplate({ name, subject, body_text, body_html?, is_shared? })` POSTs `/email_template/` and returns the new template's id; `closeCreateSmsTemplate({ name, body, is_shared? })` POSTs `/sms_template/`. Both default `is_shared: true` so Andre can see them in his org template list. [`src/lib/manifest-to-close.ts`](src/lib/manifest-to-close.ts) rewritten: `workflowToCloseSequence` is now `async`. Email steps create an email template via `closeCreateEmailTemplate` (name = `"{draft.name} · email N · {6-char draft slug}-{step}"`, e.g. `"Untitled workflow · email 1 · a3f9b2-1"`) and emit `{ step_type: "email", delay_in_seconds, email_template_id }`. SMS steps mirror the same shape with `closeCreateSmsTemplate`. Call/task steps unchanged (Close accepts inline `text`). The string `delay: "P0D"` field is gone everywhere — only `delay_in_seconds` (an integer) reaches Close. Helper `templateSuffix(draftId, stepIndex)` strips dashes off the uuid and takes 6 chars + 1-indexed step number → unique-per-publish template name so re-publishes don't collide. [`src/app/workflows/actions.ts`](src/app/workflows/actions.ts) `publishWorkflowDraftAction` now `await`s the adapter. tsc clean. End-to-end fire pending Jake re-clicking Publish on the test workflow.

- **before [workflows — implicit draft_id (fix LLM confusion)]:** Jake reported the agent is asking the operator for a `draft_id` and choking when it gets a Close `seq_…` id pasted in (Postgres uuid type rejects). Root cause: the `propose_close_workflow` tool exposed `draft_id` as a required LLM parameter, so the model thinks it has to find or solicit it. The page+API already know the draft_id (route param + body field). Fix: remove `draft_id` from the tool schema entirely, inject it server-side via the dispatcher's `opts` argument from the chat route. Same pattern other surfaces use to keep operator-context out of LLM-visible scope.
- **after [workflows — implicit draft_id (fix LLM confusion)]:** Fixed. `propose_close_workflow` tool schema now only accepts `operator_instruction` — `draft_id` removed from LLM-visible parameters. `dispatchWorkflowAuthorTool` signature changed to `opts: { draftId: string; traceId? }` (required). `/api/workflows/chat` route passes the bound `draftId` from request body into `opts`. System prompt updated: "the draft they're authoring is already bound to this chat session server-side — you don't need to know an id, find one, or ask." The "Postgres invalid input syntax for uuid" failure path is gone — the LLM can no longer pass arbitrary strings as ids. Tool description also rewritten to make the implicit binding obvious. tsc clean. Landed across 10 atoms. **Pure adapter** [`src/lib/manifest-to-close.ts`](src/lib/manifest-to-close.ts) compiles a `Workflow` draft into Close's sequence body — walks nodes in topological order from incoming-edge-free roots, maps `email_send`/`sms_send`/`task_create` to Close `step_type` values (`email`/`sms`/`call`), folds `wait` nodes into the next step's cumulative `delay` (ISO-8601 days) + `delay_in_seconds`. Throws clean error if zero compilable steps. **Chat tool** [`src/lib/workflow-author-tool.ts`](src/lib/workflow-author-tool.ts) registers `propose_close_workflow` (OpenAI Responses tool format). Dispatcher loads draft → calls sub-LLM with structured-output schema (locks vocabulary to email_send / sms_send / task_create / wait) → validates returned shape → runs NEPQ voice gate (single rewrite pass on email/SMS bodies that fail `hasBlockingViolation`) → lays out steps as nodes with auto-positions (linear x+200) → writes back via `updateAutomationDraft` → returns step summary. **Workflow chat endpoint** [`src/app/api/workflows/chat/route.ts`](src/app/api/workflows/chat/route.ts) mirrors the chat route loop with: workflow-focused system prompt, restricted tool set (only `propose_close_workflow`), in-memory chat history (sent up by client, not persisted in `threads`), 4 max tool rounds. Revalidates `/workflows/[id]` and `/workflows` on completion. **Server actions** [`src/app/workflows/actions.ts`](src/app/workflows/actions.ts): `createWorkflowDraftAction` → `createAutomationDraft` + redirect; `renameWorkflowDraftAction`; `deleteWorkflowDraftAction`; `publishWorkflowDraftAction` → `workflowToCloseSequence` → `closeCreateSequence` → stamps `close_sequence_id` + `close_steps_json` back, sets status to `active`, logs `publish_automation_draft`. **Authoring surface** [`src/app/workflows/WorkflowAuthor.tsx`](src/app/workflows/WorkflowAuthor.tsx): split-pane (chat 40% / canvas 60%, mobile stacks), in-memory chat with starter-prompt chips on empty state, ENTER-to-send composer, header with editable name (blur-to-rename) + status pill + published pill (when `close_sequence_id` set) + Publish button (disabled when empty) + delete X. Calls `/api/workflows/chat`, then `router.refresh()` so the canvas re-renders from the updated draft. Publish button fires the server action and opens the Close `html_url` in a new tab on success. **Server shells** [`src/app/workflows/[id]/page.tsx`](src/app/workflows/[id]/page.tsx) (loads draft via `getAutomationDraft`, mounts `WorkflowAuthor`) and [`src/app/workflows/new/page.tsx`](src/app/workflows/new/page.tsx) (calls `createWorkflowDraftAction` and redirects). **List page** [`src/app/workflows/page.tsx`](src/app/workflows/page.tsx): hero + two sections (Drafts / Published in Close), auto-fill card grid (320px+), drafts that have already been published are filtered out of Drafts (the "Published in Close" mirror takes their place). **TabNav** gets `workflows` between `heartbeat` and `delegations`, with `icons.automation` glyph. **Middleware** matcher gets `/workflows`, `/workflows/:path*`, `/api/workflows/:path*`. **CSS** ~370 lines at end of `globals.css`: list-page hero + cards + status pills, authoring-page split-pane + chat bubble styles + composer + graph empty state + 900px mobile breakpoint stacks vertically. **Verification:** tsc clean, build clean (route table shows `/workflows` 203 B, `/workflows/[id]` 5.38 kB, `/workflows/new` 138 B, `/api/workflows/chat` route). End-to-end pending: needs a manual run with the dev server hitting Close's API to confirm step shape (the v1 step_type values email/sms/call are educated guesses; if Close 4xx's the publish, we tune in `manifest-to-close.ts`).

- **before [AI-built Close workflows — `/workflows`, chat-driven authoring]:** Close has a manual workflow builder UI that Andre would never use. Their Sequence API (which is the same object — UI calls them Workflows, API calls them Sequences) supports full CRUD, and we already call `closeCreateSequence`/`closeUpdateSequence`/`closeEnrollInWorkflow` in the codebase. The customer-facing pitch: Andre tells the AI in plain English what he wants, sees the graph build live, tweaks via chat, clicks publish, lands in Close. 15-minute Close UI exercise → 90 seconds. Most of the build already exists (closeCreateSequence, automation_drafts table + lib, AutomationCanvas, chat tool dispatch pattern, WorkflowManifest). New pieces: a workflow-scoped `/workflows` route + chat-driven authoring page, a `propose_close_workflow` chat tool that takes English → workflow_json, a `workflowToCloseSequence` adapter, and an explicit publish action. v1 cut: manual-trigger sequences, steps = send-email/send-SMS/create-task/wait, linear flow only. Auto-triggers (webhook-driven), branches, and complex filters defer to Phase 2. Plan: `~/.claude/plans/perfect-i-m-going-to-eager-twilight.md`. 10 atoms.

- **future direction — Colorful Collaborator (deferred, design captured):** Jake floated an optional AI sidekick built on the existing 4-aux-slot infrastructure. Not always-on. Not in delegations chat. Two cadences from one character: an Observer (real-time, low-cost riffs) and a Reflector (post-event longer view) — same personality, different time scales. Cheap to run (5.x nano), so cost ceiling is not the design constraint; the constraint is whether the character is good company.

  **Reframe.** The product value isn't accurate observations; it's *anxiety relief through a strong personality.* Catering operators are anxious-by-nature about every deal (each event is a personal performance). A character whose default move is "cute, on to the next" psychologically offsets that. Entertainment is the surface; "stop catastrophizing and ship the email" is the function underneath.

  **Architecture sketch.**
  1. **Settings-driven character bible.** Per-operator. Ship 3-5 presets (Jaded Sales Genius is the demo); Andre picks, tweaks, or invents from scratch.
  2. **Settings fields per character (~6-7):** name; one-line stance; voice tics; themes they care about; things they refuse to do (cheerlead, fake warmth); a few seed quotes ("here's how they'd phrase a typical observation"); optional TTS voice profile.
  3. **Prompt library = ~200-300 angles, but variations on 8-12 themes the character cares about.** Random cycling, but within a sensibility — otherwise it reads as a slot machine instead of a single mind. Themes connect to MCP and intake/file-tree so riffs are grounded in real data ("you uploaded that proposal Tuesday and haven't opened it") instead of generic horoscope.
  4. **6 modes inside the character to avoid one-note fatigue:** the shrug · the dry compliment · the pointed obvious-thing · the mock concern · the rare serious moment · the war story. 6 modes × 8-12 themes × randomness ≈ doesn't repeat for weeks.
  5. **Surface — TBD, several shapes possible:** scrolling feed in a corner (peanut-gallery feel), summonable chat (coworker you check in with), TTS interjection (presence in the room). Each shapes the relationship differently. Pick deliberately. NOT inside the delegations chat.
  6. **Memory — open question.** Stateless feels cheap; rolling 50-event window or vector store gives continuity ("real colleague" depends on remembering things). Lean toward rolling window for v1.

  **Demo character — Jaded Sales Genius (Jake's pick for his own version):** has seen 10,000 deals, makes everything feel trivial and easy, expects deals to die, doesn't get worked up. Short sentences, no exclamation marks, vocabulary like "cute / fine / whatever / obvious / boring." Doesn't oversell or cheerlead. Notices missed timing, money left on the floor, deals dying from overthinking — surfaces those without theatrics. Reference points: old-school sales war stories ("had a guy in Phoenix in '98…"). Sample voice: "Friedman's been quiet 4 days. Cute. They'll move or they won't."

  **Open forks before any plan:**
  - Listening scope: chat turns only, or full event stream (heartbeat fires, plan changes, intake uploads, status moves)? Lean: full stream — chat-only is too narrow for a colleague feel.
  - Push vs pull: queues thoughts to a feed, or pings the operator? If pings, what rate-limit?
  - Memory shape (per above).
  - Surface choice (per above).
  - Whether the conversation surface gets read-only Close/intake tool access for grounded reflections, or stays pure chat.

  **Status.** Captured for later; Jake has a more pressing build move he wants to tackle first. When we come back to this, start with: pick the surface + commit to one preset character to ship as the demo, then build the settings page + character runtime.

- **before [proposals — tabbed view: Needs review / Approved / Sent / Cancelled]:** Jake noticed the column board only shows `needs_review` + `approved` because the other buckets are empty and the previous code hid empty columns. He's fine with not having the empty ones, but if there are only two, they should fill the page width — and ideally the whole page would be tabbed: 4 sub-pages (Needs review / Approved / Sent / Cancelled), active tab shows a full-width plate grid. Cleaner mental model + always uses full width regardless of which bucket is empty. Plan: replace the 5-column layout with a tab bar (counts inline), and a multi-column auto-fill grid for the active tab. Map `skipped` → "Cancelled" tab. Roll `not_ready` plans into "Needs review" so they don't get orphaned.
- **after [proposals — tabbed view: Needs review / Approved / Sent / Cancelled]:** Done. `ProposalReviewBoard.tsx` `STATUS_ORDER` columns gone — replaced with `TABS` array of 4 entries (`needs_review` / `approved` / `sent` / `cancelled`) where each tab maps to one or more `ApprovalStatus` values: `cancelled` matches `skipped`, `needs_review` matches both `needs_review` AND `not_ready` (so fresh-draft plans surface in the right place). Active tab state held in `useState<TabKey>("needs_review")`, persisted to `localStorage` under `cmk:proposals:tab` so the operator's last view sticks. Tab bar reads `Needs review (35) · Approved (11) · Sent (0) · Cancelled (0)` with counts updating live as plans move between buckets. Active tab renders a `proposal-grid` (CSS `grid-template-columns: repeat(auto-fill, minmax(320px, 1fr))`) so plates flow edge-to-edge across the page. Empty-tab state is a centered card with copy ("No plans here yet — try Needs review."). KPI strip at top-right kept intact. ~80 lines of new CSS for `proposal-tabs` / `proposal-tab` / `proposal-tab-active` / `proposal-tab-count` / `proposal-grid` / `proposal-tab-empty`. tsc clean.

- **before [proposals — tile per job, not per action]:** Jake reported `/proposals` is fanning a tile per plan-day, so a single 7-day plan with 4 needs-review days becomes 4 tiles in the NEEDS REVIEW column. Workbench popup is great; tile granularity is the bug. Fix: aggregate at the plan level. Each tile = one plan/lead. Bucket by highest-priority day status. Workbench gains a left nav listing all days; click a day → its panels expand inline.
- **after [proposals — tile per job, not per action]:** Done. `ProposalReviewBoard.tsx` rewritten at the data layer: new `ProposalDayItem` (per-day inner) + `ProposalPlanItem` (top-level with `days[]` + `summary_status` + `status_counts` + `total_touches`). `ProposalPlate` takes a plan: kicker reads "{N}-day plan · {generated date}", lead-name headline, plan goal_summary as body, footer combines a status-counts chip rail (sage/peach/sky tones per status), total touches, and union of channel glyphs. `ProposalWorkbench` takes a plan: left nav is now a vertical day list (each row: Day N · status pill · touch count); selecting a day expands the panel set (overview / per-touch / add / rewrite) in an indented inset. Right pane unchanged — adapter `buildItem(plan, day)` synthesizes the legacy per-day item shape so `WorkbenchPanel` / `AiChangePanel` / `TouchEditForm` / `AddTouchPanel` / `RefinePanel` keep their existing signatures and need zero rewrites. `page.tsx` swapped from per-day flatMap to per-plan map: builds plans with `summary_status` derived by walking priority order (`needs_review > approved > not_ready > sent > skipped`) until first non-zero count. Counts strip now reads "{N} needs review · {M} approved · {T} touches" where N/M are plan tile counts (matching column tallies) and T is total touches across all plans. ~110 lines of new CSS at end of `globals.css` for `proposal-plate-counts` chip rail (per-status tones), `proposal-plate-meta`, and `proposal-day-section` / `proposal-day-row` / `proposal-day-num` / `proposal-day-touches` / `proposal-day-panels` (indented panel-list with left vertical rule). tsc clean.

- **before [discovery map v1 — NEPQ sales-RPG layer]:** Jake wants a sales-game layer that rewards journey progression instead of activity volume — Comeketo is solo-operator (Andre vs the pipeline, no rep leaderboards). The unusually strong starting position: skip codes (`OWNERSHIP / STOP_SIGNAL / REPLY_GATE / SEND_WINDOW / FREQUENCY_CAP* / STALE_BOX / VOICE_FAIL / …` in [`close.ts:741–766`](src/lib/close.ts)) already record every guardrail decision, `nepq.ts` lints every drafted touch, and `heartbeat_runs.report.days[].actions[].verdict` semantically captures fire/skip per action. The honest gap is canonical *discovery facts* — Box dumps `custom.*` from Close with no slot shape. Jake confirmed Andre already has canonical custom fields for catering discovery, so atom 0 reads the live Close custom-field schema and locks slot ids to those exact field names. Full v1 tonight: 8 atoms + atom 0 schema-read. New libs `discovery-map.ts` (slot defs + resolver), `lead-facts.ts` (Supabase CRUD), `quest.ts` (rule-based current-quest/bonus/risk/move synth), `journey-score.ts` (clarity / readiness / restraint / discovery_xp, all read-derived from existing tables — no new score-events table). New `lead_facts` table (`lead_id, slot_id, value, source: close_custom|llm_extraction|operator, evidence, extracted_at`). New chat tools `extract_discovery_facts` + `lead_journey_score` via `composite-tools.ts`. New surface: `/lead/[id]/discovery` Server Component (sub-nav between Box and Heartbeat) — Discovery Map (3×3 slot grid), Current Quest panel (`cmk-stack-panel--lemon`), Restraint panel (last 7d skip-breakdown chip rail). Cockpit `PlanDayCard` gets a `Discovery 6/9` chip in its header. RichToolResult `cmk-rich-journey` branch in chat. CSS additions reuse existing tokens (sage/peach/lavender/lemon panel variants + cmk-rich-batch-head/totals idiom). Production-quality per the no-demo rule lock. Plan: [`/Users/jakeaaron/.claude/plans/hey-so-i-was-cheeky-squid.md`](file:///Users/jakeaaron/.claude/plans/hey-so-i-was-cheeky-squid.md).
- **after [discovery map v1 — NEPQ sales-RPG layer]:** Done. Atom 0 (schema read) located catering-discovery custom fields in Andre's Close org via `find_lead_custom_fields` — 5 canonical-Close slots locked (`event_date` = `Date of Event`, `venue` = `Venue Name`, `location` = `Address`+`City`+`Distance` composite, `client_type` = `Client Type` Consumer/Venue, `budget` = `Wedding Budget`) plus 4 LLM-extracted slots (`guest_count` / `service_style` / `decision_timeline` / `dietary_constraints`). Found Andre's existing `🟢 SCORE` multi-choice tag field — surfaced as `hot_tags` in journey score (we read; we don't overwrite). **Eight atoms shipped.** (1) [`src/lib/discovery-map.ts`](src/lib/discovery-map.ts) — `DISCOVERY_SLOTS` const (locked at 9-slot ceiling for 3×3 grid), `resolveSlot` resolver (close_custom → lead_facts → unknown), `buildDiscoveryMap` aggregator, plus the `PIPELINE_STAGES` / `resolveStage` track for the catering progression `lead → discovery_started → tasting_booked → tasting_done → beo_sent → agreement_signed → deposit_in → event_won` (with `lost` short-circuit). 17 unit tests. (2) [`supabase/migrations/20260505200000_lead_facts.sql`](supabase/migrations/20260505200000_lead_facts.sql) `lead_facts` table + RLS policy + [`lead-facts.ts`](src/lib/lead-facts.ts) CRUD with operator-source override priority (operator beats llm_extraction; llm_extraction won't stomp operator on conflict). (3) [`journey-score.ts`](src/lib/journey-score.ts) — `RESTRAINT_GOOD_SKIPS` / `RESTRAINT_BAD_SKIPS` classification of all 25 SkipCodes (good = STOP_SIGNAL/REPLY_GATE/FREQUENCY_CAP/VOICE_FAIL/NEEDS_APPROVAL/SEND_WINDOW/STALE_BOX, bad = CLOSE_API_ERROR/HTML_FAIL/WORKFLOW_MISMATCH/NO_*; ownership/status filters neutral); pure scorers `computeDiscoveryXP` / `computeReadiness` / `computeRestraint`; orchestrator `journeyScoreForLead` reads `loadLeadBoxPageData` + `getLeadFacts` + last-10 `heartbeat_runs` to compose `JourneyScore` (clarity / readiness / restraint / discovery_xp / stage / hot_tags / restraint_breakdown). 14 tests. (4) [`quest.ts`](src/lib/quest.ts) — `synthesizeQuest(map, stage, plan)` returns `{current, bonus, risk, recommended_move}`. Rule-based: highest-weight unknown in priority category (quest > clarity > consequence) → current; next-priority → bonus; risk inferred from plan content × clarity × stage (premature-proposal, low-clarity, open-ended-tasting, plan-inactive, quoting-on-guess); recommended-move uses `plan.best_next_question` when present, otherwise NEPQ-templated per-slot questions. 8 tests. (5) Two new composite chat tools in [`composite-tools.ts`](src/lib/composite-tools.ts) — `extract_discovery_facts({lead_id})` runs Box content (8 inbound + 4 outbound activities, ~400 chars per body) through OpenAI with structured-output instructions targeting only the 4 LLM-only slots, persists `lead_facts` rows for grounded extractions (confidence ≥ 0.6), self-audits via `intake_extract` execution-log kind; `lead_journey_score({lead_id})` returns the compact JourneyScore + DiscoveryMap shape (~700 chars worst case, fits the 2000-char tool-summary cap). Both registered in `delegations-tool-audit.ts` self-audited set as appropriate. (6) [`/lead/[id]/discovery/page.tsx`](src/app/lead/[id]/discovery/page.tsx) Server Component with score header strip (4 metrics in mono-numeral cards), horizontal pipeline stage strip (filled dots for reached, glow on current, lost short-circuit), Discovery Map 3×3 grid (panel tone tracks completeness: sage ≥ 70% / peach 40–70% / rose < 40%), Current Quest panel (`cmk-stack-panel--lemon` with current/bonus/risk/move rows + `Run discovery scan` CTA), Restraint panel (`cmk-stack-panel--lavender` with skip-code chip rail). Plus client components [`DiscoverySlotEditor.tsx`](src/app/lead/[id]/discovery/DiscoverySlotEditor.tsx) (Modal-based editor, operator-source override, evidence excerpt visible) and [`RunScanButton.tsx`](src/app/lead/[id]/discovery/RunScanButton.tsx). [`LeadSubNav.tsx`](src/app/lead/[id]/LeadSubNav.tsx) gains `Discovery` tab between `Box` and `Intake`. Server actions in [`actions.ts`](src/app/lead/[id]/discovery/actions.ts) for `setSlotValueAction` + `runDiscoveryScanAction` with `revalidatePath`. (7) RichToolResult `cmk-rich-journey` branch in [`ChatPanel.tsx`](src/app/chat/ChatPanel.tsx) — header strip with stage + xp + hot-tag count, four-up totals (clarity sky / readiness sage / restraint lavender / xp peach using existing lane tokens), 3×3 slot grid with status-tinted cards, restraint footnote with fire/hold/fail counts. `TOOL_CATEGORY` + `toolHeadline` + `pinFromCall` cover both new tools. Cockpit gets a `discovery →` link in `PlanDayStrip` plan card alongside `edit plan →` / `full Box →`. (8) ~280 lines of new CSS in [`globals.css`](src/app/globals.css) — `cmk-stack-panel--lemon` + `cmk-stack-panel-head` / `-title` / `-meta` reusable header chrome, full Discovery surface styles (head / scores / pipeline strip with reached/current state styling / slot grid with known/unknown/stale tints / source pills / editor modal / quest panel rows / restraint chips), and the journey RichToolResult variants. All built from existing pastel + lane tokens — no new colors introduced. **Verification:** `npm run build` green; `/lead/[id]/discovery` ships at **3.2 kB / 99.2 kB First Load JS**; `npm run typecheck` clean; **39/39** unit tests pass (17 discovery-map + 14 journey-score + 8 quest). Boss demo: Andre opens any lead, sees what's known/unknown about the buyer, current quest + risk + recommended move, restraint over the last 10 heartbeats. Solo-operator framing — Andre vs the pipeline. **Migration not yet applied** — the `lead_facts` SQL file is checked in but Jake should apply it on the Neon dev branch first, then production.

- **before [personal scoreboard — global view at /personal]:** Jake pointed at the dead "personal" text node in `AppHeader.tsx:46` (between proposals and briefing) — wants `/personal` to be the global scoreboard, all the journey scores aggregated across Andre's whole book. NEPQ-RPG framing: `/lead/[id]/discovery` is the per-lead game board, `/personal` is the player profile. Building one page: Andre's portfolio totals (sum Discovery XP, avg Clarity / Readiness, aggregate Restraint over 30d), the slot-fill rates ("62% of your leads have known event_date, 41% have budget" — pipeline-wide blind spots), Pipeline funnel (count of leads at each stage), top-quest themes (e.g. "11 leads need budget", "8 leads need decision_timeline"), top-5 hottest leads with mini journey-score bars. Cost discipline: don't call `journeyScoreForLead` per lead (50× full Box loads). Lighter aggregator — `closeListLeadsByAssignee(andreId, 200)` already returns `custom.*` fields so `buildDiscoveryMap` runs on each lead without a Box fetch; `lead_facts` batched in one Supabase query; restraint pulled in one `select skip_breakdown, actions_fired from heartbeat_runs where ran_at > now() - 30d` query. New file `src/lib/personal-scoreboard.ts` (aggregator), new page `/personal/page.tsx` (Server Component), `AppHeader.tsx` swaps the dead text node for a `Link`. CSS additions reuse existing tokens.
- **after [personal scoreboard]:** Done. New aggregator [`src/lib/personal-scoreboard.ts`](src/lib/personal-scoreboard.ts) — `buildPersonalScoreboard()` does **one** Close call (`closeListLeadsByAssignee(andreId, 200)`) plus **one** Supabase batched `lead_facts` select plus **one** `heartbeat_runs` 30-day aggregate; no per-lead Box fetches. Filters out `lost`-status leads from the active pool. Per lead it runs `buildDiscoveryMap` + `resolveStage` to compute slot states + stage; then aggregates into `PersonalScoreboard` shape: `total_leads`, `total_xp`, `avg_clarity`, `restraint_30d`, `restraint_breakdown`, `hot_tag_counts` (counts of Andre's existing 🟢 SCORE multi-choice tags across pipeline), `slot_fill_rates` (per-slot known/total/pct — "62% have event_date"), `pipeline_funnel` (count per catering stage), `top_quests` (top-5 most-frequently-unknown slots — "11 leads need budget"), `top_leads` (top-8 ranked by hot-tag count → clarity → recency, each links to `/lead/[id]/discovery`). Lead-facts errors swallowed gracefully so the page renders pre-migration. New page [`src/app/personal/page.tsx`](src/app/personal/page.tsx) — Server Component with: header (player profile · Andre · pipeline scoreboard); 4-up KPI strip (Total Discovery XP peach / Avg Clarity sky / Restraint 30d lavender / Active leads sage); Pipeline funnel panel (`cmk-stack-panel--sage`, horizontal bar per stage normalized to max); Discovery coverage panel (`cmk-stack-panel--sky`, per-slot row with category pill + fill bar + N/total + %); 2-col grid for Top quests (`cmk-stack-panel--lemon`) and 🟢 SCORE tag distribution (`cmk-stack-panel--peach`); Top leads list (`cmk-stack-panel--lavender`, ranked rows with name + stage + clarity bar + relative time, each row clicks through to per-lead Discovery); Restraint 30d footer panel with skip-code chip rail. [`AppHeader.tsx`](src/components/AppHeader.tsx) "personal" text node swapped for a `Link href="/personal"`. ~210 lines new CSS in [`globals.css`](src/app/globals.css) — kpi cards in 4 lane-token color variants, funnel/coverage bar layouts, top-leads grid, mobile collapse rules. **Verification:** `npm run build` green; `/personal` ships at **200 B / 96.2 kB First Load**; `npm run typecheck` clean. Boss-demo path: click "personal" in the header → Andre's player dashboard with portfolio totals, where the pipeline-wide gaps are, which quests come up most, who his hottest leads are. Click any top-lead row → drops into per-lead Discovery game board. The two pages mirror — `/personal` is the player profile, `/lead/[id]/discovery` is the per-lead game board. **Migration still pending** — `lead_facts` select runs in a try/catch and treats absence as empty.

- **before [proposals plates + workbench overlay]:** Jake wants `/proposals` to become manageable: plate/list cards on the page, then a large standardized pop-up/workbench when a plate is clicked. The workbench pattern should eventually replace the inconsistent pop-ups on Proposals / Delegations / Graph: left side = clickable panels/sections, top-right = active selected panel editor, bottom-right = AI change/request context. First landing should make this real on Proposals using existing edit/add/status/refine actions, not fake chat.

- **before [briefing page]:** Build the top-right `briefing` surface as an at-will operational summary: new/recently updated leads, movement inside existing leads, waiting/approved proposal pressure, and recent heartbeat/execution/webhook signals if available. It should be readable in one pass and refreshable on demand. First pass should use current real data helpers, route the header `briefing` label, protect the route, and avoid fake insights.
- **after [briefing page]:** Landed `/briefing`. Header `briefing` now links to it; middleware protects it. Page composes real sources: recent Close leads (`new leads` and `movement`), `pipelineStateForOwner("andre")`, `aggregateOperatorTruth()`, `listRecentExecutionGlobal()`, and `listPlansForProposalReview()`. It renders KPI strip, new-leads panel, movement panel, waiting-on-you examples, recent system moves, and a Refresh link. `npm run typecheck` clean; `npm run build` clean (`/briefing` 196 B / 96.1 kB, server-rendered).

- **before [proposals review page]:** Build the top-right `proposals` surface as a real daily operator page: clear view of what is queued to be sent, what is approved, what needs review, and what has already been sent/skipped. It must support editing/regenerating/changing from the page, not just passive display. First pass should wire the existing plan/day server actions and real `lead_plans` data; no inert header link. Also make the top-right proposals label route to the new page.
- **after [proposals review page]:** Landed `/proposals` as a real daily review workbench. Header `proposals` now links to the route; middleware protects it. Page reads active plans via new `listPlansForProposalReview()`, resolves lead names, flattens plan days into status columns, and exposes edit draft, add touch, rewrite day, rewrite whole plan, approve, needs-review, mark-sent, skip, Graph, and Lead actions. Existing plan/touch actions now revalidate `/proposals` so the desk stays fresh after edits. `npm run typecheck` clean; `npm run build` clean (`/proposals` 4.24 kB / 100 kB).

- **before [graph full-canvas + popover correction]:** Jake's next correction: remove the permanent right editor entirely. Graph gets the full screen; clicking a node opens the editing surface as an overlay/popover/drawer only when needed. Also remove the redundant `scheduled_call cycle` title — every plan has that shape, so it adds noise. Keep goal summary/stats/simulate controls, but make the canvas the hero. Target: full-width compact topology map + contextual day editor overlay.
- **after [graph full-canvas + popover correction]:** Done. Graph now owns the full-width canvas; `DayEditor` is rendered only as a modal-style overlay after clicking a node, with backdrop click to close. Removed the redundant `{primary_goal} cycle` heading; the page now shows only the plan graph eyebrow, the useful goal summary, stats, and Simulate. Canvas height restored as the visual hero. `npm run typecheck` clean; `npm run build` clean (`/lead/[id]/graph` 8.53 kB / 104 kB).

- **before [graph map correction]:** Jake correctly flagged the first graph control-board pass as overcorrected: it made the canvas into redundant day-detail cards, which removes the reason to have a graph. Corrective target: graph should be a compact structural map visible at once, while the right drawer carries full editable detail. Nodes should show day/status/channel/audit/verdict density without repeating objectives/drafts. Preserve the new drawer/edit plumbing, but redesign canvas cards smaller and more topology-forward, with future room for Close workflows/automation nodes.
- **after [graph map correction]:** Corrected. Graph canvas now uses compact map nodes instead of full day-detail cards: day number orb, short objective label, channel icon row, touch/verdict/audit lane dots, status pill, and touch count. Right drawer remains the detail/edit surface. This restores the graph's purpose (whole-plan topology at a glance) while preserving the new no-giant-modal editing workflow. `npm run typecheck` clean; `npm run build` clean (`/lead/[id]/graph` 8.48 kB / 104 kB).

- **before [cockpit Delegations eyebrow becomes the mode toggle]:** Jake — even after removing the `cmk-mode-pill` and slimming "+ new", the cmk-mode-toggle segmented control (Andre | Lead · Friedman Anniversa) reads as a "fat box" eating vertical space. Cut: drop the segmented control entirely and turn the "Delegations" eyebrow itself into a clickable mode toggle. Eyebrow reads `DELEGATIONS · ANDRE` or `DELEGATIONS · LEAD`, click toggles. Existing IN SCOPE card / LeadPicker continue to handle lead-name display + picking, so no info is lost. Classier than the segmented control.
- **after [cockpit Delegations eyebrow becomes the mode toggle]:** Done. `cmk-mode-toggle` segmented `<div>` deleted from `ChatPanel.tsx` (~22 lines). Rail header eyebrow now renders as `<button className="cmk-eyebrow-btn">DELEGATIONS · {mode}</button>` — clicking toggles between Andre and Lead modes via the existing `setMode()`. Subtle inline `⇄` glyph after the mode word in `cmk-eyebrow-btn-glyph` (faint by default, ink-bright on hover) so togglability reads at a glance. New `.cmk-eyebrow-btn` CSS extends `cmk-eyebrow` chrome (no padding/box at rest — looks like the eyebrow it's replacing) but gains hover underline + ink-bright color so the affordance is discoverable. tsc clean. Vertical space reclaimed: ~36px of fat segmented-control gone, IN SCOPE card now sits directly under the header.

- **before [cockpit rail header tighten]:** Jake reported the `/chat` left rail wraps at narrower widths because the header packs Delegations eyebrow + redundant "normal/wide" pill + bulky "+ NEW" button + PaneControl arrows + ×, then the Andre/Lead mode toggle below. He correctly diagnosed the cut: the "normal"/"wide" pill is verbose chrome that the `‹›` / `›‹` icon button already conveys. Same situation on the right scope head. Plan: (a) delete both `<span className="cmk-mode-pill">` instances; (b) slim `+ new` to a single-glyph `+` button matching the PaneControl chrome (so the rail header is one tight icon row). Cockpit reads cleanly at narrow rail width AND wide.
- **after [cockpit rail header tighten]:** Done. Two `cmk-mode-pill` spans removed from `ChatPanel.tsx` (left rail head line 3047, right scope head line 3468). `+ new` button rewritten as an icon-only "+" with `aria-label="New thread"` and a tighter chrome (square, paper-2 bg, hover lifts to white). Same treatment for the rerun "↻" affordance — both buttons now sit in a `cmk-rail-icon-btn` shape that matches `cmk-pane-btn` size so the four header buttons (rerun, +, ‹›, ×) read as one consistent icon strip. `cmk-mode-pill` CSS left in place (single `.cmk-mode-pill` rule, ~6 lines) since deleting it has zero benefit and keeps the diff narrow if a future surface wants it. Andre/Lead mode toggle untouched — already flexes to rail width and isn't part of the header pack. tsc clean.

- **before [lead heartbeat tab — timeline + recent-actions visual pass]:** `BoxTimeline` and `RecentExecutionStrip` were authored with semantic class names (`lead-timeline`, `lead-timeline-item`, `lead-timeline-dot`, `lead-timeline-meta`, `lead-timeline-kind`, `lead-timeline-body`, `appr-{status}`, `lead-recent-audit`) but **none of those classes were defined in `globals.css`** — so the section rendered as unstyled `<ul>` bullets with raw fields squished together inline ("plan day 7approved2026-05-11"). Two-part fix: (a) add the missing CSS so the timeline rows match the card / dot / pill language used elsewhere; (b) format `it.at` and `r.at` as friendly human dates inside the components instead of raw ISO timestamps.
- **after [lead heartbeat tab — timeline + recent-actions visual pass]:** Landed. ~280 lines of CSS appended to `globals.css` covering `.lead-timeline` (vertical list, 6px gap), `.lead-timeline-item` (grid: dot + content, hover lift, soft border on hover), `.lead-timeline-dot[data-kind=*]` (call lavender / email-thread sky / sms peach / note neutral / task lemon / meeting sage / plan rose, with a 2px card-color halo so dots read on either bg), `.lead-timeline-meta` (mono, 10px, ink-soft, kind+direction+time inline-flex with time pushed right via `margin-left: auto`), `.lead-timeline-kind` (uppercased pill with paper-2 bg + thin rule), `.lead-timeline-dir` (italic ink-faint), `.lead-timeline-body` (Fraunces 13px, line-height 1.55), `.lead-timeline-appr` + per-status tones (`not_ready` faint, `needs_review` lemon, `approved` sage, `sent` lavender, `skipped` strikethrough), and a fully restyled `.lead-recent-audit*` (real card frame, mono tabular-num timestamp, `code`-styled action_kind chip, rose pill on non-ok results, peach skip-code chip, dashed-rule footer with code-styled lead-id). [`BoxTimeline.tsx`](src/app/lead/[id]/BoxTimeline.tsx) edited: new `fmtAt()` + `fmtDay()` helpers format times as "May 11 · 1:13 PM" and plan dates as "Mon, May 11"; `approval_status` underscores rendered as spaces; `direction` only emitted when truthy; new `.lead-timeline-empty` card-frame replaces the bare `.lead-empty` line; `RecentExecutionStrip` rewritten with semantic class names instead of inline styles. tsc clean. Heartbeat tab now reads as part of the same product surface as Plan / Graph / Box / Intake.

- **before [graph control-board implementation]:** Jake approved the graph redesign immediately instead of waiting for the other agent to push. Implement now, but with collision discipline: read current dirty tree first, avoid touching intake work except ledger taps, keep scope to graph/editor/CSS/server-action reuse, and preserve existing PlanDayCard behavior where other surfaces depend on it. Target: plan-native graph board with rich day/touch cards, right-side editor drawer at 100% zoom, simulation/verdict overlays, and immediate visual feedback after mutations.
- **after [graph control-board implementation]:** Landed the first serious graph pass. `PlanGraphView.tsx` no longer uses the generic `AutomationCanvas` for plans; it now renders a plan-native horizontal board with rich day cards, stacked touch strips, status/audit/verdict signals, simulation summary, and a sticky right editor drawer. Drawer supports status changes, add touch, edit touch, delete touch, and AI rewrite using existing server actions, with optimistic local updates plus route refresh for source-of-truth sync. `globals.css` adds the `plan-graph-*`, `graph-day-*`, `graph-touch-*`, and `graph-editor-*` visual system. `npm run typecheck` clean; `npm run build` clean (`/lead/[id]/graph` 8.39 kB / 104 kB).

- **after [graph + day-editor redesign direction]:** Direction locked: replace the current abstract `AutomationCanvas` usage on plan graph with a plan-native board. Build day columns/cards across the timeline, touch chips inside each day, verdict/audit overlays from simulation, and a compact reusable day editor that can render as a right drawer on graph and as a modal elsewhere. First implementation should avoid repo collision until the other agent lands; once clear, touch `PlanGraphView.tsx`, factor editor pieces out of `PlanDayCard.tsx`, add graph-specific CSS, then verify with desktop and narrow screenshots.

- **before [graph + day-editor redesign direction]:** Jake wants the lead Graph page to stop reading as tiny-symbol workflow wire art and become a serious operator surface. Design target: graph and reusable day editor work as one artifact; edits in the modal/panel update the graph immediately; no forced 50-60% browser zoom; reduce the current muddy brown field; replace tiny abstract node glyphs with informative, tactile day/touch cards that show channel, approval/fire state, audit state, and action density at a glance. No code edits until the other active agent lands their current repo changes.

- **after [intake page — drop zone + card grid pass]:** Landed. New client component [`LeadIntakeBoard.tsx`](src/app/lead/[id]/intake/LeadIntakeBoard.tsx) (~270 LOC) replaces the form+panel composition with: (a) a hero drop zone — dashed border, serif "Drop anything in here." headline, italic file-types blurb, click OR drag-and-drop, click-or-Enter/Space keyboard accessible, full-width and tactile with hover + drag-state styling; (b) per-file pending-row strip showing each upload ticking through `uploading… → extracting… → done` (rose tone on errors), auto-clears 2s after success; (c) sequential multi-file upload (`reduce(async)`) so the order of dropped files preserves in the grid; (d) auto-fill card grid (`repeat(auto-fill, minmax(320px, 1fr))`) with type-glyph chips (PDF rose / CSV sage / JSON lavender / MD sky / IMG peach / VID-WAV lemon / TXT neutral), serif filename, mono meta line (date · size · char count), 4-line clamp on the extracted snippet, expand-for-full-text `<details>` with rotating chevron, download link, × delete, and a muted "extraction deferred" badge for image/audio/video Phase-2 placeholders. New server action [`deleteIntakeArtifactAction`](src/app/lead/[id]/actions.ts) deletes the storage object via the Storage API (NOT a SQL DELETE — Supabase blocks that via `protect_delete()`) AND drops the DB row, with a lead-id mismatch guard against stale form posts. [`page.tsx`](src/app/lead/[id]/intake/page.tsx) now reads "intake · {lead name}" eyebrow and embeds the board. Old `LeadIntakeUploader.tsx` deleted (replaced). [`globals.css`](src/app/globals.css) gets ~370 lines of `cmk-intake-*` tokens — drop zone, pending rows, section header (Fraunces 26px), grid, cards, kind glyphs (six tones from the existing pastel palette), card body clamp + details `<summary>` chevron, footer, mobile breakpoint at 720px collapsing to single-column grid + tighter drop zone padding. tsc clean. Reads as Comeketo, not as a debug surface. Box tab's `IntakeArtifactsPanel` untouched — it keeps the compact list shape since Box has limited vertical space.

- **before [intake page — drop zone + card grid pass]:** Jake wants the intake sub-tab to feel like part of the product, not an outlier. Two visual goals: (1) hero drag-and-drop zone at the top — drag files in OR click to browse, multiple files at once; (2) card-grid display of artifacts with type icon + filename + meta + extracted-text snippet + per-file remove (×) and download. Not the full big-screen redesign yet (no embedded scoped Q&A — that's a separate sprint when he's ready) but the visual language so the page reads as Comeketo, not "weird outlier." New client component `LeadIntakeBoard.tsx` replaces the separate uploader+panel composition for this route only (Box tab keeps the existing `IntakeArtifactsPanel`). New `deleteIntakeArtifactAction` server action wires the × button. CSS additions in globals.css for `.cmk-intake-drop`, `.cmk-intake-grid`, `.cmk-intake-card`.
- **before [intake uploader — await extract before refresh]:** Jake reported the uploader feels like it's hanging. Logs show it's not — extract runs ~2s after upload returns and writes `extracted_len > 0`. The bug is UX: `LeadIntakeUploader.tsx` fires extraction with `fetch().catch(() => {})` (fire-and-forget) and immediately calls `router.refresh()`, so the refresh runs BEFORE extract resolves and the row appears with no extracted text. Fix: await the extract call inside the submit handler before refresh, with a 30s `AbortController` timeout so a stuck extract doesn't lock the form.
- **after [intake uploader — await extract before refresh]:** Fixed in [`LeadIntakeUploader.tsx`](src/app/lead/[id]/intake/LeadIntakeUploader.tsx). Submit handler now `await`s the extract `fetch` (with abort handling so the page still refreshes if extract throws or times out) and only then calls `router.refresh()`. Replaced the `busy` boolean with a 3-state `Stage = "idle" | "uploading" | "extracting"`; button copy ticks "Uploading…" → "Extracting…" → back to "Upload" so the operator sees the two-stage progress instead of staring at a dead "Uploading…". 30s `AbortController` timeout on the extract surfaces as "Extract timed out — refresh in a moment to see what landed." tsc clean. Extracted-text collapsible now renders on the next paint after submit instead of needing a manual reload.

- **before [Lead-scoped Intake — Phase 1, no Gemini yet]:** Move `/intake` from top-level tab to `/lead/[id]/intake` sub-tab — same architectural shape as the recent `/automation → /lead/[id]/graph` migration. Six atoms: (1) migration adding `extracted_text` column to `intake_artifacts` + `DELETE WHERE lead_id IS NULL` (Jake confirmed orphan drop is fine); (2) widen extractor to PDF (`pdf-parse`), images deferred to Phase 2 / Gemini; (3) new `/lead/[id]/intake` route mirroring `graph/page.tsx`, with collapsible per-file extracted-text preview; (4) delete `src/app/intake/`, move `redirectIntakeArtifactDownload` action into `src/app/lead/[id]/actions.ts`, drop intake from TabNav + middleware; (5) extend `buildLeadContextBlock` in `api/chat/route.ts` to thread per-lead intake materials into the system prompt with a 15k-char total cap; (6) add Intake link to `LeadSubNav` between Box and Heartbeat. Plan file: `~/.claude/plans/perfect-i-m-going-to-eager-twilight.md`. Phase 2 (Gemini for image/video/audio understanding) deferred to a separate plan — `GEMINI_API_KEY` already in `.env.local` but not yet wired into `env.ts`.

- **after [Lead-scoped Intake — Phase 1, no Gemini yet]:** Landed across 6 atoms. Migration `supabase/migrations/20260505140000_intake_extracted_text.sql` adds `extracted_text text` column + a `(lead_id, created_at desc)` index, and drops orphaned (`lead_id IS NULL`) rows + their storage objects. **Migration is staged but not yet applied — Jake to run when ready** since it's destructive. `pdf-parse` installed, `src/app/api/intake/extract/route.ts` rewritten with PDF branch (dynamic-imported to dodge the test-fixture quirk), explicit image / audio-video / unknown-binary placeholders, and writes `extracted_text` alongside the 600-char `summary`. Cap raised 12k → 50k chars for text bodies (PDFs need it). Upload route now requires `lead_id` (400 if missing). New route `src/app/lead/[id]/intake/page.tsx` mirrors `graph/page.tsx`: loads via existing `loadLeadBoxPageData`, renders `LeadSubNav` + new `LeadIntakeUploader` (lead_id baked in, kicks off extraction async after upload, `accept` filter for the supported MIME set) + extended `IntakeArtifactsPanel` with `showExtracted` prop that adds a collapsible `<details>` per file showing the full extracted text. `src/app/intake/` directory deleted. `redirectIntakeArtifactDownload` action moved into `src/app/lead/[id]/actions.ts` and now redirects to the lead's intake tab on errors (was: box tab). `TabNav` `TabKey` union loses `"intake"` and the array entry. `middleware.ts` matcher comment updated (already covered by `/lead/:path*`). `buildLeadContextBlock` in `api/chat/route.ts` extended: after the lead summary, calls `listIntakeArtifactsForLead(leadId, 10)` and appends a `## Intake materials` section, per-file capped at 3000 chars and total capped at 15k chars (truncated-files trailer if exceeded). System prompt nudge tells the agent to trust intake content over training defaults for proposal terms, contract dates, CSV row contents. `LeadSubNav` now shows Plan / Graph / Box / Intake / Heartbeat. **`npm run typecheck` clean. `npm run build` clean — `/lead/[id]/intake` route 1.68 kB / 97.7 kB First Load JS, top-level `/intake` removed from the route table.** Phase 2 (Gemini) deferred to a separate plan; image / audio / video uploads work today but show the "extraction deferred to Phase 2" placeholder. Operator workflow: `/lead/<id>/intake` → upload PDF → see extracted text appear in the collapsible after a moment → switch to `/chat` Lead mode → ask "what does the proposal say about deposit terms" → agent answers from the intake block without calling any tool.

## 2026-05-04

- **before [settings MCP auth row semantics]:** Jake reported `/settings` shows `CLOSE_MCP_AUTH_HEADER` as missing (red) while the pill says MCP fallback ready — confusing because `close-mcp.ts` already falls back to `Bearer ${CLOSE_API_KEY}` when the override env is blank.
- **after [settings MCP auth row semantics]:** `settings/page.tsx` builds the MCP table’s auth row from `CLOSE_MCP_AUTH_HEADER` if set, else from `CLOSE_API_KEY` (green dot, status `optional · uses CLOSE_API_KEY`, fingerprint column `Bearer ***last4`). Generic `table()` accepts optional `statusLabel` on cells. Intro blurb spells out leaving the header blank. No need to duplicate the API key in env.

- **before [Close MCP env template]:** Jake asked for commented `.env` templates documenting Close MCP URL plus `Close-API-Key` / `Close-Scope` header semantics without removing existing secrets.
- **after [Close MCP env template]:** Done. `.env.local` now has one Close MCP section after webhook vars: documented URL, app vars (`CLOSE_MCP_URL`, `CLOSE_MCP_AUTH_HEADER`), plus mirror placeholders `CLOSE_MCP_HEADER_CLOSE_API_KEY` / `CLOSE_MCP_HEADER_CLOSE_SCOPE` for Close's documented HTTP headers. `.env` received the same single block (duplicate from an intermediate edit removed).

## 2026-05-02

- **before [verify /settings MCP fallback panel]:** Snapshot — Jake added an MCP fallback config panel to `src/app/settings/page.tsx`: a `closeMcpStatus` import, three derived constants (`mcpState`/`mcpLabel`/`mcpBlurb`), an `mcpEnvRows` array, a new `<div className="cmk-stack-panel cmk-stack-panel--sky">` panel slotted between "Reserved for later" and "Where these are used", and one new `<li>` in the bottom reference list. About to run `npm run typecheck` then `npm run build` from `/Users/jakeaaron/ComeketoAgent`. If build trips a phantom `_document` error, recover by `rm -rf .next` and retrying. Reporting typecheck pass/fail (with verbatim errors), build pass/fail, and the `/settings` route bundle size.
- **after [verify /settings MCP fallback panel]:** Both green on the first try, no `.next` recovery needed. `npm run typecheck` (tsc --noEmit) exit 0, no errors. `npm run build` (next build) exit 0, "Compiled successfully", 20/20 static pages, no warnings. `/settings` route bundle: **1.19 kB / 97.2 kB First Load JS** — the panel adds tiny weight relative to the page baseline (was bumped marginally by the new panel + reference `<li>`). MCP fallback config surface now lives on the operator settings page alongside the rest of the env reveal.

- **before [trace correlation + composite audit ack]:** Two coupled fixes I caught while staging the next move. (1) Composite tools self-generate `randomUUID()` for `trace_id` per call — but the chat route also generates its own `traceId` and stamps it on the persisted `cmk:trace` fence. So a single batch turn produces 2-3 different trace ids: the one in the chat message vs. the ones in the `execution_log` rows. The widget's `trace ↗` link clicks into a partial picture instead of the whole turn. (2) Composite tools self-log via direct `logExecution` calls inside the dispatcher — bypassing `logDelegationsToolCall`, which means the route's `delegationsAuditLogged` flag stays false, so `revalidatePath('/console')` never fires after batch turns. Fix shape: `dispatchCompositeTool` accepts `traceId` parameter from caller and uses it everywhere; `logDelegationsToolCall` recognizes composite write-class names and returns true (acknowledging "audited elsewhere") without re-logging. Anti-churn note: was about to build STOP_SIGNAL detection from scratch — `detectStopSignal` already exists in `close.ts` with 10 patterns covering legal opt-outs, fully wired into heartbeat verdict at line 322 with skip code that bypasses solo mode. Production-quality already. Did not rebuild.
- **after [trace correlation + composite audit ack]:** Done. Three edits across three files. (1) [`composite-tools.ts`](src/lib/composite-tools.ts) `dispatchCompositeTool` signature gains optional third arg `opts?: { traceId?: string | null }`; `generate_plans_for_leads` and `approve_and_fire_plans` resolve `const traceId = opts?.traceId ?? randomUUID()` instead of always rolling fresh — script callers still get an auto-generated id, route callers thread their own. (2) [`api/chat/route.ts`](src/app/api/chat/route.ts) dispatch site passes `{ traceId }` (the route's id) into composite calls. (3) [`delegations-tool-audit.ts`](src/lib/delegations-tool-audit.ts) gains `COMPOSITE_SELF_AUDITED_TOOLS = Set('generate_plans_for_leads', 'approve_and_fire_plans')` and a guard that returns `true` for those names — no re-logging, just flips the route's `delegationsAuditLogged` flag so `revalidatePath('/console')` fires. Result: every chat turn that fires composite writes ends with one `trace_id` shared across the chat message, the per-success `generate_plan` rows, the `approve_run` rows, and the heartbeat-driven downstream rows. `trace ↗` from a fire-batch widget shows the whole turn. tsc clean. Anti-churn: did not rebuild `detectStopSignal` — it exists in `close.ts:818` with full coverage and is already wired into the heartbeat verdict path.

- **🔒 RULE LOCK [no demo, ever]:** Jake's directive (2026-05-02 evening, verbatim): *"Let's just stop with demos. No Demo. We either build the whole big fucking thing, or we don't touch it. Because this thing that we're building right now isn't a demo. It's the thing that I'm going to be turning in in a week. I don't want to rebuild it again, and I hate cleaning up demo sludge after we do it."* Working rule going forward: every move ships at production quality or doesn't ship. No demo loops, no demo data, no "demo for now / wire it later" stubs. If a feature can't be production today, it doesn't ship today — same standard as the existing CLAUDE.md "no inert UI" rule, but applied to the work *itself*, not just the surface. Verb hygiene: don't say "demo" in tap entries, prompts, comments, or commit messages — call the thing what it is (the build, the run, the loop, the test, the verification). When tempted to scope something down to "just enough to demo," don't — either build it real or defer to the next move.

- **before [Lane B — composite chat tools]:** Andre's chat verb gap exposed today by the 2/196 screenshot — 29 Close tools all operate on one lead at a time, so "make plans for my top 5 and fire them" is 30+ chained calls and no clean batch result shape. Adding three composite tools as thin wrappers over existing single-lead paths so chat becomes the OS instead of the debug. Working in parallel with Lanes A/C/D — collision strategy: new file `src/lib/composite-tools.ts` (no edits to `close-tools.ts`); chat route gets surgical edits (import + tools merge + dispatch branch + system-prompt section).
- **after [Lane B — composite chat tools]:** Done. New file [`src/lib/composite-tools.ts`](src/lib/composite-tools.ts) exports `COMPOSITE_TOOLS` (3 OpenAI Responses tool defs) + `dispatchCompositeTool` + `isCompositeTool`. Three tools live: `find_top_n_leads_for_owner` (sorts `closeListLeadsByAssignee` / `closeListLeads` by `date_updated`, defaults owner=andre/n=5/exclude_won_lost=true, cap n=25), `generate_plans_for_leads` (fan-out `generateSevenDayPlanForLead`, ownership/status gated per lead, concurrency 3, cap 10 ids, returns plan_id + primary_goal + best_next_question per success), `approve_and_fire_plans` (voice-clean day-approve loop + `runHeartbeatForPlan` per plan, concurrency 2, cap 10, returns actions_eligible/fired/skipped + skip_breakdown). [`api/chat/route.ts`](src/app/api/chat/route.ts) gets four edits: composite imports, `tools: [...CLOSE_TOOLS, ...COMPOSITE_TOOLS]`, dispatch branch on `isCompositeTool`, "### Batch — chain across many leads" prompt section between Plan and MCP fallback. Each batch tool emits a shared `trace_id` and writes `execution_log` rows (`generate_plan` / `approve_run`) so console correlation works for batch turns. tsc clean. Full operator loop now runs in one turn: "make me 5 plans for my top leads and fire them" → find → plan → approve+fire → terse summary.

- **before [Lane B widgets — composite tool rendering]:** Lane B tools fire and write rows to `execution_log` correctly, but the chat surface still renders their results as raw 600-char-truncated JSON in the tool-trace card. That's debug surface, not the product Andre is going to use. Closing the loop on Lane B end-to-end: bump the truncation cap (600 → 2000 — covers all current tool shapes including `pipeline_state_for_owner`, `generate_plans_for_leads` × 5, `approve_and_fire_plans` × 5), then add three `RichToolResult` widget branches in `ChatPanel.tsx` matching the existing `cmk-rich-*` visual language. Production quality only — every row is a working click-through to the real data, no placeholder rows, no "preview" framing.
- **after [Lane B widgets — composite tool rendering]:** Done. Five edits across two files: [`api/chat/route.ts`](src/app/api/chat/route.ts) summary truncation 600→2000 chars; [`ChatPanel.tsx`](src/app/chat/ChatPanel.tsx) gets `TOOL_CATEGORY` entries for the three composite names, `toolHeadline` cases ("Top {n} · {owner}" / "Plans · {n} leads · {h}d" / "Approve & fire · {n} plans"), `pinFromCall` branches that map each result to a tool-result pin with meaningful sublabel ("5 of 50 andre leads" / "4/5 ok · 1 skipped · 0 failed" / "8/12 actions fired"), and three new `RichToolResult` branches: **find_top_n_leads** renders a header strip (rank · name · status · relative-time) with each row a `Link` to `/lead/{id}`; **generate_plans_for_leads** renders per-lead mini-cards with status tag (`plan ready` sage / `OWNERSHIP` skip pill mono / `error` rose), primary_goal in serif italic, best_next_question in muted body, and a `trace ↗` link to `/console?trace=`; **approve_and_fire_plans** renders a 4-up totals strip (approved / eligible / fired / gated, lavender / sky / sage / peach respectively) plus per-plan rows showing fired/eligible counts mono, voice-blocked + stale-Box pills, and skip-code chip rail (top 4 codes). [`globals.css`](src/app/globals.css) gets `.cmk-rich-toplist`, `.cmk-rich-batch-plans`, `.cmk-rich-batch-fire`, `.cmk-rich-batch-head`, `.cmk-rich-batch-totals`, `.cmk-rich-batch-pill-*`, `.cmk-rich-batch-trace`, mobile collapse rules — all in the `cmk-rich-*` family with the existing card chrome (cream + 0.5px rule + shadow-sm + paper-2 head). tsc clean. Operator now sees structured product surfaces for every batch turn, not JSON.

- **before [Lane C — Close MCP fallback]:** Direct Close client wraps ~30 endpoints; everything else hits a wall ("we don't have a tool for that yet"). Need an audited fallback path so the agent can reach untyped Close operations through the official Close MCP server while keeping the gated direct tools as the primary path. Reverses the 2026-05-01 "no MCP in repo, ever" memo intentionally — MCP is now a parametric escape hatch, not a primary architecture.
- **after [Lane C — Close MCP fallback]:** Five-atom landing. New [`src/lib/close-mcp.ts`](src/lib/close-mcp.ts) (JSON-RPC 2.0/HTTP client, accepts plain JSON or SSE response, 15s timeout, structured `{ ok, error }` returns — never throws into dispatcher). Two new chat tools `close_mcp_list_tools` + `close_mcp_call` registered in [`close-tools.ts`](src/lib/close-tools.ts) (CLOSE_TOOLS array + CloseToolName union + dispatchCloseTool switch). Env adds `CLOSE_MCP_URL` + `CLOSE_MCP_AUTH_HEADER` (latter optional, defaults to `Bearer ${CLOSE_API_KEY}`); both surface in `envStatus()`. New `mcp_fallback` ExecutionLogKind in [`execution-audit.ts`](src/lib/execution-audit.ts); [`delegations-tool-audit.ts`](src/lib/delegations-tool-audit.ts) stamps every `close_mcp_call` with verb-heuristic `is_write` flag in payload. System prompt in [`api/chat/route.ts`](src/app/api/chat/route.ts) gains an "MCP fallback (escape hatch)" section teaching discipline (list first, prefer direct gated tool, customer-facing writes need operator confirmation). Built clean alongside Lane B's parallel merge of `composite-tools.ts` + `pipeline-state.ts`. tsc + next build green; /chat 60.1 kB. Code lands today; URL/auth filled by Jake on his timeline — when `CLOSE_MCP_URL` is blank, tools return clean `{ error: "Close MCP not configured" }` instead of crashing.

- **before [Lane C — `/test` MCP health check]:** Lane C's natural follow-on. With the MCP fallback wired in chat, operators need a one-click verification path that confirms `CLOSE_MCP_URL` + auth resolve and lists the tool catalog the chat agent will see. Without it, the only way to prove the MCP layer works is to ask the agent something it doesn't have a direct tool for and hope it routes correctly. Add `close-mcp` mode to `/api/test` route + a fifth section on `/test` page matching the existing pattern (title/description/button/result-pane). On success, surface tool count + first 8 tool names + first-call latency. Doesn't block on URL — when `CLOSE_MCP_URL` is blank, the mode returns the same `{ ok: false, error: "Close MCP not configured" }` shape the chat path returns, so operators see exactly what the agent would see.
- **after [Lane C — `/test` MCP health check]:** Two-file landing. [`api/test/route.ts`](src/app/api/test/route.ts) gains a `testCloseMcp()` function + `close-mcp` mode in the validator and dispatcher. When `CLOSE_MCP_URL` is blank: returns `{ configured: false, url_set, auth_resolved, tool_count: 0, sample_tool_names: [], hint }` as a successful response — operators learn what's missing without an exception. When configured: calls `closeMcpListTools()`, returns `{ configured: true, url_set, auth_resolved, tool_count, sample_tool_names: top12, has_descriptions }`; transport errors throw and surface in the existing error pane. [`test/page.tsx`](src/app/test/page.tsx) Mode union extended + fifth MODES entry titled "Close MCP fallback" describing what the click does. tsc + next build clean; `/test` 4.16 kB / 100 kB First Load. Operator path now: drop `CLOSE_MCP_URL` → click the button → see tool catalog + latency in the result pane → know the chat agent's fallback will route correctly before asking it to.

- **before [Lane C — `/console` kind filter]:** Lane C built the audit kind (`mcp_fallback`) but `/console` only filters by `?trace=`, no way for operators to scan "did the agent reach outside the gated path today, and what for?" without reading every row. Adding a generic `?kind=<ExecutionLogKind>` filter to the existing `/console` page so the question is one click. New helper `listExecutionByKind(kind, limit)` in [`execution-audit.ts`](src/lib/execution-audit.ts) mirrors `listExecutionByTraceId`; page validates the param against the live `ExecutionLogKind` enum (rejects unknown values cleanly with the existing error-banner pattern); curated filter pills for the two highest-value kinds (`mcp_fallback`, `heartbeat_run`) plus a freeform jump-to-clear link. When `kind=mcp_fallback`, the Action column adds a sub-line showing the `target_tool` from payload so operators see *which* MCP tool the agent reached for. No collision with parallel lanes (`/console` was Lane C's surface in the original split).
- **after [Lane C — `/console` kind filter]:** Two-file landing. [`execution-audit.ts`](src/lib/execution-audit.ts) refactored: `EXECUTION_LOG_KINDS` const array is now the single source of truth, `ExecutionLogKind` type is derived from it (`(typeof EXECUTION_LOG_KINDS)[number]`), `isExecutionLogKind` type guard exposed for runtime validation, new `listExecutionByKind(kind, limit)` helper. [`console/page.tsx`](src/app/console/page.tsx) accepts `?kind=<ExecutionLogKind>` (trace filter wins when both are set), renders `KIND_PILLS` (4 curated shortcuts: MCP fallback / Heartbeat runs / Chat writes / Approve & run) when nothing is filtered, kind banner with clear-filter link when filtered, dedicated invalid-banner for unknown kind, and a `target_tool` + `is_write` sub-line on the Action cell when filtered to `mcp_fallback`. Build-agent caught one type-narrowing issue (`isWriteFlag` was inferred as `unknown` from the `&&` chain) and patched with `Boolean()` coercion before final pass; final tsc + next build clean. Operator question "did the agent reach outside the gated path today and what for?" is now one click from the Console toolbar.

- **before [Lane C — `/settings` MCP fallback panel]:** *Recorded retroactively — this move was announced in chat but not tapped in the ledger before executing. Flagged to Jake; ledger trail being honest about it.* Lane C's natural follow-on. The MCP env vars (`CLOSE_MCP_URL`, `CLOSE_MCP_AUTH_HEADER`) are wired into `envStatus()` already from the original Lane C atom, but `/settings` curates which env rows it surfaces (it doesn't dump envStatus generically). Result: operators couldn't see MCP config state at a glance. Adding a dedicated "Close MCP fallback" panel to `/settings` with the two env rows + a derived 3-state status pill (`ready` / `URL set · auth missing` / `off`) pulled from `closeMcpStatus()`, plus a one-line description that links to the operator forensics path (`/console?kind=mcp_fallback`) and the verification path (`/test` → MCP fallback button). One file edit, no new files, no collision risk.
- **after [Lane C — `/settings` MCP fallback panel]:** Done. [`settings/page.tsx`](src/app/settings/page.tsx) gains: import of `closeMcpStatus` from [`close-mcp.ts`](src/lib/close-mcp.ts); `mcpEnvRows` array (CLOSE_MCP_URL + CLOSE_MCP_AUTH_HEADER); `mcpState` discriminated 3-way derived from `closeMcpStatus()`; `mcpLabel` + `mcpBlurb` strings per state; new `<div className="cmk-stack-panel cmk-stack-panel--sky">` panel slotted between "Reserved for later" and "Where these are used" with status pill + env rows + cross-links to `/console?kind=mcp_fallback` and `/test`; one new `<li>` in "Where these are used" describing the two env vars. tsc + next build clean; `/settings` route 1.19 kB / 97.2 kB First Load. Operator can now see at a glance whether the MCP fallback is wired, alongside the other primary credentials.

- **before [Lane C — `enable_mcp_fallback` setting toggle]:** Closing the "system prompt is advisory, not enforced" watch item from the original Lane C after-tap. Right now the MCP fallback tools are unconditionally exposed when `CLOSE_MCP_URL` is set; if the model overuses fallback, operators have no kill switch except blanking the URL. Adding a `enable_mcp_fallback` boolean to `.cmk-settings.json` (default `true`), a server action + UI toggle in the existing MCP panel on `/settings`, and a tool-list filter in [`close-tools.ts`](src/lib/close-tools.ts) so when disabled the model literally doesn't see `close_mcp_list_tools` / `close_mcp_call` in its tools array. Defensive guard inside the dispatcher too — even if a stale message context tries to invoke them, returns `{ error: "MCP fallback disabled in settings" }` instead of executing. Five files: settings.ts (field), settings/actions.ts (action), settings/page.tsx (toggle UI in the existing MCP panel), close-tools.ts (filter helper + dispatcher gate), api/chat/route.ts (use filter helper). The chat route edit is one-line additive — Lane B is in this file but the change is scoped to the `tools:` argument shape.
- **after [Lane C — `enable_mcp_fallback` setting toggle]:** Five-atom landing. [`settings.ts`](src/lib/settings.ts) `Settings` type + `DEFAULT_SETTINGS` + `getSettings`/`setSettings` parse paths gain `enable_mcp_fallback: boolean` (default `true`). [`settings/actions.ts`](src/app/settings/actions.ts) gains `updateMcpFallbackAction` that revalidates `/settings` + `/chat`. [`close-tools.ts`](src/lib/close-tools.ts) exports `getCloseToolsForSettings({ enable_mcp_fallback })` (filters MCP entries when off) and gates `close_mcp_*` cases at the top of `dispatchCloseTool` via the new `hooks.enable_mcp_fallback` flag — defensive double-layer (filter from view + refuse on dispatch). [`settings/page.tsx`](src/app/settings/page.tsx) adds a `<SettingsForm>` checkbox toggle inside the existing MCP panel with copy that explains the kill-switch semantics + a contextual "URL not set yet" hint when the env var is blank. [`api/chat/route.ts`](src/app/api/chat/route.ts) one-line additive: replaces `[...CLOSE_TOOLS, ...COMPOSITE_TOOLS]` with `[...getCloseToolsForSettings({ enable_mcp_fallback: settings.enable_mcp_fallback }), ...COMPOSITE_TOOLS]` and adds `enable_mcp_fallback: settings.enable_mcp_fallback` to the dispatcher hooks. tsc + next build clean. Lane C is now end-to-end complete: MCP layer wired, audited, configurable, kill-switchable from operator UI without env edits.
- **before [error-handling review · PR #1]:** Need a focused pass on silent-failure / graceful-degradation risk in the analytics render, given the `as unknown as T` cast pattern used on every snapshot import.
- **after [error-handling review · PR #1]:** Findings written to `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/review/error-handling-findings.md`. Verdict APPROVE; one MEDIUM (`fmtCents` lets NaN through → `"$NaN"` in metric strip + funnel value tiles) and one LOW (5 of 9 chart sections lack the `length === 0` empty-state guard already used by 4 sections). Both auto-fixable in <10 lines total.

- **before [PR #1 review synthesis]:** Five parallel review agents wrote independent findings; need to combine into one master artifact + post a single comprehensive PR comment so Jake (and the auto-fix step) can act from one source of truth.
- **after [PR #1 review synthesis]:** Consolidated artifact at `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/review/consolidated-review.md` (14 findings: 0C / 4H / 6M / 4L). PR comment posted. Verdict `REQUEST_CHANGES` — 3 of 4 HIGHs and 4 of 6 MEDIUMs are auto-fixable in this PR; the test-coverage HIGH (snapshot-shape tests) + 2 MEDIUM extractions belong to a follow-up sprint. Auto-fix step queued next.

- **before [PR #1 auto-fix · CRITICAL+HIGH]:** Implement consolidated review's auto-fixable HIGHs (cohort slice direction, funnel arrow logic, legacy `_reference/CC Agent (legacy)` path) on the PR branch, then validate (typecheck/lint/test/build), commit, push so PR shows the fixes; defer test-coverage HIGH to follow-up sprint per review.
- **after [PR #1 auto-fix · CRITICAL+HIGH]:** 3 HIGHs + 4 MEDIUMs landed in `src/app/analytics/page.tsx` + `src/lib/analytics-types.ts`. HIGH 1 cohort slice now `[...].sort(b.cohort.localeCompare(a.cohort)).slice(0,12)` (verified: snapshot is oldest-first). HIGH 2 funnel arrow now only renders between Total→Active as "X% of total are still active" (Won/Lost siblings carry their own % of total). HIGH 3 legacy path stripped from header comment + JSX `<code>` tag + redundant `<Link href="/analytics">` self-link removed (dropped unused `Link` import). MEDIUM 1 `fmtCents` + `pct` tightened with `!Number.isFinite`. MEDIUM 2 `UrgencyCardSeg` deleted, imports `UrgencySegment` from central types. MEDIUM 3 `window_days: number` now required on `WinLossSnapshot._meta` + `RevenueTrendsSnapshot._meta` (verified both snapshots carry the field). MEDIUM 4 `missingViews` comment rewritten as timeless ("If a real comms-style snapshot lands later"). Validation: tsc --noEmit passes; vitest 12/12 passes; next build static-prerenders /analytics. Lint skipped (interactive ESLint setup not configured — pre-existing). HIGH 4 (snapshot-shape tests) + MEDIUM 5/6 (helper extraction) intentionally deferred to follow-up sprint per review verdict.

- **before [operator lock on `/`]:** Deployed demo could show marketing hero without cookie when env lock on.
- **after [operator lock on `/`]:** Matcher includes `/` — unauthed users hit operator login first; unset password/secret still means open tree.
- **before [login land on console from root]:** `next=/` after auth wasted a click back through TabNav to console.
- **after [login land on console from root]:** Operator login normalizes `/` → `/console` on successful cookie set.
- **before [operator nav + lock parity]:** Goals still called out wordmark home ambiguity (`/leads` vs `/chat` vs marketing).
- **after [operator nav + lock parity]:** `/console` is the default operator home click target; operator lock covers delegations + threads API when env on.
- **before [operator API matcher sweep]:** Lead activity/summary APIs and `/api/test` + `/test` page bypassed cookie lock.
- **after [operator API matcher sweep]:** Full operator API surface behind matcher except auth + webhooks; `/analytics` gated too.
- **before [console trace column]:** Table had no at-a-glance correlation for chat-originated audit rows vs heartbeat-only rows with null trace.
- **after [console trace column]:** Trace column surfaces non-null `trace_id`; heartbeat rows with trace still linkable.
- **before [trace → UI + console filter]:** Operators had no one-click path from a delegations reply to “just this run” in console.
- **after [trace → UI + console filter]:** Persisted trace fence + console `?trace=` + Run trace bar completes the demo thread.
- **before [delegations → execution_log]:** Demo path chat → reality needs the same audit tail as heartbeat (trace + kinds operators can scan).
- **after [delegations → execution_log]:** Chat-sourced writes/plan-gen emit `execution_log` + `trace_id` on POST so console/approvals stay correlated with delegations turns.
- **before [operator console]:** Mon/Tue demo needs a hub: where we are (execution mode), what needs human (approval queue), what just happened (audit tail).
- **after [operator console]:** Demo hub: one screen for “what mode are we in / what needs me / what just ran,” linked to chat · approvals · sweeps.
- **before [Lead Box multi-tab routes]:** Single `/lead/[id]` stacked plan, heartbeat, timeline, and profile grid — too tall; operator wants full-viewport tabs (Plan / Box / Heartbeat).
- **after [Lead Box multi-tab routes]:** `loadLeadBoxPageData` + `layout.tsx` shell; sub-nav **Plan** (`/lead/[id]`), **Box** (`/box`), **Heartbeat** (`/heartbeat`); `revalidatePath(..., "layout")`; viewport CSS (`lead-main--tab`, column scroll on Box).
- **before [Lead Box Box tab layout + surface polish]:** Activity column content-height left a tall empty gap; minimal motion on feeds, subnav, primary actions.
- **after [Lead Box Box tab layout + surface polish]:** `lead-activity-card` + `lead-feed-scroll` fill grid row; gradients, hover/lift, tab mount fade, stronger feed rows + box-panel + enroll + plan-btn transitions.
- **before [chat Close tool breadth]:** Delegations chat had a skinny Close tool set (~16); Jake wants ~30 high-value tools without MCP.
- **after [chat Close tool breadth]:** 29 delegations-chat Close tools (list leads / by assignee / by status, activities, subscriptions, update lead + merge_patch_json, task, draft email+SMS activity, create lead, webhooks list, sequence create+update) — [`close-tools.ts`](src/lib/close-tools.ts).
- **before [Close leads assignee search]:** `/leads` Andre tab used POST `/data/search/` with `regular_field` `user_id` on `lead` — Close 400 (`user_id` not filterable on lead in Advanced Filtering); Jake had no owner pill.
- **after [Close leads assignee search]:** `closeListLeadsByAssignee*` implemented via paginated `GET /lead/` + `user_id` match; `/leads` adds **Jake** pill when `CLOSE_USER_ID_JAKE` set.
- **before [automation UX sweep — Rodbot + inspector + drafts]:** Jake wants Rodbot naming and noisy draft form gone; inspector rail real; compact workflow header; empty sequences shows demo graph; interpret path via delegations chat.
- **after [automation UX sweep — Rodbot + inspector + drafts]:** Demo actor → **Agent** (`sub_agent`); Rodbot removed from library/glyphs/header/footer; `AutomationCanvas` `forwardRef` + `externalInspector` + side panel; sequences empty state embeds `SequencesWorkflowPreview`; draft detail strips JSON/name form, CTA to `/chat?draft&draftName` with composer seed; `Suspense` + `useSearchParams` in chat.
- **before [workflow studio route]:** Surface CC Agent–style workflow canvas at `/automation/workflows` (demo Morning Sweep graph) without removing Sequences/Drafts.
- **after [workflow studio route]:** `/automation/workflows` + `WorkflowStudio` shell (library/inspector rails, play/zoom/theme), sub-nav **Workflows**, demo renamed to Grid v1 + 8:45 trigger; `AutomationCanvas` supports `pulseNodeId`.
- **before [automation empty states + matcher doc]:** Status filter with zero hits still showed demo canvas; drafts list empty was a single muted line; middleware had no route inventory for future matcher edits.
- **after [automation empty states + matcher doc]:** `/automation` distinguishes “no match for Active|Paused|Draft” from truly empty Close list; [`drafts/page.tsx`](src/app/automation/drafts/page.tsx) empty card + links; [`middleware.ts`](src/middleware.ts) comment lists protected roots + public exceptions; preview copy + Guardrails lede avoids `§`.
- **before [intake route + TabNav + attachment types]:** Intake surface missing on disk; chat TS failed on `text` attachments using `data_url`.
- **after [intake route + TabNav + attachment types]:** Functional `/intake` (upload + recent list + signed download action); `TabNav` intake; `ChatPanel` user bubble + pending strip handle image vs text.
- **before [intake download error surfacing]:** Failed signed URL / mismatch redirected to Box with no explanation.
- **after [intake download error surfacing]:** `intake_dl` query (`bad_request` / `not_found` / `signed_url`) + dismissible banner on [`IntakeArtifactsPanel`](src/app/lead/[id]/IntakeArtifactsPanel.tsx).
- **before [ledger / agent discipline — anti-churn]:** Jake reminded: **`Goals` / `Problems` / `Global`** taps exist so intent + outcomes are explicit; agents were **restoring deleted trees** just to quiet `tsc` / build — churn with user intent.
- **after [ledger / agent discipline — anti-churn]:** Working rule recorded in ledgers: **before tap = planned move + respect deletions**; **after tap = what landed**; do **not** resurrect removed app surfaces **without Jake explicit OK** — default is **remove references / matcher / TabNav** (or ask) instead of replaying restore/delete.
- **before [panel + icon-hover UI pass]:** Jake wants **tinted panels** everywhere, less default form “slots”, **subtle 1px icon rings** on hover (Vercel-adjacent polish).
- **after [panel + icon-hover UI pass]:** [`globals.css`](src/app/globals.css) `.cmk-icon-hit`, `.cmk-stack-panel*`, `.cmk-field-panel`, `.cmk-file-lane`, chat attach/send hover rings; [`TabNav`](src/components/TabNav.tsx) / [`AppHeader`](src/components/AppHeader.tsx); [`settings/page.tsx`](src/app/settings/page.tsx) section stacks; [`intake`](src/app/intake/) panels; [`operator-login`](src/app/operator-login/page.tsx); [`heartbeat/webhooks`](src/app/heartbeat/webhooks/page.tsx); leads + plan field styling.
- **before [panel pass — console + leads + approvals]:** `/console`, `/leads`, `/approvals` still read flat on the canvas next to settings/intake (KPI strips + search + tables unpaneled).
- **after [panel pass — console + leads + approvals]:** Lavender KPI stack + sky trace filter + sage execution table on [`console/page.tsx`](src/app/console/page.tsx); sky filter stack + sage results on [`leads/page.tsx`](src/app/leads/page.tsx); sage queue panel on [`approvals/page.tsx`](src/app/approvals/page.tsx); nested strip / trace / empty padding utilities in [`globals.css`](src/app/globals.css).
- **before [OpenAI breadth — 5.5 + media + background]:** Model picker stopped at GPT‑5.4 snapshots; no TTS / STT / image demos; delegations had no “long runner” path surfaced for OpenAI background Responses.
- **after [OpenAI breadth — 5.5 + media + background]:** [`AVAILABLE_MODELS`](src/lib/settings.ts) adds **gpt‑5.5** family (aliases + dated snapshots + pro); [`/api/openai/tts`](src/app/api/openai/tts/route.ts), [`transcribe`](src/app/api/openai/transcribe/route.ts), [`image`](src/app/api/openai/image/route.ts) + [`OpenAiMediaLab`](src/app/test/OpenAiMediaLab.tsx) on `/test`; chat **Deep think** = `store`+`background` Responses + [`pollBackgroundResponse`](src/lib/openai-background.ts) (no Close tools that turn); [`middleware`](src/middleware.ts) matches `/api/openai`; chat route `maxDuration` 300 for long polls.

- **before [Lane D — heartbeat dashboard tells the truth]:** Four-fork sprint launch. `/heartbeat` KPI strip currently leads with "2/196 firing"-shaped math that reads as failure; truth is most of those actions are scheduled for other days or one-click-from-going-out. Reframe so the dashboard answers "what's my morning look like" instead of "why didn't this fire." **Atoms:** (1) inventory `src/app/heartbeat/page.tsx` + `src/lib/heartbeat.ts` aggregator + relevant globals.css; (2) new aggregator returning `today_eligible / waiting_on_approval[] / fired_today[] / not_today_count / gated_by_code`; (3) KPI strip rewrite — three cards: **Today eligible · Waiting on you · Fired today**; (4) inline lead names + action types under Fired + Waiting cards (no buried clicks); (5) collapse skip-code breakdown into a forensics `<details>`; (6) CSS for new KPI tones (waiting = action, fired = win, today eligible = neutral). No DB schema changes; aggregation + presentation only. Lanes A (heartbeat verdict / settings UI), B (chat composite tools), C (MCP fallback) own different files — no collisions.

- **after [Lane D — heartbeat dashboard tells the truth]:** Landed. tsc clean. New file [`src/lib/heartbeat-truth.ts`](src/lib/heartbeat-truth.ts) — `aggregateOperatorTruth()` walks `heartbeat_runs.report` (DayVerdict[]) and returns `{today_eligible, waiting_on_approval[], fired_today[], not_today_count, gated_today[]}` plus the historical eligible/skipped totals for the forensics block. Latest-per-lead semantics for current-state buckets; every fire across 24h kept in `fired_today` (each is a real Close write). `WAITING_ON_OPERATOR_CODES = {DAY_NOT_APPROVED, EXECUTION_DISABLED}` so the surface flags both "approve to fire" and "flip mode to live" as operator-actionable. [`src/app/heartbeat/page.tsx`](src/app/heartbeat/page.tsx) rewritten — three KPI cards (Today eligible · Waiting on you · Fired today) lead the page, each with inline lead rows (channel chip + linked lead_id + truncated intent + relative time on fired / "flip mode" tag on EXECUTION_DISABLED) capped at 6 with `+ N more · go to approvals →` overflow. Below: a one-line `also today:` strip with non-operator gates (SEND_WINDOW / FREQUENCY_CAP / STALE_BOX / VOICE_FAIL etc.). Below that: `<details>` "Skip breakdown — forensics" containing the original 4-card eligible/fired/skipped strip + the top-skip-codes bar (existing `aggregateLast24h` aggregator preserved). Recent runs table unchanged. [`src/app/globals.css`](src/app/globals.css) gains `~190` lines: `.hb-truth-strip` 3-col grid (collapses to 1-col under 1100px), `.hb-truth-card` with three tone variants (eligible=lavender, waiting=peach with left accent, fired=sage with left accent), `.hb-truth-num` 38px serif, `.hb-truth-list` + `.hb-truth-row` (56/130/1fr/auto grid), reuses `.plan-action-chip-{call|email|sms|task}` for channel chips, `.hb-truth-row-tag` peach pill for "flip mode", `.hb-truth-gated-line` wrap row for non-operator skips, `.hb-forensics` collapsible details with rotating chevron + meta line. No DB schema change. Lane A consumes this aggregator's shape from chat-side per their before-tap.

- **before [Lane A — pipeline-state tool + chat widget]:** Jake reassigned Lane A from heartbeat-verdict (now scoped into Lane D) to the chat-side mirror: a `pipeline_state_for_owner` tool that returns the same morning-briefing shape Lane D renders on `/heartbeat`, but consumable from chat as a `RichToolResult` widget. Andre lives in chat, the dashboard is a debug surface — chat needs the answer to "what's my morning?" without leaving the conversation. **Atoms:** (1) read `lib/heartbeat.ts` + `lib/plans-db.ts` + `lib/plan.ts` + `lib/close-tools.ts` + `app/chat/RichToolResult.tsx` + `app/chat/ChatPanel.tsx` to lock aggregation + tool-registration shape; (2) new `src/lib/pipeline-state.ts` returning `{today_eligible_count, waiting_on_approval[], fired_today[], gated, total_active_plans}` — compact payload sized to live under the 600-char tool-result truncation; (3) new `pipeline_state_for_owner({owner})` chat tool registered in `close-tools.ts` + dispatched in `api/chat/route.ts`; (4) new branch in `RichToolResult.tsx` switch rendering structured card with drill-in lists; (5) extend `pinFromCall` in `ChatPanel.tsx` so the result pins as kind `pipeline-state`; (6) system-prompt nudge: "for 'state of pipeline', 'what's my morning', 'what's going on today', call this tool first." Coordination: Lane B also extends `close-tools.ts` (additive, narrow); Lane C also extends `RichToolResult` (different switch branch); Lane D shares aggregation shape — if Lane D ships their aggregator first I'll consume rather than fork.

- **after [Lane A — pipeline-state tool + chat widget]:** Landed. tsc clean, build artifacts present (`.next/server/app/chat/page.js` regenerated). Files: new [`src/lib/pipeline-state.ts`](src/lib/pipeline-state.ts) (server-only aggregator, ~270 lines: walks active `lead_plans` for today-eligible + waiting buckets via `isTodayOrCatchup` mirroring `lib/heartbeat.ts` catchup window; walks `heartbeat_runs` last-24h scope='lead' deduped per-lead for `fired_top` + `gated_top`; uses `closeListLeadsByAssignee` to build a `lead_id → display_name` map so the widget shows real names, not truncated ids). Compact output by design: counts + 5-item example arrays + 6-item gated_top, fits within ~700 chars stringified so the chat `cmk:tools` 600-char summary preview renders structured content most of the time (gracefully falls back to raw JSON pre on truncation). Tool registered in [`close-tools.ts`](src/lib/close-tools.ts): `pipeline_state_for_owner({owner})` enum andre/jake/all, default andre. System prompt in [`api/chat/route.ts`](src/app/api/chat/route.ts) gains a **Briefing** section + a leading bullet under "Common operator patterns" so the agent reaches for this tool first when the operator asks a briefing-shaped question. Chat UI in [`ChatPanel.tsx`](src/app/chat/ChatPanel.tsx): added to TOOL_CATEGORY (list/Survey), new `toolHeadline` case ("Morning briefing · andre"), new `pinFromCall` branch (kind: tool-result, label "Morning · andre", sublabel "{N} eligible · {M} waiting · {K} fired"), new `RichToolResult` branch rendering tone-tinted KPI strip (today=lavender, waiting=peach, fired=sage, plans=cream) + Waiting/Fired row lists with channel chip + linked `/lead/[id]` + intent + Top skip-codes pill row. CSS in [`globals.css`](src/app/globals.css): ~110 lines of `.cmk-rich-pipeline*` (KPI grid, row layout, gated pills, mobile breakpoint at 720px). **Important:** Lane D shipped their own server-only aggregator `aggregateOperatorTruth()` in `lib/heartbeat-truth.ts` between my before-tap and after-tap; we did NOT collide because we landed independent files (mine `pipeline-state.ts`, theirs `heartbeat-truth.ts`). Both walk the same source tables but with slightly different aims: Lane D returns shapes for the `/heartbeat` page renderer; mine returns owner-scoped shapes with name resolution for the chat tool. Follow-up: unify into one aggregator if/when DRY pressure justifies the merge — for now both work and shipping parallel was the right call.

- **before [Lane D follow-up — lead names on the truth strip]:** Watch item from Lane D after-tap is the obvious next atom: rows on `/heartbeat` show truncated `lead_xxx…` because `heartbeat_runs` doesn't store display names. Lane A's `pipeline-state.ts` already proved the pattern (one `closeListLeadsByAssignee` call per owner, build a name map, render with fallback). Porting that into `/heartbeat`. Single move: collect unique lead_ids from `truth.waiting_on_approval` + `truth.fired_today`, fire `closeListLeads({limit: 250})` (one call — practice org has 101 leads total, sweeps cover all owners not just Andre), build `Map<lead_id, display_name>`, render display_name with fallback to truncated id. CSS: widen the lead column from 130px fixed to flexible so longer names don't clip. Touches `src/app/heartbeat/page.tsx` only — no aggregator change, no DB change. Demo-grade readability.

- **after [Lane D follow-up — lead names on the truth strip]:** Landed. tsc clean. [`heartbeat/page.tsx`](src/app/heartbeat/page.tsx) gains `resolveLeadNames()` helper + `displayName(id, map)` — the helper does one `closeListLeads({limit: 250})` call wrapped in defensive try/catch so a Close outage falls back to truncated ids without breaking the page. Joined the existing `Promise.all` so the Close hit runs in parallel with the three Supabase queries (no added latency on the critical path). Both row renderers (Waiting, Fired) now use `displayName`. CSS in [`globals.css`](src/app/globals.css): row grid widened from `56px 130px 1fr auto` → `56px minmax(150px, 200px) 1fr auto`; `.hb-truth-row-lead` flipped from mono 11px → sans 12.5px weight 500 with `overflow:hidden; text-overflow:ellipsis; white-space:nowrap; min-width:0` so long names truncate cleanly. Lead-id stays as `title=` tooltip on hover for forensic lookups. Reads "Holloway Events · email" instead of "lead_f0mnUGzqDM…". Demo-grade.

- **before [Lane D — auto-refresh the truth strip]:** When the cron sweep fires, when chat fires `approve_and_fire_plans` (Lane B just landed it), or when the Lead Box "Run heartbeat now" runs — the dashboard is stale until the operator hits ⌘R. Live demo energy demands auto-refresh. Tiny atom: a `"use client"` component `HeartbeatAutoRefresh.tsx` that mounts `setInterval(() => router.refresh(), 30_000)`, paused via `document.visibilitychange` so a hidden tab doesn't burn API calls. Mount once on the `/heartbeat` page above the toolbar. ~25 lines. No collisions; touches a new file + page.tsx import line. No DB or aggregator changes.

- **after [Lane D — auto-refresh the truth strip]:** Landed. tsc clean. New file [`heartbeat/HeartbeatAutoRefresh.tsx`](src/app/heartbeat/HeartbeatAutoRefresh.tsx) (~50 lines) — `"use client"` component using Next.js App Router's `useRouter().refresh()`. Visibility-aware: starts the 30s interval only when `document.visibilityState === "visible"`, stops on hidden, and fires one immediate refresh on return-to-foreground so a backgrounded operator doesn't see stale data when they tab back. Mounted once on `/heartbeat` between `TabNav` and `<main>`. `router.refresh()` is App Router's surgical re-fetch — re-runs the Server Component data path in place without a hard navigation, so the truth strip + recent-runs table tick without flash or scroll-jump. When chat fires Lane B's `approve_and_fire_plans` or the cron sweep runs, the dashboard reflects new fires within 30s with no manual reload.

- **before [pre-break ledger sweep]:** Consolidate automation epic + sub-nav polish into Goals/Problems for Jake’s return.
- **handoff [pre-break] — milestone recap (single source of truth):**
  - **Automation + multi-touch epic (plan A–G):** Honest heartbeat copy via [`src/lib/heartbeat-summary.ts`](src/lib/heartbeat-summary.ts) (client-safe; avoids bundling `heartbeat.ts`). Multi-touch: [`src/lib/plan.ts`](src/lib/plan.ts) prompt bias + [`appendRequiredActionToPlanDay`](src/lib/plans-db.ts) + [`addPlanDayTouchAction`](src/app/lead/[id]/actions.ts) + PlanDayCard “Add another touch” + PlanSection frequency note. Real graph: [`src/lib/close-workflow-graph.ts`](src/lib/close-workflow-graph.ts), [`src/app/automation/[id]/page.tsx`](src/app/automation/[id]/page.tsx), Vitest in [`close-workflow-graph.test.ts`](src/lib/close-workflow-graph.test.ts). Drafts + AI + publish: migration [`supabase/migrations/20260501180000_automation_drafts.sql`](supabase/migrations/20260501180000_automation_drafts.sql), [`src/lib/automation-drafts.ts`](src/lib/automation-drafts.ts), [`src/lib/sequence-ai.ts`](src/lib/sequence-ai.ts), [`src/app/automation/actions.ts`](src/app/automation/actions.ts), routes under `/automation/drafts`, [`closeCreateSequence`](src/lib/close.ts) / [`closeUpdateSequence`](src/lib/close.ts). Run watch: [`SubscriptionRunWatch.tsx`](src/app/lead/[id]/SubscriptionRunWatch.tsx), [`getSequenceSubscriptionSnapshotAction`](src/app/lead/[id]/actions.ts). Reference table: [`_reference/close-llms.md`](_reference/close-llms.md). Audit kinds extended in [`execution-audit.ts`](src/lib/execution-audit.ts).
  - **Product IA polish:** [`AutomationSubNav.tsx`](src/app/automation/AutomationSubNav.tsx) + `.ag-subnav*` in [`globals.css`](src/app/globals.css) on all four automation routes; sequences list lede links Drafts without a duplicate CTA button.
- **Verification last run:** `npm run typecheck`, `npm test`, `npm run build` — clean.
- **next when back (North Star: real app, not demo):** (1) AppHeader wordmark default for operators (`/leads` or `/chat` vs marketing `/`). (2) Stronger empty states (drafts + sequences lists) with primary CTA. (3) Optional: run watch + activity cross-check (plan stretch).
- **after [pre-break ledger sweep]:** Single handoff recap block above; older chronological `before/after` lines kept for audit trail.
- **before [automation IA polish]:** Add first-class Sequences / Drafts sub-nav under Automation tab so drafts are not a buried demo affordance.
- **after [automation IA polish]:** `AutomationSubNav` + `ag-subnav*` styles on `/automation`, `/automation/[id]`, `/automation/drafts`, draft detail; sequences toolbar lede links to Drafts; redundant toolbar buttons removed.
- **before [automation + multi-touch epic]:** Ship plan phases A–G: honest automation copy (M2), multi-touch plan days, Close step → canvas graph, Supabase `automation_drafts` + AI interpret + approval, Close sequence create/update + publish action, subscription run-watch polling, tests + `close-llms.md` updates.
- **after [automation + multi-touch epic]:** Phases A–G landed: `heartbeat-summary` (client-safe copy), `appendRequiredActionToPlanDay` + Add touch UI, `close-workflow-graph` + detail graph, Supabase migration + drafts routes + `sequence-ai` + publish action, `SubscriptionRunWatch`, Vitest mapper tests, `close-llms` wiring table updated. `npm run typecheck` + `npm test` + `npm run build` clean.
- **inception:** Stand up the three-ledger system (Goals / Problems / Global) and lock the tap rule into CLAUDE.md before any build work begins.
- **next:** Awaiting Jake's brief on the "incredible new functionality" that triggered the scheduled-task wipe — that will define the first real build goal.
- **after inception:** Three ledgers live, CLAUDE.md anchored, tap rule active. Ready for first real move.
- **milestone defined:** Build a new Automation Web Application on the OpenAI Responses + remote MCP architecture (GitHub + Supabase). Replaces the old CC Agent app. Reuses Aesthetic Asset Kit so it reads as an evolution to the client, not a rewrite.
- **before [foundation alignment]:** Lock stack, project location, and credential posture with Jake before any code lands. Foundation = Settings page + Test page, wired to OpenAI Responses (not chat completions) with GitHub MCP + Supabase MCP attached as tools.
- **before [finalize alignment]:** Process Jake's answers (OpenAI key in hand, Supabase MCP IS hosted HTTP at mcp.supabase.com), lock remaining defaults (stack + GitHub MCP), set up tasks, hand back the green-light prompt.
- **before [scaffold]:** Lay down the Next.js 14 + TS skeleton at `/Users/jakeaaron/ComeketoAgent/app/` with `src/app/` for routes. Settings page, Test page, `/api/test` route firing OpenAI Responses with both MCPs attached. Pull credentials from the .env Jake uploaded. Run `npm install` from sandbox to verify the dep tree resolves.
- **after [scaffold]:** Foundation is up. 12 files written across `app/`, `app/src/app/`, `app/src/app/settings|test|api/test/`, `app/src/lib/`. `npm install` clean (75 packages). `tsc --noEmit` passes with zero errors. Settings page reads env, Test page POSTs to `/api/test`, route fires OpenAI Responses with optional GitHub + Supabase MCP tools attached. Ready for Jake to `npm run dev` and verify in browser.
- **next:** First real Test-page firing — discover the actual MCP auth shape (Supabase secret key as bearer? GitHub PAT as bearer? OAuth bridge needed?). Update Settings page to allow editing once auth shape is known.
- **before [pivot to direct APIs]:** Drop MCP from the app's outbound path. Supabase via JS SDK, GitHub via Octokit (PAT auth), OpenAI Responses with no tools attached. Test page becomes three independent connection tests. MCP env vars stay in `.env.local` as a side door for agent-side prototyping later.
- **after [pivot]:** Done. `octokit@^4` installed (34 new packages), `src/lib/github.ts` + `src/lib/supabase.ts` factories added, `/api/test` route rewritten with three modes (`openai`/`supabase`/`github`), Test page refactored to three buttons + three result panes, Settings page split into Primary + Optional groups. `tsc --noEmit` passes. Once Jake drops a `GITHUB_PAT` in `.env.local`, all three buttons should go green.
- **before [strip MCP]:** Jake dropped the GitHub PAT in `.env.local` and asked for MCP cruft removed entirely (no side-door, no "future" labels). Strip MCP env vars from `.env.local`, `src/lib/env.ts`, and the Settings page. Verify tsc clean. Update memory.
- **after [strip MCP]:** Done. Four MCP env vars deleted from `.env.local`, `src/lib/env.ts` env+envStatus, and the Settings page. Settings now has Primary (OpenAI / Supabase / GitHub PAT) and Reserved (Close / ClickUp) — that's it. tsc clean. Repo grep confirms zero MCP refs remaining. Architecture memory updated to reflect no-side-door rule.
- **before [sync env]:** Jake added the GitHub PAT and switched Supabase URL + publishable key to `NEXT_PUBLIC_*` in his uploaded `.env` — but `app/.env.local` is the file the dev server actually reads, and it's still showing the old values. Sync `.env.local` to match the uploaded `.env`, update `src/lib/env.ts` to read `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. Going forward, Jake should edit `app/.env.local` directly so Next.js picks up changes on save.
- **after [sync env]:** Done. `app/.env.local` carries all 10 vars (OpenAI, Supabase URL + publishable + secret, GitHub PAT, Close × 2, ClickUp × 3). `src/lib/env.ts` reads them under their plain names. tsc clean. Convention locked: `app/.env.local` is the only env file the dev server reads.
- **before [hero page]:** Build the entry-point hero page matching Jake's screenshot. "Compose the week. Then approve it." headline, sign-in card with Google button (non-functional placeholder per Jake's call), 24h dial graphic on the right, stats row at the bottom. Use Jake's new template at `/Users/jakeaaron/ComeketoAgent/Chat page/` as the visual reference, blend with old-app Aesthetic Kit only where gaps remain. Make `/` the hero; `/settings` + `/test` stay accessible by direct URL.
- **after [hero page]:** Done. Design system (Fraunces / Inter / JetBrains Mono, paper/ink palette, soft pastel accents) ported to `globals.css` from Jake's `shared.css` + `hero.css`. Hero page renders the split layout with section label, two-line headline (regular + italic), lede, sign-in card, daily-clock SVG motif on the right, and the 218 / 14k / 99.7% / 12 counter strip. Google button is a visual stub. Settings + Test re-skinned with new tokens, each gets a wordmark header linking back to /. tsc clean.
- **before [hero hotfix]:** Hero page throws a Server Component serialization error at runtime — GoogleBtn has an onClick that crosses the server/client boundary. Remove the onClick. Verify with `next build`, not just tsc.
- **after [hero hotfix]:** GoogleBtn onClick removed (button is visual-only anyway). Repo grep confirms zero event handlers remain in any Server Component. Comment left in page.tsx documenting the rule for whoever wires auth: extract GoogleBtn to a "use client" file or use a `<form action={serverAction}>`.
- **before [layout rules + hero tighten]:** Lock three project-wide design rules: (1) every page fits the viewport — no page scroll; (2) when widgets need to scroll their content, scrollbars are hidden; (3) widgets carry visible dimensionality — thin border + subtle shadow + cream card on a pastel-swim-lane background, modeled on Jake's four-box identity image. Demonstrate the rules by tightening the hero so the headline + sign-in card + clock + counter strip all fit in 100vh.
- **after [layout rules + hero tighten]:** Done. `globals.css` now defines `--lane-lavender/-blue/-sage/-peach`, `--shadow-sm/-md/-lg`, `.scroll-hide` utility, `.widget` pattern (cream + border + shadow), and `.lane`/`.lane-*` classes for the swim-lane backgrounds. `body` is locked to 100vh + overflow hidden; `cme-shell` is the flex-column 100vh frame. Hero is tightened: smaller paddings, motif capped at 380px, counter-strip numbers from 40px → 34px, gaps reduced, hero-main has internal overflow-y: auto + hidden scrollbar as a safety net. Auth card now picks up `var(--shadow-sm)` for dimensionality. Settings + Test pages pick up the new admin-main scroll-hidden frame so they fit too. Rules saved to memory at `feedback_layout_rules.md`.
- **before [chat page]:** Absorb Jake's `comeketo_agent_chat_dots.html` as a new `/chat` route. Header with the four colored title dots + utility nav, tab strip (people/activity/intake/analytics/boxes/automation/delegations) with notification dots, two-panel grid (210px Today rail + flex chat panel), conversation surface with user bubble / lavender-ribbon thought block / sage-ribbon automation card / amber-ribbon needs-your-call / input bar, footer breadcrumb. Make it fit 100vh inside cme-shell — drop the fixed 720px height. Static content only, no interactivity.
- **after [chat page]:** Done. `/chat` route lives at `src/app/chat/page.tsx`. Chat-specific classes (`.cmk-panel`, `.cmk-rail-item`, `.cmk-nav`, `.cmk-pill`, `.cmk-dot/-sm`, `.cmk-eyebrow`, `.chat-msg-user/-tool/-text/-card/-call`, `.chat-input-bar/-add/-send`) added to `globals.css`. Two `.cmk-panel` columns sit on the cream chat-shell bg, both with internal `cmk-scroll` (hidden scrollbars) so only the message column scrolls. All four message types render with their colored ribbon (lavender thought, sage automation card, amber needs-your-call, plain user bubble + plain text). All nav and rail items are inert (no Server Component event handlers). tsc clean.
- **before [unify with chat]:** Make the chat page the canonical reference for everything. Three changes: (1) Lighten the paper bg from honey `#EFEBE3` → cooler `#F5F3EE` matching chat. (2) Kill mono font from editorial elements site-wide (eyebrows, section labels, fine-print, stat labels, breadcrumbs) — Jake explicitly wants no typewriter font. Keep mono only where it's genuinely code/data display (env fingerprints, var names). (3) Smaller fonts overall to match chat sizing. Plus: add the four colored title dots before the wordmark on every page, and wire the hero wordmark to navigate to `/chat`.
- **after [unify with chat]:** Done. Token palette repointed to chat (paper `#F5F3EE`, card `#FCFBF8`, ink `#1A1A1A`, rules as `rgba(0,0,0,0.05/0.09)`). `cme-section-label` + `cme-eyebrow` rewritten to sans-serif small caps (10px / 0.14em / weight 500) — no more mono on editorial elements. Hero shrunk: title 38→34, lede 15→13.5, auth-h 26→22, auth-lede 13.5→12.5, counter-n 34→30, wordmark 22→19. New `.cme-identity-dots` + `.cme-dot.brown/.gold/.sage/.lavender` classes carrying the four-dot identity. Wordmark + dots now lead every page header (hero, chat, settings, test). Hero wordmark links to `/chat`; admin pages get a real utility nav (`chat / settings / test`). tsc clean.
- **before [functional foundation + intake]:** Five things in one tight move: (1) Settings gets a real model picker — three GPT-5.4 options (nano/mini/full at the dated revisions Jake supplied), persisted to `.cmk-settings.json`, written via Server Action. (2) New `/api/chat` endpoint using OpenAI **Responses** API (NOT chat completions), reads model from settings. (3) Chat panel extracted as a `"use client"` component with real input wired to `/api/chat` — demo turns visible until first user message, then real conversation takes over. (4) `/api/test` updated to read model from settings so the picker actually drives behavior. (5) Port `comeketo_intake_report_panels.html` to `/intake` route with shared `AppHeader` + `TabNav` components (also DRY-ing chat's header). Triple-A polish, tightest possible.
- **after [functional foundation + intake]:** Done. Files: `src/lib/settings.ts` (file-backed JSON, AVAILABLE_MODELS), `src/app/settings/actions.ts` (`updateModelAction` Server Action), `src/components/{icons,AppHeader,TabNav}.tsx` (shared header + tab nav extracted), `src/app/chat/ChatPanel.tsx` (use-client interactive panel with auto-scroll), `src/app/api/chat/route.ts` (Responses API with system instructions), `src/app/intake/page.tsx` (full visual port). Updated: `/api/test` reads `settings.model`, `chat/page.tsx` is now a thin Server shell that mounts AppHeader + TabNav + static rail + ChatPanel, settings page renders the model `<form action={updateModelAction}>`, `.gitignore` covers `.cmk-settings.json`. Routes now: `/`, `/chat`, `/intake`, `/settings`, `/test`. tsc clean.
- **before [intake rename + research]:** Two tasks: (1) replace every "Rodbot" string in `/intake` with "Comeketo Agent" — pure terminology cleanup. (2) research the original intake concept in `/Users/jakeaaron/Downloads/CC Agent/` since Jake says the current visual is confusing and the old app has the canonical intake idea. Report findings, propose how to align the page; do not restructure without Jake's approval.
- **after [intake rename + research]:** All "Rodbot" → "Comeketo Agent" in `/intake/page.tsx`. Found the original intake concept at `CC Agent/CCAgentindex/intake_reports/*.json` — three real example reports (`test.json`, `check_thsi.json`, `soliton_collision_theory.json`). Each report = `{ slug, name, description, documents[], conversation[], metadata }`. Documents carry `raw_text` (OCR), `summary`, structured extraction (`title`, `theme`, `sections`, `metrics`, `equations`, `concepts`), classification (`kind`, `confidence`), and `ocr_route`. Conversation is a Q&A array against the doc set. Intake is a "drop docs → structured report → ask questions" loop, organized as named reports you can navigate between. The `← reports` button in the polished HTML implies a list view that doesn't exist yet. Concept is solid; the polished HTML covers ~80% of it. Proposing alignment in chat, no code refactor without Jake's call.
- **before [chat depth — threads + vision + markdown]:** Ship the chat as the real surface. Five things in one move: (1) Supabase migration creating `threads` + `messages` tables. (2) `lib/threads.ts` server helpers + `/api/threads` (GET list, POST new) + `/api/threads/[id]/messages` (GET history, POST persist). (3) `/api/chat` updated to accept `thread_id` and image attachments — builds OpenAI Responses input as multi-turn array with mixed text+image content. (4) Refactor: lift left rail into the client component so "+ NEW" creates a thread, thread list loads from Supabase, active thread switching loads its messages. (5) `react-markdown` renders assistant text, file-picker button + base64 image preview in the input bar. tsc clean.
- **after [chat depth]:** Done. Migration `create_chat_threads_and_messages` applied to project `vppjxfhhmmdxlhiufuwc` (threads + messages tables, indexes, RLS enabled with service_role policies). New code: `lib/threads.ts`, `/api/threads/route.ts` (GET, POST), `/api/threads/[id]/messages/route.ts` (GET). `/api/chat/route.ts` rewritten — accepts `{ thread_id?, input, attachments? }`, persists user → builds full-history Responses input → persists assistant → bumps thread updated_at and derives title for new threads. ChatLayout client component replaces the static rail+panel pair: loads threads on mount, "+ new" resets to fresh-thread mode, clicking a thread loads its messages, file-picker button stages image attachments with thumbnail previews and an `×` to remove, send wires multi-modal input, react-markdown renders assistant text with full styling (headings, code, lists, tables, blockquotes, links, hr). 99 new npm packages (`react-markdown` + `remark-gfm` + transitive). tsc clean.
- **before [add vercel cli]:** Install the Vercel CLI globally and link `app/` to a Vercel project so we can use `vercel dev`, `vercel env pull`, and per-branch preview deploys. Lightweight platform on-ramp — no commitment to Vercel-specific primitives yet.
- **after [add vercel cli]:** Done. Vercel CLI v44.7.3 already installed at `/opt/homebrew/bin/vercel`, logged in as `expressedai`. `app/` is now linked to `expressedais-projects/comeketo-agent` (auto-detected Next.js). `.vercel/project.json` written, `.vercel/` added to `.gitignore` automatically. No `vercel.json` was needed. Available now from `app/`: `vercel dev`, `vercel env pull .env.local` (will overwrite — coordinate before running), `vercel` for preview deploy, `vercel --prod` for prod.
- **after [push to RodbotCC]:** Repo is live at `https://github.com/RodbotCC/ComeketoAgent` on `main`. Two commits: initial foundation (59 files: app skeleton + Chat page template + ledgers) and a cleanup commit removing a stale nested `ComeketoAgent/` folder from an earlier botched push. Root `.gitignore` written covering `.env*`, `.logs/`, `node_modules/`, `.next/`, `.vercel/`, `.cmk-settings.json`. No secrets pushed (verified before commit). Local repo initialized at working-dir root with `RodbotCC` user / `tech@comeketocatering.com`.
- **before [vercel↔github finish]:** Git push is done; Vercel CLI still 400s on `vercel git connect`. Means the Vercel team `expressedais-projects` doesn't yet trust the RodbotCC GitHub install. Jake to finish in the Vercel dashboard: settings/git → Connect → add RodbotCC if missing → pick the repo → set Root Directory to `app`.
- **after [flatten + re-nest]:** Done. Repo root is now a clean Next.js project (src/, package.json, tsconfig.json, next.config.mjs at top). Scaffolding (3 ledgers + chat-template) tucked into `_scaffold/`. CLAUDE.md stays at root with updated ledger pointers. tsc clean. Pushed as commit `f1d8b57`. Vercel can now import with Root Directory = `./` (default).
- **MILESTONE LOCKED [2026-05-01]: Real Close-powered automation product.** Direct Close REST API integration in our app — no ChatGPT/MCP middleman. ChatGPT 5.5 plugs in only as the LLM via OpenAI Responses API (via our own backend). The product: (1) chat agent with Close tool-calls so it can read leads / list workflows / enroll leads / create opps / log activities by talking to it in plain English; (2) `/automation` page with a visual flow editor (clone-and-modify the existing automation page from `/Users/jakeaaron/Downloads/CC Agent`) that reads + edits + saves Close workflows; (3) plain-English → workflow JSON the agent emits, previewed in the editor, user approves, POSTed to Close; (4) `/api/webhooks/close` + Supabase Realtime → live activity feed showing steps as they fire. Approach: add capabilities incrementally, scrape API docs once for grounding, mine CC Agent for UI patterns, build only what's needed at each step.
- **before [close docs scrape + cc agent recon]:** Two parallel moves: (1) firecrawl-crawl Close's API docs to local markdown for the agent's grounding; (2) explore `/Users/jakeaaron/Downloads/CC Agent` to find the automation page UI Jake wants to clone, plus understand the data shape he's bringing. Don't write app code yet — recon first.
- **after [close docs scrape + cc agent recon]:** (1) Skipped full crawl in favor of `_reference/close-llms.md` — Close publishes a curated 365-line llms.txt indexing every API resource. We lazy-fetch specific resource docs as we build each tool. (2) CC Agent recon found the gold: `automation.jsx` (6878 lines) is a hand-rolled SVG visual editor with 5 node shapes (capsule/diamond/hexagon/rounded/stacked), drag-to-move + drag-to-connect + undo/redo + JSON export. Workflow schema is `comeketo.automation_graph.v1` (nodes[role,kind,label,x/y,config,notes,description] + connections[src,dst,kind,label,condition,annotations,narration] + metadata). Schema adopted as-is. Editor port plan: `automation.jsx` → `src/app/automation/*` as `"use client"`, swap globals for imports, replace `window.SecretaryAI` with `/api/chat`, replace `/api/workflows/save` with Supabase. Library extended with Close-flavored kinds when we wire Close.
- **after [lib/close.ts + chat tool wiring]:** Done. (1) `src/lib/close.ts` — server-only Close client with HTTP Basic auth, 6 endpoints: `closeListWorkflows`, `closeSearchLeads`, `closeGetLead`, `closeEnrollInWorkflow`, `closeListEmailTemplates`, `closeCreateOpportunity`. Verified live against api.close.com (returned the user's actual workflow names). (2) `/test` page got a 4th button "Close" — tests by listing workflows + email templates from the live org. (3) `src/lib/close-tools.ts` — defines `CLOSE_TOOLS` (6 tool schemas in OpenAI Responses format) and `dispatchCloseTool` (executes a tool call by name + args). (4) `/api/chat/route.ts` rewritten with a tool-use loop: passes `tools: CLOSE_TOOLS` to Responses, loops up to 6 rounds executing tool calls and feeding `function_call_output` back, prepends `_used: tool_a, tool_b_` markdown trace line to the assistant message. System instructions updated to teach the agent to prefer live data and confirm before write actions. tsc clean, next build clean. Jake can now: (a) click Close on /test to see workflow names; (b) type "list my Close workflows" in chat and watch the agent call the API; (c) ask the agent to enroll a lead, which will trigger close_enroll_in_workflow after confirmation.
- **before [/automation page first cut]:** Port CC Agent's automation.jsx visual editor to a Next.js `/automation` route. First cut is read-only: render the morning_sweep demo workflow with all 5 node shapes + orthogonal-routed edges, click-to-select shows an inspector. Skip drag-to-move, drag-to-connect, library, persistence — those are next rounds. Re-add `automation` to TabNav since it now points at a real route.
- **after [/automation page first cut]:** Done. (1) `src/app/automation/AutomationCanvas.tsx` ("use client", ~280 lines) — types (Workflow / WorkflowNode / WorkflowEdge / NodeRole / EdgeKind), NODE_DIMS, KIND_GLYPHS, orthogonalPath geometry, NodeShape (5 shape SVGs: capsule/diamond/hexagon/rounded/stacked), SelectionRing, GraphNode (clickable, with glyph + truncated label + endpoint dots), ConnectionPath (kind-styled paths with optional label pill + conditional diamond). AutomationCanvas computes viewBox from node positions to auto-fit; click-to-select + side inspector that shows role/kind, description, config (JSON), notes. (2) `src/app/automation/page.tsx` — server shell with AppHeader + TabNav + toolbar (workflow name + node/edge count) + canvas + footer. DEMO_WORKFLOW seeded inline (matches morning_sweep_grid.workflow.json schema). (3) globals.css gets ~170 lines of automation styles: `--ag-actor/-trigger/-transform/-sink/-state` role colors mapped to existing palette (sage/lavender/peach/lemon/rose-deep), `.ag-main`, `.ag-toolbar`, `.ag-canvas-frame` (cream card with shadow), `.ag-shape` per-role tinted fills, `.ag-link[data-kind=*]` per-kind edge styling (data/trigger/reference/conditional/observe/invalid), `.ag-edge-label` mid-edge label pill, `.ag-inspector` slide-over panel. (4) TabNav gets `automation` back (between intake and delegations) — not inert anymore; routes to /automation. tsc clean, next build clean (route weighs 2.71 kB). Watch items: (a) read-only — drag-to-move and drag-to-connect are explicit next round; (b) DEMO_WORKFLOW is hardcoded — Supabase persistence and chat-emitted workflow loading come after; (c) graph is abstract — Close-flavored kinds (close_enroll, close_create_opp, etc.) get added to LIBRARY when we wire the visual editor's create flow.
- **before [Guardrails pin + Lead Box foundation]:** Big multi-part move per Guardrails adoption: (1) Pin Guardrails.md as North Star in CLAUDE.md. (2) Update lib/env.ts to read CLOSE_USER_ID_JAKE + CLOSE_USER_ID_ANDRE (Jake renamed the env vars). (3) Extend lib/close.ts with closeListActivities, closeListSequenceSubscriptions, closeGetLeadFull (parallel fan-out for the Box), closeListLeads, plus checkOwnershipAndStatus gate function + SkipCode enum. (4) Update lib/close-tools.ts: add close_get_lead_full tool; gate close_enroll_in_workflow + close_create_opportunity with the ownership/status check that returns a skip_code instead of executing when violated (Guardrails §C1+C2+M4). (5) New /leads page (server, lists Close leads with owner badges + click-through). (6) New /lead/[id] page (server, renders the Lead Box: profile, contacts, workflow enrollments, opportunities, custom fields, activity feed with kind/direction/timestamps, gate badge). (7) TabNav gets `leads` as the first tab (uses boxes icon).
- **after [Guardrails pin + Lead Box foundation]:** Done. tsc clean, next build clean (1 phantom app/ regenerated mid-build, killed). Files added: `src/app/leads/page.tsx`, `src/app/lead/[id]/page.tsx`. Files modified: `CLAUDE.md` (Guardrails pinned), `src/lib/env.ts` (CLOSE_USER_ID_JAKE/ANDRE), `src/lib/close.ts` (+5 functions, +SkipCode enum, +checkOwnershipAndStatus gate, expanded CloseLead type), `src/lib/close-tools.ts` (close_get_lead_full added, write actions gated), `src/components/TabNav.tsx` (+leads tab). globals.css gets ~250 lines of leads-index + lead-box styles: leads table grid, owner badges (andre=sage, jake=lavender), lead-toolbar, lead-gate badge (ok/warn/block), lead-grid two-col, lead-card widget, contacts/workflow-enrollments/opps/custom-fields cards, activity feed (timestamp + direction arrow + kind tag + preview line). Routes now: /, /chat, /intake, /automation, /leads, /lead/[id], /settings, /test. Hard gates LIVE in chat tools — agent can no longer enroll a non-Andre or Won/Lost lead, returns explicit skip_code instead. Watch items: (a) Box is read-only / no plan generation yet; (b) gate only checks ownership+status — STOP_SIGNAL, REPLY_GATE, SEND_WINDOW, FREQUENCY_CAP, STALE_BOX gates are not implemented; (c) heartbeat cron not built; (d) /leads currently lists ALL leads not just Andre's — needs an ownership filter toggle; (e) closeListLeads returns 25 by default — no pagination UI yet.
- **before [Andre filter + 7-day plan generator]:** Two moves: (1) /leads gets an Andre/All filter toggle (default Andre per Guardrails §C1). (2) New `lib/plan.ts` + `generate_seven_day_plan` chat tool — pulls full Box, calls OpenAI Responses with NEPQ-voice + Guardrails-§D system prompt, parses JSON output into `SevenDayPlan` schema (Guardrails §D4 shape). Tool is ownership-gated. No persistence yet — agent emits the plan inline; UI rendering on /lead/[id] is the next round.
- **after [Andre filter + 7-day plan generator]:** Done. tsc clean, next build clean. (1) `/leads?owner=andre|all` filter wired — default Andre per §C1. UI is two pills with counts ("Andre 12" / "All 47") — clicking each updates the URL via `<Link>`. CSS adds `.leads-filter` segmented control + count badges. (2) `src/lib/plan.ts` (new, ~270 lines): types (SevenDayPlan, SevenDayPlanDay, PlannedTouchpoint, StopCondition, PlanGoal/PlanStatus/ApprovalStatus/PlanChannel), `snapshotIdForBox()` (deterministic hash of lead update + activity count + last activity + sub statuses — for §E3 staleness detection), `summarizeBoxForPrompt()` (compact box→prompt formatter), `activityCompactLine()` (one-line per activity with type+direction+key fields), `SYSTEM_PROMPT` (NEPQ voice rules, tasting-date allowlist, send windows, frequency caps, hard rules per §D, JSON-only output spec), `generateSevenDayPlanForLead()` (fetch box → OpenAI Responses → defensive JSON parse → fill plan_id + cycle_started_at + snapshot_id + force approval_required:true + days[].approval_status:'needs_review'). (3) `close-tools.ts`: new tool `generate_seven_day_plan` — ownership-gated like the writes (returns skip_code if not Andre's lead even though it's read-only — Guardrails §D plans are only for Andre's leads). System prompt updated to teach the chat agent when to use it. Watch items: (a) plan is emitted but not persisted — Supabase table comes next; (b) plan rendering on /lead/[id] not yet built — agent shows it inline in chat for now; (c) only ownership+status gate applied — STOP_SIGNAL detection from activity feed, frequency cap check, send window check still TODO; (d) snapshotIdForBox uses a small hash not crypto — fine for dev, swap if collisions matter; (e) plan generation is one-shot OpenAI call — no validation pass / no auto-retry on bad JSON yet.
- **before [plan persistence + Box page rendering]:** Three moves: (1) Supabase `lead_plans` table (RLS, indexes by lead+generated_at desc and by active statuses); (2) `lib/plans-db.ts` server CRUD (savePlan, getLatestPlanForLead, listPlansForLead, approvePlan, killPlan, pausePlan); (3) Plan section on /lead/[id] with Server Actions (generate / approve / pause / kill / regenerate) and stale-snapshot badge.
- **after [plan persistence + Box page rendering]:** Done. tsc clean, next build clean. (1) Supabase migration `create_lead_plans` applied to project vppjxfhhmmdxlhiufuwc — `lead_plans` table with text PK, status check constraint, primary_goal check constraint, jsonb fields for known_facts/unknowns/days/stop_conditions, approved_at/approved_by/killed_at/killed_reason audit columns, RLS on with service_role full access, two indexes (lead_id+generated_at desc, status partial). (2) `src/lib/plans-db.ts` — typed Row→SevenDayPlan round-trip with savePlan/getLatestPlanForLead/listPlansForLead/approvePlan/killPlan/pausePlan. (3) `lib/plan.ts` updated: generateSevenDayPlanForLead now persists via savePlan; surfaces saveError if persistence fails (plan still returned). (4) `src/app/lead/[id]/actions.ts` — Server Actions: generatePlanAction, approvePlanAction, killPlanAction, pausePlanAction. Each revalidates `/lead/{id}` after mutation. (5) `src/app/lead/[id]/PlanSection.tsx` — empty state ("Generate plan" button); loaded state shows status pill (draft/approved/active/paused/killed/completed colored), stale badge if snapshot mismatch, goal serif headline, lead_state paragraph, known/unknowns two-col, "best next question" lavender ribbon callout, 7 day cards with day-status pill + objective + per-action rows (channel glyph + intent + draft_seed in serif italic + tasting_date + notes) + send_window footer, stop conditions in a rose-tinted block, action row (Approve/Pause/Regenerate/Kill — gated by current status), snapshot ID footer for traceability. (6) `/lead/[id]/page.tsx` loads latest plan from DB in parallel with the Box, computes currentSnapshotId, only renders PlanSection when planEligible (passes the ownership+status gate). globals.css gets ~210 lines of plan styling (.plan-card, .plan-status-* per-status colors, .plan-stale-badge, .plan-question lavender ribbon, .plan-day cards with auto-fit grid, .plan-action rows with channel column, .plan-btn primary/danger variants, snapshot trace footer). Watch items: (a) only ownership+status gates — STOP_SIGNAL detection still pending; (b) approval_by hardcoded to "andre" — needs auth context when real auth lands; (c) "approved" status doesn't yet do anything operationally — heartbeat that consumes approved plans is the next big move; (d) regenerate creates a NEW plan row — old one stays in history (good for audit, but list view to browse history isn't built); (e) plan generation latency is ~5-15s on first call (full Box fetch + OpenAI Responses) — no streaming UI yet, button just sits.
- **after [Box polish round 1]:** Done. tsc clean, next build clean. Five quality moves per Jake's feedback: (1) **Seven distinct day colors** — added `--lane-lemon`, `--lane-rose`, `--lane-sky` to globals.css (sibling pastels to the existing identity four). PlanSection cycles through `["lavender","sky","sage","lemon","peach","rose","blue"]` via `data-tone` attribute on each `.plan-day`. CSS paints just the `.plan-day-head` strip in the pastel — body stays cream — so the 7 cards form a calm rainbow without overwhelming the read. (2) **Killed code-look font everywhere on the Box and Plan** — removed `font-family: var(--mono)` from .lead-feed-when, .lead-feed-kind, .lead-feed-count, .lead-cf-key, .lead-route, .lead-sub-status, .lead-gate, .lead-footer-meta, .lead-error, .plan-status, the old .plan-stale-badge, .plan-day-status, .plan-action-channel, .plan-action-tasting, .plan-day-window, .plan-snapshot, .cmk-eyebrow, .leads-filter-count. Replaced with sans + small caps + tabular nums where appropriate. Mono retained only in true code/data spots (markdown <code>/<pre>, ag-inspector-config JSON dump, admin pages). (3) **Snapshot trace condensed** — removed the bottom snapshot-id footer line; replaced with a compact "In sync" / "Stale" pill at the top-right of the plan card with the full snapshot diff in a `title=` tooltip. (4) **Lead footer no longer overflows** — removed the long `lead_id` from the visible footer (kept only as `title=` tooltip on the row), reformatted as flex with sep dots, fixes the overlap into the ACTIVITY column. (5) **Channel chip overhaul** — killed the fixed-width 50px gutter; replaced `.plan-action-channel` with a compact pill `.plan-action-chip` that sits inline with the intent and is color-keyed per channel (call=lavender, email=sky, sms=sage, task=peach). Day cards now have proper internal structure (head strip / objective row / action rows / send-window footer) instead of one flat box. Status pills moved to fully rounded (border-radius:999px) for a cleaner look across the page. Watch items / next round: (a) right-click context menus on panels (Jake's ask); (b) click-to-expand modal with AI re-prompt for any panel (centerpiece UX); (c) plan→Close-codegen — translate approved plan into Close sequence_subscription + scheduled task API calls; (d) /automation page should be able to load + render an existing plan as a graph (cross-page consistency).
- **before [click-to-expand modal + per-day AI refine]:** New universal modal primitive (`Modal.tsx` — backdrop scrim + centered card + ESC + click-outside + body-scroll lock + focus trap entry). Each plan day card becomes clickable; click opens a modal with the expanded day plus a textarea for an AI re-prompt instruction ("make this more aggressive about scheduling a call" / "switch to email-first" / "pivot to tasting"). New Server Action `refinePlanDayAction` calls `refinePlanDay` LLM helper — gets the revised day JSON, persists via `updatePlanDay`, revalidates page. Modal closes on success, shows error inline on failure.
- **after [click-to-expand modal + per-day AI refine]:** Done. tsc clean, next build clean. Files added: `src/app/lead/[id]/Modal.tsx` (~50 lines, reusable backdrop+card with ESC/click-outside/body-lock/focus), `src/app/lead/[id]/PlanDayCard.tsx` (~140 lines, client component with useState+useTransition for the modal + form). Files updated: `lib/plan.ts` (+`refinePlanDay()` + `RefineDayResult` type + REFINE_DAY_SYSTEM prompt with NEPQ rules + tasting allowlist + send-window rules), `lib/plans-db.ts` (+`updatePlanDay(planId, dayIndex, newDay)` + `getPlanById(planId)`), `app/lead/[id]/actions.ts` (+`refinePlanDayAction` Server Action returning `RefineDayState` for `useTransition` flow), `app/lead/[id]/PlanSection.tsx` (uses `<PlanDayCard />` instead of inline JSX). globals.css gets ~145 lines of modal styles: `.cme-modal-backdrop` (semi-opaque ink scrim + 2px backdrop-blur, 140ms fade-in), `.cme-modal-card` (12px radius, layered shadow, max-w 720px, animated rise on open with cubic-bezier), `.cme-modal-close` top-right ×, `.plan-day-clickable` hover lift + lavender focus ring, `.plan-day-modal-head` per-tone color strip matching the parent card, `.plan-day-modal-title` 22px serif, `.plan-day-modal-refine` paper-2 panel containing the AI re-prompt textarea + Cancel/Take-another-crack buttons + inline error surface. Modal is fully accessible (role=dialog, aria-modal, aria-labelledby, ESC, focus on first focusable). Watch items: (a) only PlanDayCard wired to modal — Box panels (Contacts, Workflow, Opportunities, Custom Fields, Activity) don't have modals yet; (b) right-click context menus still pending; (c) "Approve" on the day status doesn't update individual day status yet — just the whole plan via the section's Approve button; (d) the refine flow regenerates one day; whole-plan refine (re-prompt the entire plan) isn't surfaced; (e) modal scroll inside long content uses default scrollbar — could hide for cleaner look.
- **after [whole-plan refine + right-click context menus + activity expand]:** Big sprint. tsc clean, next build clean (lead/[id] at 4.62 kB now). Three features wired: **(1) Whole-plan AI refine** — `lib/plan.ts` gains `refineWholePlan()` + `REFINE_PLAN_SYSTEM` system prompt; `actions.ts` adds `refineWholePlanAction` (hydrates plan, calls LLM, replaces all 7 days + summary fields in one DB write, resets status to draft so it gets re-approved); new `PlanCardClient.tsx` wraps the plan card chrome and ships a "Refine plan…" button + modal with NEPQ-aware textarea ("make whole week more aggressive about scheduling a call" / "pivot from email-first to SMS-first" / "cut Day 7 — they've gone cold"). Button positioned absolute top-right of plan card; snap-badge gets `padding-right: 100px` to clear it. **(2) Right-click context menus** — new `ContextMenu.tsx` primitive (cursor-positioned popover with viewport-clamping, ESC/outside-click/scroll/resize close, label + divider + item types, danger tone for destructive actions). PlanCardClient applies a menu with [Refine whole plan, Regenerate, Approve, Pause, Kill]. PlanDayCard applies a per-day menu with [Edit & refine, Mark approved, Mark needs review, Mark sent, Skip]. New `setDayStatus()` in `plans-db.ts` + `setDayStatusAction` Server Action persist per-day status changes. **(3) Activity expand modal** — new `ActivityFeed.tsx` ("use client") wraps the activity list; clicking any item opens the universal modal with full body (email subject + text/html, SMS text, call duration + note + transcript, Note body, Task body, Meeting title). page.tsx feeds activities to `<ActivityFeed />` instead of inline rendering. globals.css gains: `.cme-cm` context-menu popover (z-index 200, layered shadow, 90ms pop animation), `.cme-cm-item` rows with hover + danger variant, `.cme-cm-divider`, `.cme-cm-label` eyebrow, `.plan-refine-btn` pill (lavender hover), `.lead-feed-item-clickable` (hover background + focus ring), `.activity-body-pre` (sans, paper-2 bg, max-h 420 with scroll). Watch items: (a) Box panels Contacts / Workflow / Opportunities / Custom Fields still don't have modals or right-click menus — could apply ContextMenu to them next; (b) day-status changes via right-click bypass the day modal — fast path is good but no visual confirmation that the change happened (just a re-render); (c) refine pending state on whole-plan modal is "AI is rewriting all 7 days…" but takes 15-30s with no progress feedback; (d) activity transcript rendering is plain pre — could prettify call/SMS/email differently; (e) the inline `activityLine` and `sortedActivities` in page.tsx are now duplicated in ActivityFeed — minor cleanup deferred.
- **after [Box panel modals + Plan→Close codegen]:** Big sprint. tsc clean, next build clean (lead/[id] now 5.6 kB). **(1) Plan→Close codegen** — new `src/lib/plan-to-close.ts` translator with `codegenPlanForClose({plan, box, andreUserId})` returning typed `PlannedCloseAction[]` grouped by day. Discriminated union over create_task / log_activity / enroll_in_workflow / skip — emits intent only, no side effects (Guardrails §I4). Day index → ISO date computed from `cycle_started_at`. call/task channels → create_task assigned to Andre at lead-local send window. email/sms channels → log_activity (heartbeat may upgrade to enroll_in_workflow when a matching template sequence is found). Skipped days emit a single skip action so preview shows them. Missing primary contact / missing andre user_id surface as `blocking_warnings` (soft warning, doesn't fail codegen). New Server Action `getCloseCodegenPreview(planId)` hydrates plan + Box and runs translator. New client component `CloseActionsPreview.tsx` — "Preview Close actions" button in the plan-actions row → modal opens, shows: total action count + per-group warnings, then per-day cards with date, objective, send window, and each action with its colored channel chip + kind label (CREATE TASK / SEND & LOG / ENROLL / SKIP) + meta (due/send-after) + intent + draft seed in serif italic. **(2) Box panel modals + right-click menus** — new `BoxPanel.tsx` reusable wrapper that takes summary + expanded slots; renders the summary in a `.lead-card` with click-to-expand modal (paper-2 head, full data in body) and right-click context menu. page.tsx wraps Contacts / Workflow Enrollments / Opportunities / Custom Fields each in a `<BoxPanel>`. Summaries truncate to 2-4 items with "+ N more" affordance; expanded modals show everything (full contact list with all routes, full enrollments with paused reasons + updated dates, opportunities with confidence/value-period, all custom fields with full IDs). Contacts panel ships an "Open in Close" right-click menu item. globals.css gains: `.box-panel` (cursor:pointer, hover lift via shadow-md, focus-visible lavender ring), `.codegen-summary` (paper-2 callout with serif total + bullet warnings list), `.codegen-group` (8px radius outlined groups with paper-2 head strip showing day num + date + objective), `.codegen-row` per-action rows reusing `.plan-action-chip-*` for channel coloring, plus `.codegen-row-skip` rose tint, `.codegen-kind` uppercase tag, `.codegen-meta` right-aligned timing info, `.codegen-row-seed` serif italic preview block. Watch items: (a) translator emits actions but no execution path yet — heartbeat cron is the next big move; (b) "enroll_in_workflow" branch is defined but never picked — needs a plan-step → existing Close sequence matcher; (c) Box panels are now uniformly clickable but their inner action surface (e.g. "Pause workflow", "Edit opp value") is still pending — only "Open in Close" + open-expanded are wired in the menus; (d) the modal for expanded panels reuses paper-2 head style; could give each panel its own pastel tone like the day cards for consistency.
- **after [approve & run combo]:** Closed-loop full-cycle button. New `approveAndRunAction` Server Action: hydrates plan → runs `approveAllDaysInternal` (the existing logic factored out of approveAllDaysAction so we don't duplicate) → reads settings → fires `runHeartbeatForPlan(planId, "manual", settings.execution_mode)` → returns `{approved, skipped_voice, report, execution_mode}`. PlanCardClient gets the new "Approve & run" black-pill primary FAB (left of "Approve all" + "Refine plan…"). Click → useTransition → toast shows "5 approved · 0 voice-blocked · 8 fired · 14 skipped" with a "view report →" link that opens an inline modal showing skip breakdown + lead tz + duration. Right-click menu also gets "Approve & run heartbeat" as the top item. Combined with the auto-refresh + execution-mode toggle, this is the actual end-to-end execute-the-week flow: review → approve & run → watch the activity feed update via auto-refresh as Close ingests the writes. plan-head-r padding bumped to 320px to clear the wider 3-button FAB cluster. Watch items: (a) report modal here is a brief summary — for the full per-day verdicts user goes to /heartbeat dashboard or HeartbeatPanel below; (b) approveAndRunAction doesn't return a run_id so the toast can't deep-link to /heartbeat/[run_id] — could add by making setDayStatus and the heartbeat insert return their generated row ids; (c) toast caps at 5s — failures should pin until dismissed.

- **after [approve all days]:** Tiny atom. New `approveAllDaysAction` Server Action — hydrates plan, iterates needs_review days, runs voice gate per day, calls `setDayStatus(planId, i, "approved")` for safe ones, returns `{approved, skipped}` count. PlanCardClient gains a 2-button FAB cluster top-right (Approve all + Refine plan) replacing the single refine button. Click "Approve all" → useTransition fires → toast shows "5 approved · 2 skipped (voice blocked)" for 4 seconds. Right-click menu also gains "Approve all days" item. CSS: `.plan-card-fab` flex cluster, `.plan-fab-btn` shared style, `.plan-card-toast` fade-in callout. plan-head-r padding bumped 100px→200px to clear the wider FAB cluster. Watch items: (a) toast auto-clears at 4s — could persist longer for errors; (b) skipped-by-voice days don't get rewritten — operator still has to refine each one; (c) "Approve all" doesn't fire heartbeat — could chain auto-run heartbeat after.

- **after [per-day approve button]:** Tiny atom. PlanDayCard's `.plan-day-head-r` swaps the status pill for a sage "approve" button when day status is `needs_review`. Click → setDayStatusAction(planId, dayIndex, "approved") via the existing path. stopPropagation so clicking the button doesn't open the day modal. Disabled with "blocked" label + tooltip if the day has any blocking voice violations (Guardrails §G4 — operator must resolve before approving). On success the page revalidates and the button is replaced by the "approved" status pill. globals.css gets `.plan-day-approve` (sage tint pill, hover + active states, disabled state). Watch items: (a) right-click menu still works for the other statuses (sent/needs_review/skipped) — button only handles the most common needs_review→approved path; (b) batch-approve-all-days isn't surfaced — could add a "Approve all" button on plan card.

- **after [auto-refresh on lead box]:** Small atom, big UX win. tsc clean, next build clean (lead/[id] +0.44 kB → 9.1 kB). New `src/app/lead/[id]/AutoRefresh.tsx` ("use client", ~80 lines) — calls `router.refresh()` every 30s by default. Pause/resume toggle (click the pill). Auto-pauses when tab is hidden (visibilitychange listener), refreshes immediately when tab regains focus. Manual `↻` button alongside. Visible "auto · 30s" sage pill when on, "paused" gray pill when off, with last-refresh-time relative label. Wired into the Lead Box toolbar in a `.lead-toolbar-r-stack` flex column under the gate badge — non-intrusive, always visible. globals.css gets `.auto-refresh-row/-pill/-dot/-now/-last` with subtle pulse animation when refresh is in flight. Closes the loop Jake explicitly asked for ("I want to go to my CRM and change things and see if our app actually updates"). Watch items: (a) 30s polling = ~120 Close API hits/hour per open Lead Box — fine for one operator at small scale, would need WebSocket / Supabase Realtime + Close webhooks to scale; (b) manual refresh and timer fire are independent — no debounce; (c) interval is hardcoded 30s — could come from settings; (d) router.refresh() re-runs ALL server components on the page, which means re-fetching plan/heartbeat/box together each tick — fine at this size, watch if it gets heavy.

- **after [lead-local timezone + heartbeat dashboard]:** Big sprint, atomized into A1-A4 + B1-B7 per CLAUDE.md atomize rule. tsc clean, next build clean (lead/[id] 8.66 kB, /heartbeat + /heartbeat/[run_id] both in route table). **Group A — lead-local tz:** new `src/lib/timezone.ts` with `resolveLeadTimezone(lead)` returning `{tz, source, detail}`. Resolution priority: address state (US 50-state → IANA dominant zone) → primary contact phone area code (~280 NPAs → state → tz) → operator-machine-local fallback. `isInSendWindow(channel, now, tz?)` extended to compute weekday + hour-in-zone via Intl.DateTimeFormat when tz given. `runHeartbeatForPlan` resolves tz once per sweep, passes into every send-window check. HeartbeatReport adds lead_tz/lead_tz_source/lead_tz_detail. HeartbeatPanel modal shows "lead tz America/Chicago (+1 415 → CA)" in the report header. **Group B — /heartbeat dashboard:** `lib/heartbeat.ts` exports `listRecentHeartbeats(limit, scope?)`, `getHeartbeatRunById(id)`, `aggregateLast24h()` (lead_run_count, total_actions_eligible/fired/skipped, top_skip_codes top-8). New `/heartbeat/page.tsx` server route: KPI strip (4 stat cards), top-skip-reasons bar (rows with serif count + label + filled track), recent-runs table (time + scope pill + trigger pill + lead-id short + fires + skips + snap status — clicks through to /heartbeat/[run_id]). New `/heartbeat/[run_id]/page.tsx` detail route: same shape as the modal but as a full page (snapshot summary + skip breakdown + day-by-day verdicts with chip+intent+fire/skip pill+executed-close-id tooltip). TabNav gains `heartbeat` tab using existing `icons.activity`. globals.css gets ~150 lines for dashboard: `.hb-page-main`, `.hb-kpi-strip`, `.hb-kpi-card` (with `.hb-kpi-fire` sage tint), `.hb-skip-bar` row layout with track+fill, `.hb-runs-table` 7-column grid, `.hb-scope-*` and `.hb-trigger-*` pills, `.hb-snap-ok/-stale/-na`. Watch items: (a) area code table is US-only ~280 entries — international leads always fall back; (b) state→tz uses dominant zone for split states like Tennessee/Florida/Texas — operator can see `source` and override later if needed; (c) sweep summary rows (scope=all) show payload as JSON dump on detail page — could prettify later; (d) /heartbeat dashboard is read-only — could add filter chips (scope, trigger, time range) for power users; (e) recent-runs table caps at 80 — pagination not yet wired.

- **after [overlap fixes + real execution mode + Close write helpers]:** Big sprint, real execution wired. tsc clean, next build clean (lead/[id] now 8.62 kB). **Express authorization from Jake to act against the practice org (Openera Automation), all leads are fake and safe to write.** **(1) Overlap fixes** — activity feed `.lead-feed-item` grid kind column widened 70px→110px with overflow ellipsis, fixes "LEADSTATUSC(LeadStatusChange)" overlap. `.lead-footer-meta` gets paper background + z-index + horizontal padding so it stops bleeding into the next panel below. **(2) Close write helpers** in `lib/close.ts` — `closeCreateTask({lead_id, text, date, assigned_to})` POST /task/, `closeLogEmail({lead_id, contact_id, subject, body_text, status})` POST /activity/email/, `closeLogSms({lead_id, contact_id, text, status})` POST /activity/sms/. SAFETY: email/sms default `status:"draft"` — activities appear in lead's activity feed but Close does NOT actually send via SMTP/Twilio. Outbox/sent modes deferred to a future deliberate flag. **(3) Settings — Heartbeat execution mode** — `lib/settings.ts` adds `EXECUTION_MODES` enum + `execution_mode: ExecutionMode` field with default `draft_only`. `app/settings/actions.ts` adds `updateExecutionModeAction`. `/settings` page gets a 4-radio selector with descriptive blurbs and tone pills (safe / writes-to-close / manual). **(4) Heartbeat actually fires** — `lib/heartbeat.ts` gets `executeAction(channel, action, ctx)` that dispatches to `closeCreateTask` (call/task channels) or `closeLogEmail`/`closeLogSms` (email/sms). `runHeartbeatForPlan` rewritten as a sequential loop: for each day's required_actions it computes a verdict, AND if `executionMode === "approved_plan_execution"` AND verdict came back fire-eligible, it actually calls Close. On success: verdict mutates to `{fire:true, reason:"fired", executed:{kind, close_id}}`; failure: `{fire:false, skip_code:"CLOSE_API_ERROR", close_error}`. After all actions in a day attempt: if all fires succeeded AND day was approved, day's approval_status auto-promotes to "sent" (via setDayStatus) so future heartbeats skip it. **(5) Mode plumbing** — cron route reads `getSettings().execution_mode` instead of hardcoded draft_only. `runHeartbeatNowAction` Server Action reads settings too. HeartbeatPanel shows the current mode as a colored pill ("draft only" sage, "live → close" rose, "manual only" lemon) right next to the "heartbeat" eyebrow so Jake always knows what state he's in. globals.css gets `.hb-mode-*` tone classes. Watch items: (a) email/sms always go as `status:"draft"` — toggle to `outbox` is a separate future flag (live SMTP/Twilio sends require Close email integration setup); (b) execution loop is sequential not parallel — fine at small scale; (c) day promotion to "sent" only triggers if day was already "approved" — first-time approvers must approve the day BEFORE heartbeat fires it; (d) no rate limiting on the manual button — could spam Close API; (e) /settings page is still the authoritative toggle, no per-lead override; (f) failures land as CLOSE_API_ERROR but next heartbeat will retry — no exponential backoff yet.

- **after [dev-server stability + NEPQ voice validator]:** Two moves. **(1) Dev-server stability** — added `npm run fresh` script (`rm -rf .next app/.next .turbo && next dev`) to package.json so Jake has a one-command reset for stale-cache phantoms. CLAUDE.md updated with a hard rule for me: tell Jake to `npm run fresh` proactively after any sprint that touches Server Actions, new dynamic routes, or client-component shape. The Next 14 dev server in-memory webpack cache desyncs from disk under those conditions; `next build` is fine, only the running `next dev` breaks. **(2) NEPQ voice validator (Guardrails §G4)** — new `src/lib/nepq.ts`: pure-regex validator with 13 rules across two severity tiers. **block** rules (must rewrite before send): FAKE_WARMTH ("hope this finds you well"), JUST_TOUCHING_BASE, CIRCLE_BACK, REACHING_OUT, FOLLOWING_UP, SYNERGY_LANGUAGE, PLEASE_DONT_HESITATE, AT_YOUR_CONVENIENCE, PER_MY_LAST. **warn** rules (surface, don't block): EXCLAMATION_OVERUSE (more than one !), AS_DISCUSSED, QUICK_QUESTION, WANTED_TO_SHARE, I_HOPE_YOU. Exports: `validateNepqVoice(text)`, `hasBlockingViolation(violations)`, `validatePlanDrafts(days)`. Each violation carries code + severity + rule + matched substring + position. Wired into `lib/heartbeat.ts`: every email/sms required_action's draft_seed is voice-validated; blocking violations surface as `VOICE_FAIL` skip code in the verdict (Guardrails §G4 + §O2). PlanDayCard runs the validator at render time and shows: a "voice ✗" (rose) or "voice ⚠" (lemon) pill in the card head when any action has hits, plus a per-action violation list inside the modal showing severity tag + rule + matched phrase. globals.css gets `.plan-day-voice` head pill, `.plan-action-voice-*` violation list with rose/lemon left-rule treatment. Watch items: (a) regex-only — context-aware "does this sound like Andre" check is the lattice's job (deferred per Jake); (b) only `email`/`sms` channels validated — call/task scripts may also need it; (c) violations show in modal but the day card hides matched phrases — visible only on click; (d) RULES list is hardcoded — could move to settings JSON for Andre-tunable thresholds.
- **after [heartbeat + gates: stop signal, send window, freq cap]:** Big sprint. tsc clean, next build clean (lead/[id] at 6.82 kB). **(1) New gate primitives** in `lib/close.ts`: `detectStopSignal(activities)` regexes opt-out phrases over inbound text (returns `StopSignalHit[]` with the matched phrase + preview); `isReplyGateActive(activities)` for §F3 (true if last inbound newer than last outbound); `isInSendWindow(channel, now)` for §J2 (SMS 9-7, email 7-9, Sunday SMS after 11); `checkFrequencyCap(activities, now)` for §J3 (1/24h, 4/7d). SkipCode enum expanded with FREQUENCY_CAP_24H, FREQUENCY_CAP_7D, NO_CONTACT, DAY_NOT_APPROVED, DAY_SKIPPED, DAY_ALREADY_SENT, DAY_NOT_TODAY, EXECUTION_DISABLED. **(2) Heartbeat runner** in `lib/heartbeat.ts` (~280 lines): `runHeartbeatForPlan(planId, trigger, executionMode)` rehydrates the Box, recomputes the snapshot id, marks plans stale + auto-pauses on snapshot mismatch (§E3), runs all gates against the current Box, then walks every day's required_actions and emits a typed `ActionVerdict` (fire vs. skip + skip_code + reason). Decision tree per Guardrails §P: day approval → today check → ownership/status → stop signal → reply gate → channel-specific (no contact / send window / freq cap) → execution mode. Default `executionMode: "draft_only"` per §I1 — heartbeat never silently sends; it audits + reports. `runHeartbeatSweep()` iterates all plans in draft/approved/active. Each run writes a row to `heartbeat_runs` (audit trail per §O). **(3) Vercel cron** at `/api/cron/heartbeat` — Bearer auth via CRON_SECRET, calls `runHeartbeatSweep("cron", "draft_only")`, returns sweep summary. `vercel.json` schedules daily at 13:00 UTC (9am ET — works on Vercel hobby tier; manual button is the dev path). **(4) Supabase migration `create_heartbeat_runs`** — uuid PK, scope (lead/all/manual), plan_id, close_lead_id, snapshot_match, plan_was_stale, action counts, skip_breakdown jsonb, report jsonb, duration_ms, trigger, RLS on with service_role full access, two indexes (lead+ran_at desc, ran_at desc). **(5) HeartbeatPanel** client component on the Box page (under PlanSection): shows last sweep summary inline ("last sweep [time] · N would fire · M skipped · box changed since plan"), "Run heartbeat now" button (uses useTransition; opens modal with the report on success), "View last report" button to reopen. Modal renders: header summary + flag callouts (stop signal, reply gate, ownership skip), skip breakdown grid (count + human label per code), day-by-day verdicts with status pill + today badge + per-action chip + verdict pill (sage "would fire" or paper-2 "skip code with reason tooltip"). globals.css gains ~150 lines: `.hb-panel` summary bar, `.hb-stale` warning text, `.hb-summary` callout, `.hb-flag` warning lines, `.hb-skips-grid` count grid, `.hb-day` outlined groups with paper-2 head, `.hb-day-status-*` per-status pills, `.hb-day-today` ink-on-card today badge, `.hb-action` rows, `.hb-action-fire`/`.hb-action-skip` pills. Watch items: (a) execution mode is hardcoded `draft_only` everywhere — moving to `approval_required` or `approved_plan_execution` requires a deliberate flag (Settings page); (b) Vercel cron is daily for hobby-tier compatibility — Pro tier could run every 30 min; (c) send-window check uses operator-machine local time, not lead-local (Guardrails specs lead-local); (d) frequency cap counts ALL Email/SMS outbound — doesn't distinguish manual vs. automated; (e) heartbeat doesn't yet auto-mark days as "sent" after firing — once execution is enabled, that loop closes; (f) no rate limiting on the manual "Run heartbeat now" button — could spam Close API; (g) sweep over many plans is sequential — fine at small scale, would need parallelism for hundreds.

## 2026-05-01 (evening)

- **before [practice lead seed + Close comms probe]:** Jake asked for ~50 synthetic Close leads, rebalance ownership so Andre and Jake each hold ~half pre-existing + ~half newly seeded leads, and an API smoke test (SMS/email) to confirm activities land in Close for fake numbers.
- **after [practice lead seed + Close comms probe]:** Done. Added `closeCreateLead` + `closeUpdateLead` in `lib/close.ts`, `scripts/seed-practice-leads.ts` + `npm run seed:practice-leads` + `tsx` devDep. Openera run: 51 legacy → +50 seeded (101 total); ownership split **Andre 25 old + 25 new (50)**, **Jake 26 old + 25 new (51)** — one extra legacy lead made Jake +1. Draft **SMS + email** both returned 200; activity count on probe lead went 1→3. **Outbox** SMS returned Close `400` (`local_phone` required; `remote_phone` “cannot send to yourself”) — real sends need Close SMS integration + real paired numbers, not the draft path.

## 2026-05-02

- **before [Close API automation + surface expansion]:** Jake asked to “make the Close API set up amazing” with automation first, grounded in Guardrails + ledgers + `_reference/close-llms.md`. Expand `lib/close.ts` with sequence/workflow fetch, subscription get/update, SMS templates, lead statuses, phone numbers, internal notes; extend chat tools + `/test` Close probe; keep Guardrails gates on writes.
- **after [Close API automation + surface expansion]:** Done. `close.ts` gains automation/config helpers (`closeGetWorkflow`, `closeGetSequenceSubscription`, `closeUpdateSequenceSubscription`, `closeListSmsTemplates`, `closeListLeadStatuses`, `closeListPhoneNumbers`, `closeLogNote`). `close-tools.ts` adds read tools + gated `close_update_sequence_subscription` / `close_log_internal_note`. `/api/test` Close mode reports counts for SMS templates, lead statuses, phone numbers. `api/chat` system string lists new tools. Header points at `_reference/close-llms.md`. Next atoms: webhooks, bulk actions, opportunity update, call log, custom-field schemas — lazy-add per Guardrails needs.

- **before [Close webhook ingress — functionality not demos]:** Jake: no more demo-only work; ship execution-adjacent paths. Guardrails milestone still names `/api/webhooks/close` + Supabase for live activity.
- **after [Close webhook ingress — functionality not demos]:** Done. `POST /api/webhooks/close` verifies `close-sig-hash` per Close docs (HMAC-SHA256 over `close-sig-timestamp` + raw body) when `CLOSE_WEBHOOK_SIGNATURE_KEY` is set; production returns 503 if unset; dev allows unsigned POSTs for local curl. Payloads persist to `close_webhook_events` (unique `event_id`, idempotent on Close retries). Migration: `supabase/migrations/20260502120000_close_webhook_events.sql`. `close.ts` adds `closeListWebhookSubscriptions`; `/api/test` Close mode reports `webhook_subscription_count` + sample URLs. Settings documents the key + endpoint. **Ops:** apply SQL to Supabase → set hex `signature_key` in `.env.local` → point Close subscription at `https://<host>/api/webhooks/close`. Next: Realtime or Lead Box feed reading this table; heartbeat reactions to selected `object_type`/`action`.

- **queued [Close webhook go-live — Jake deferred ops headspace]:** Code + DB ready; **no app auth involved**. When ready: follow the checklisted section **“Things to do — Close webhooks”** in `_scaffold/Global.md` (public URL → deploy → Close subscription → `CLOSE_WEBHOOK_SIGNATURE_KEY` → SQL verify).

- **before [Close comms + snapshot + leads API depth]:** Wire lead/comms APIs to Box + 7-day ledger: accurate pulls, staleness when Close updates, map `close-api-docs.json` / `close-llms.md` into real surfaces.

- **after [Close comms + snapshot + leads API depth]:** `closeGetLeadFull` = paginated activities (≤500) + `GET /activity/emailthread/` + subs. `snapshotIdForBox` uses sorted newest activity + email-thread touch + subs (fixes bad sync when API order isn’t chronological). Plan prompt adds **Email threads** section. `/leads` Andre tab = `closeListLeadsByAssignee` (Advanced Search) + fallback. Box: Email threads panel. Chat: `close_list_email_threads`. `npm run close:box-audit`; `_reference/close-llms.md` **Comeketo Agent — where this doc lands**. **Once:** snapshot formula changed — existing plans may read **stale** until regenerate.

- **before [Delegations round 2]:** Jake wanted variety + motion + control. Specific asks: tool panels feel different per kind, right-click menus everywhere we can, beautiful panels, markdown always rendered, no boring static "thinking..." text, panes (Delegations / Chat / In scope) need to be closeable / 2x wider / cycle modes, every interaction needs visible feedback so the screen never feels frozen.
- **after [Delegations round 2]:** Done. (B1) Pane resize — hidden/normal/wide per side pane, persisted to localStorage, header cycle+hide controls, vertical reveal tabs when hidden, 220ms grid transition. (B2) Tool panels with variety — categorized per tool (`read`/`list`/`generate`/`write`) with distinct tone + glyph + ribbon, click opens tone-headed modal with args JSON + result preview (markdown when textual, formatted JSON otherwise). (B3) Animated thinking — three pulsing dots + rotating serif italic phrases, top progress-bar gradient slide on chat panel during requests, spinning send icon, header dot flips sage→amber while loading. (B4) Right-click menus on every interactive surface — messages (copy text / copy markdown), quick delegations (run / copy prompt / seed input), tool panels (view / copy result / open lead), scope dock, threads. (B5) Tool result persistence — `api/chat/route.ts` stores a 600-char result summary in the `cmk:tools` block so the modal previews real data. tsc + next build clean (/chat 50→52 kB). `npm run fresh` recommended.

- **before [Delegations page magic]:** Jake asked to spruce up /chat (the "Delegations" tab) while another agent works the Close API in parallel. Goal: turn it from a thread-list + chat panel into a real delegation surface — visible tool calls, quick-action shortcuts, contextual scope, tight thread management.
- **after [Delegations page magic]:** Done. Five atoms shipped: tool-call cards (structured chips replace `_used:` italic), quick-delegations rail (5 prebuilt prompts with tone-tinted edges), right-side scope dock (auto-pins the latest lead in conversation, lazy-hydrates name/status, jump to Box or Close), lead-mention chips (auto-link `lead_xxx` IDs in assistant markdown), thread context menu (right-click → rename/archive/delete via new PATCH+DELETE `/api/threads/[id]`). New endpoint `/api/leads/[id]/summary` backs the dock. tsc + next build clean. Recommend `npm run fresh` — this sprint added a new dynamic route + reshaped /chat client/server boundaries.

- **before [variable-horizon cycle plans + practice leads on /leads]:** Plans were hardcoded to 7 days; Jake needs N-day cycles (1–180 calendar buckets), settings default, chat tool `horizon_days`, and /leads visibility for seed-script leads + Practice filter.
- **after [variable-horizon cycle plans + practice leads on /leads]:** Done. `lib/plan.ts` — dynamic prompts + validation per N; `SevenDayPlanDay.day` is numeric; refine whole/day uses `fullPlan.days.length`. `settings` — `default_plan_horizon_days` + Settings UI + `updateDefaultPlanHorizonAction`. Box: cycle length input + regenerate with editable days; `PlanCardClient` uses live day count in copy + menu regenerate. `close-tools` / `api/chat` accept optional `horizon_days`. `closeListLeads` requests `description`; `/leads` — Practice pill (`?seed=practice`), row badge, shared `practice-seed.ts` with seed script. tsc + next build clean.

- **before [Delegations round 3]:** Jake handing me runway to cook in parallel with the other agent's Close-API work. Asked for "balls to the wall": more keyframes, modal styles, layout transitions, motion polish so nothing feels frozen, right-click menus everywhere they fit, beautiful panels, markdown always rendered.
- **after [Delegations round 3]:** Big motion + chrome sprint, fully client-side (no `api/chat/route.ts` or `lib/close.ts` edits — clean of the parallel agent). New `src/components/Toast.tsx` provider mounted in root layout. Toasts fire on copy / seed / rename / archive / delete / quick-delegation / image-attach / send-error. Tool panels + quick delegations enter with staggered keyframes; running quick delegation gets a tone-colored sliding flare bar. Send-success ring pulses the chat panel border green. Scope dock lands with a from-right overshoot when a new lead pins. Reveal tabs + empty scope state breathe. Active thread rows: animated left sage strip + tabular-num relative time. Drag-drop images onto the chat (translucent overlay + bouncing chevron + dashed outline) + paste-image from clipboard. Smooth scroll on new messages. Auto-grow textarea (max 160px). Comprehensive markdown chrome (Fraunces h1-h4 with italic descents, code, pre with sage rule, blockquote with peach rule, hr dashed, tabular-num tables, kbd keycaps, sage links). Keyboard layer: ⌘K focus, ⌘/ alt focus, ⌘\ toggle rail, ⌘. toggle scope, ⌘⇧N new thread, "?" opens a help overlay. tsc + next build clean (/chat 52→54.4 kB). `npm run fresh` recommended when Jake's back at the keyboard.

- **before [/automation live Close sequences + CLI]:** `/automation` was demo graph only; Jake wants the site + scripts to reflect real Close (sequences API), not webhooks-only.
- **after [/automation live Close sequences + CLI]:** `/automation` lists live sequences from `closeListWorkflows` (status pills, step counts, step types, Open in Close). Sample graph moved under `<details>` as layout reference. `closeSequenceBrowserUrl()` in `lib/close.ts`. New `scripts/close-list-sequences.ts` + `npm run close:list-sequences`. tsc + next build clean.

- **before [Delegations round 4]:** Jake fired up — sees the finish line. Continuing client-side polish so the other agent's Close API work stays unblocked.
- **after [Delegations round 4]:** Done. Slash command palette in the input bar (10 commands incl. `/lead <id-or-query>` smart-routing, `/heartbeat`, `/today`, `/rerun`, `/rail`, `/scope`, `/reset`) with arrow-key nav + Tab-complete + Esc-dismiss. Abort/cancel — send button flips to a pulsing red stop button while loading; Esc cancels from anywhere; canceled assistant turn lands as `_(canceled)_`. Thread search filter (appears when >4 threads). Re-run-last delegation `↻` button in rail header. Markdown code-block copy buttons (hover-revealed, top-right of every `<pre>`). Live timestamp tick (60s interval refreshes relative-time labels). Updated shortcut overlay. tsc + next build clean (/chat 54.4→55.8 kB).

- **before [Close surface sprint — leads search + automation detail + CLI]:** Deepen live Close on `/leads` (`?q=`, `?status_id=` + status catalog, href preservation) and read-only `/automation/[id]`; add `npm run close:list-lead-statuses` + optional lead search CLI; run tsc + next build; fix scaffold references that still named the wrong sequence URL helper.

- **after [Close surface sprint — leads search + automation detail + CLI]:** Done. `/leads` — GET form for `?q=` + status dropdown (`closeListLeadStatuses`), chips, pill hrefs preserve filters; Advanced Filtering for `status_id` (`closeListLeadsByStatusId`, `closeListLeadsByAssigneeAndStatus`); `LEAD_LIST_FIELDS` includes `status_id`. `/automation/[id]` — `closeGetWorkflow` step table, back link preserves list filter when `?status=` passed, list links to detail. Scripts: `close:list-lead-statuses`, `close:search-leads` (`--q`, `--limit`). `_reference/close-llms.md` table updated. Goals/Global historical lines now say `closeSequenceBrowserUrl()`. tsc + `npm run build` clean — run `npm run fresh` if dev server had stale chunks.

- **before [big moves roadmap — live loop + Box + heartbeat + approvals + safety]:** Implement plan todos: webhooks visibility, SSE Box bump, structured heartbeat sweep + trace, approvals page, draft lint, gated workflow enroll, intake storage+artifacts, operator lock + RLS for publishable keys, vitest for webhook + snapshot id, fix TS/build regressions.
- **after [big moves roadmap]:** Shipped. Webhook readers + `/heartbeat/webhooks`; per-lead SSE + `BoxActivityWatch`; sweep rows with `trace_id` + `lead_summaries` on `/heartbeat/[run_id]`; `/approvals`; `draft-lint.ts` + PlanDayCard; `WorkflowEnrollSection` + guarded enroll; `/intake` + `IntakeUploader` + artifact list; `OPERATOR_*` env + Settings + `assertOperatorSession` on plan mutations (incl. pause/generate/refine/day status); migration `20260504100000_rls_publishable_denied.sql`; Vitest (`npm run test`) for webhook signature + `snapshotIdForBox`; `settings-action-state.ts` fixes `use server` non-async export. Verified: `tsc --noEmit`, `npm run test`, `npm run build`.

---

### Post-auth epic (2026-05-01)

- **before [post-auth epic — Lead OS hardening]:** Execute plan phases 0–8: operator middleware semantics, execution/approval audit coverage, unified timeline + visible stale gate on AI refine, SSE keyed off webhook + `lead_activity_touches`, approvals v2 (snapshot diff + reject), subscription pause/resume, intake extract + Box artifacts + signed URLs, structured logs + extra vitest, §H1 email preview path + Box analytics strip alignment.
- **after [post-auth epic — Lead OS hardening]:** Shipped in repo. Operator cookie + middleware (prior); extended `execution_log` writers (refine, day status, approve+run w/ trace, manual heartbeat, reject queue, subscriptions); `assertPlanSnapshotFresh` blocks day/whole refine when stale; Plan + day UI disables refine with §I3 copy; SSE `activity-stream` merges webhook latest + `lead_activity_touches`; `/approvals` shows live snapshot diff, skip column, Open Box / Edit plan (`#cycle-plan`) / Reject queue; `WorkflowSubscriptionControls` + template counts in enroll panel; `/api/intake/extract` + upload returns `artifact_id` + uploader triggers extract; `IntakeArtifactsPanel` + `redirectIntakeArtifactDownload`; `logStructured` on webhook + cron; new tests (activity freshness, edge HMAC, email HTML). Verified: `tsc --noEmit`, `npm run test`, `npm run build`. **Ops:** apply `20260505120000_execution_audit.sql` if not on project DB.

- **after [polish plan rounds 5→9 in one autopilot sprint]:** Done. Round 5 (hoist Modal/ContextMenu, prefers-reduced-motion, toast adoption + cap+dedup, modal exit anim), Round 6 (Lead Box stagger, gate-block pulse, BoxPanel right-click verbs, activity feed motion, plan day hover, stale pill chrome), Round 7 (/leads + /heartbeat + /automation row hovers + skip bar scaleX + day stagger + today glow), Round 8 partial (/test result chrome with copy + spinner), Round 9 partial (hero stagger + cascading title + clock breathing + 1100/768/480 responsive). tsc + next build clean. Round 10 queued (KPI counter tick-up, RouteProgress, EmptyState lib, slash palette /lead/[id], thread URL deep-link).

- **before [Round 11 — chat-first widgets]:** Jake's vision shift: Lead Box page is too dense, hates scrolling, wants daily work to happen in chat with widget results pinning to the right dock. Tool-call modals should render real widgets, not JSON.
- **after [Round 11]:** Pinboard live (right dock = stack of pinned widgets, localStorage-persisted, dedup on lead_id). Tool-call modals now render `RichToolResult` widgets — sky-tinted lead card with contacts, peach-tinted plan grid with day cards, sage-tinted search results — instead of raw JSON. `📌 Pin` button on every tool modal + `/pin` slash command + auto-pin when a tool call references a lead. Removed legacy single-lead scope auto-fetch in favor of inline tool-call snapshots. tsc + next build clean.

- **before [Lane A R2 — payload tighten + parser fallback + /morning slash]:** Closing the watch item I flagged on Lane A R1 — 600-char `cmk:tools` summary truncation can drop my widget into the raw-JSON fallback when example arrays + long lead names exceed the budget. Three atoms in this round, all isolated to Lane A files: (1) `pipeline-state.ts` — cap `waiting_top` + `fired_top` to 3 each (was 5), trim intent to 40 chars (was 60), keep counts ahead of arrays in object-key order so even a truncated payload retains the parseable head; (2) `ChatPanel.tsx` `RichToolResult` — when `JSON.parse(summary)` fails, fall back to a regex-extract of the leading number fields (today/waiting/fired/plans) and render a counts-only KPI strip with a "lists truncated — view raw below" hint; (3) `ChatPanel.tsx` slash palette — add `/morning` (and alias `/today`) that prefills "What's my morning?" so Andre/Jake can fire the briefing in one keystroke. No new files; no cross-lane edits. Stay in lane.

- **after [Lane A R2 — payload tighten + parser fallback + /morning slash]:** Landed clean. tsc clean (no new errors alongside Lane B/C/D's parallel edits). **(1) Payload tighten in [`pipeline-state.ts`](src/lib/pipeline-state.ts):** EXAMPLE_CAP 5→3, GATED_CAP 6→5, INTENT_TRIM 60→40 chars. Counts (plans_active / today_eligible / waiting_count / fired_count) already lead the output object's key order, so even a truncated payload retains a parseable head. **(2) Hardened widget parser in [`ChatPanel.tsx`](src/app/chat/ChatPanel.tsx):** extracted `PipelineStateWidget` + `parsePipelineSummary()` helper. Tries full `JSON.parse(summary.replace(/…$/, ""))` first; on failure falls back to regex extraction of the count fields (`/"plans_active"\s*:\s*(\d+)/` etc.) and renders a counts-only KPI strip with a peach-tinted "Lists truncated — counts above are reliable" banner. The widget always shows the four KPI numbers — never falls through to raw-JSON pre. Bypass for the function's `if (!parsed) return null` early-out: pipeline_state branches to the dedicated component before the generic check. **(3) `/morning` slash command** with aliases `/morn` and `/brief` — fires the natural-language briefing prompt that the system prompt teaches the agent to route to `pipeline_state_for_owner`. Inserted after `/today` so existing operator muscle memory isn't disturbed. **(4) CSS:** added `.cmk-rich-pipeline-partial` peach-rule banner block in `globals.css`. Files touched: `pipeline-state.ts`, `ChatPanel.tsx`, `globals.css`. No cross-lane bleed. Lane A R2 done.

### 2026-05-04
- **before [fix disappearing plan]** — root cause: AutoRefresh ticks every 30s → re-runs loadLeadBoxPageData → getLatestPlanForLead errors are swallowed by .catch(()=>null) → plan: null → 'No plan yet' UI. Fix: default AutoRefresh off on /lead/[id], stop swallowing plan-read errors, surface as a distinct fetch-failed UI.
- **after [fix disappearing plan]** — done. AutoRefresh defaults to off on /lead/[id] (auto-refresh pill now reads 'auto off'); load-lead-box.ts surfaces planError instead of swallowing; PlanSection renders a distinct 'Plan fetch failed' state when fetch throws. Typecheck ✅. Live test: open /lead/[id], generate plan, leave 5+ min — must persist. Watch: any plan now staying through tab focus/refresh confirms fix. Heartbeat page still auto-refreshes (intentional).
- **before [site-wide lead names]** — replace raw lead_xxx IDs with lead display names everywhere they're rendered (heartbeat, console, approvals, run logs). Build a batched server-side resolver, cache by lead_id, render as link to /lead/[id]. Keep ID accessible on hover for debugging.
- **after [site-wide lead names]** — done. Created src/lib/lead-names.ts (resolveLeadNames cached 60s, displayName, shortLeadId). Wired into 4 surfaces: /heartbeat (Recent Runs row), /heartbeat/[run_id] (per-lead summary), /console (execution log), /heartbeat/webhooks (Lead column). Every name links to /lead/[id]; full lead_id available on hover via title attr. Typecheck ✅.
- **before [group MCP-fallback panels]** — chat tool execution panels currently render each tool call as its own red 'failed' card when API tools fail before the LLM falls through to MCP. Server-side group adjacent failure→close_mcp_call sequences into 'recovered' status with the failed attempt(s) collapsed underneath.
- **after [group MCP-fallback panels]** — done. New src/lib/tool-groups.ts (groupToolTrace pure helper). Server emits cmk:tool-groups fence; client parses both new + legacy cmk:tools fence (legacy promotes to single-call groups so old history still renders). New ToolGroupPanel renders 3 shapes: ok-single (unchanged), recovered (green pill, prior failures collapsed), failed (red pill, all attempts visible). Three additive CSS classes. Typecheck ✅.
- **before [search-tool fix + honest MCP labels]** — close_search_leads body shape is wrong against Close /data/search/ endpoint (klausApiError 'value_search.id required'). Switch it to delegate to the simple GET /lead/?query= path that closeListLeads already uses — eliminates the failures at the source. Update tool description so LLM sends plain text not Klaus DSL. Make settings UI honest about the MCP toggle being inert without CLOSE_MCP_URL.
- **after [search-tool fix + honest MCP labels]** — done. closeSearchLeads delegates to closeListLeads (GET /lead/?query=) instead of the broken POST /data/search/. Tool description tells LLM to send plain text not Klaus DSL. Settings MCP-fallback toggle disabled (greyed out + 'Inert until MCP is configured' copy) when CLOSE_MCP_URL missing. Typecheck ✅.
- **before [bake MCP tool catalog into system prompt]** — MCP is now configured + ready. The LLM only sees close_mcp_list_tools/close_mcp_call wrappers and has to discover MCP tools at runtime — which it rarely does. Bake the full ~50-tool MCP catalog (read/write_safe/write_destructive scopes) into DEFAULT_INSTRUCTIONS so the LLM picks the right MCP tool first try.
- **after [bake MCP tool catalog into system prompt]** — done. Replaced the 3-line MCP fallback section in DEFAULT_INSTRUCTIONS with a full ~50-tool catalog organized by scope (read / write_safe / write_destructive). Each entry has the tool_name verbatim + a tight description so the LLM can pick by name without runtime discovery. Added an explicit destructive-operations rule (no delete_* on inferred intent). Typecheck ✅.
- **before [Phase 1 WorkflowManifest foundation]** — define canonical WorkflowManifest type + read-side adapters from lead_plans and automation_drafts + manifestToMermaid emitter + inspector rail prototype on /automation/drafts/[id]. Zero risk to live data or runtime. Defer Change 5 (lead-page debug link) — tests prove the adapter.
- **after [Phase 1 WorkflowManifest foundation]** — done. NEW src/lib/workflow-manifest.ts (canonical type + leadPlanToManifest + automationDraftToManifest + manifestToMermaid). NEW src/lib/workflow-manifest.test.ts (14 unit tests, all pass). Drafts page at /automation/drafts/[id] now renders the AutomationCanvas with an external inspector rail showing the unified manifest node config + a 'View as Mermaid' modal with copy. AutomationCanvas already had the onSelectionChange + externalInspector hooks — used as-is, no canvas changes needed. Typecheck ✅, 26/26 tests ✅, build ✅. Skipped Change 5 (lead-page debug link) — adapter is proven by tests.
- **before [Phase 2 cockpit + automation cleanup]** — Half A: /chat becomes lead-scoped Studio with Andre/Lead toggle, lead picker (search + paste-Close-URL + recent), lead context in chat API system prompt, day-strip in right rail, heartbeat tile, generate-plan button. Half B: delete /automation/workflows demo route + Morning Sweep + workflows tab. Read-only on inspector — edit affordances are Phase 3+.
- **after [Phase 2 cockpit + automation cleanup]** — done. /chat now has Andre/Lead mode toggle. Lead mode: LeadPicker (search + paste-Close-URL + recent), lead-context block prepended to system prompt server-side, lead-scoped quick prompts in left rail, PlanDayStrip in right rail (uses existing PlanDayCard wholesale + heartbeat tile + generate-plan flow). New routes: GET /api/leads/search, GET /api/lead/[id]/plan. Half B: /automation/workflows + Morning Sweep demo + WorkflowStudio + workflow-studio-data + SequencesWorkflowPreview all DELETED. Workflows tab dropped from sub-nav. Empty-state copy on /automation rewritten. Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [cockpit polish: deeper lead context + editable day card]** — two issues caught in cockpit smoke test: (1) Lead-mode context block uses closeGetLead (compact) which trains the agent NOT to dig deeper — manual prompts via Andre mode produce way richer answers because they force close_get_lead_full + searches; (2) PlanDayCard modal lets you ADD a touch but not EDIT/APPROVE/RECLASSIFY the existing touch. DB layer (updatePlanDay, setDayStatus) already supports all three.
- **after [cockpit polish: deeper lead context + editable day card]** — done. (1) Lead-context block in route.ts now explicitly tells the agent the summary is JUST identification — first move on any open-ended question is close_get_lead_full + close_list_activities/close_list_email_threads. Should produce richness parity with manual-prompt Andre mode. (2) PlanDayCard modal: each existing touch has an inline 'edit' button that opens a channel-select + intent + draft_seed editor with save/cancel/delete. New 'Approve day' / 'Move back' / 'Mark sent' / 'Skip' buttons in the modal header (above the body) so the operator can approve from inside the modal. NEW DB fns editPlanDayTouch + deletePlanDayTouch in plans-db.ts; NEW server actions editPlanDayTouchAction + deletePlanDayTouchAction. Typecheck ✅, 26/26 tests ✅, build ✅.
- **after [day modal width + chip picker styling]** — done. Modal now accepts width="wide" prop (max 960px); PlanDayCard uses it. Native <select> for channel REPLACED with ChannelChips picker — pill-row of sms/email/call/task chips colored by channel-family tokens (sage/sky/lavender/peach), active state opaque + subtle shadow, glyph + label per chip. Text inputs swapped from plan-horizon-input (designed for tiny number) to plan-touch-input (full-width, matches site aesthetic, italic placeholder, focus ring in peach). Form layout reflowed into plan-touch-form (warm-paper card with gap-12 column + responsive 2-col row). Both inline-edit AND add-touch forms use the same components. Typecheck ✅, 26/26 tests ✅.
- **before [Andre rail names + remove deep think]** — (1) Andre-mode right rail pins still show raw lead_xxx IDs because pinFromCall only captures display_name when pin originates from a close_get_lead/get_lead_full call (auto-pin from list/search calls leaves display_name undefined). Fix: client-side LeadNamesContext that fetches /api/leads/search?limit=200 once + lookup at pin render time. (2) Deep-think checkbox disables tool calls — Jake wants it gone entirely (UI, state, API field, route branch).
- **after [Andre rail names + remove deep think]** — done. (1) NEW LeadNamesContext + useLeadNamesProvider() in ChatPanel.tsx fetches /api/leads/search?limit=200 once on mount + caches id→name. NEW PinTitle component looks up the live name at render time, so lead pins now show 'Trailhead Catering Guild' instead of 'lead_xxxxxx…'. PinBody for lead kind dropped the verbose ID row (still in title attr for hover); status row + Open Box action stay. (2) Deep-think fully removed: state vars, AnimatedThinking prop, send-body field, UI checkbox/label, ChatRequest field, deepThink branch in chat route, DEEP_THINK_SUFFIX constant, pollBackgroundResponse import. /openai-background.ts file left in place — orphan but harmless. Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [task #8 API tool dictionary audit]** — 4 surgical fixes to DEFAULT_INSTRUCTIONS in src/app/api/chat/route.ts: (1.A HIGH) close_enroll_in_workflow signature is wrong (workflow_id → sequence_id, missing required contact_id), (1.B MED) close_update_sequence_subscription overstates 'unsubscribe' capability, (1.C MIN) close_list_leads_by_assignee user_id optionality not surfaced, (1.D MIN) close_create_task uses wrong param name (due_date → date) and missing is_complete + ISO date format note. Then optional Phase 2 copy-edit pass.
- **after [task #8 API tool dictionary audit]** — done. Phase 1 (4 surgical fixes): close_enroll_in_workflow signature corrected (workflow_id → sequence_id, contact_id added as required), close_update_sequence_subscription unsubscribe claim removed (status enum 'paused'/'active' only), close_list_leads_by_assignee user_id optionality made explicit, close_create_task date param renamed (was due_date) + is_complete + ISO format documented. Phase 2 polish surfaced 6 MORE signature mismatches: close_get_workflow uses sequence_id (not workflow_id), close_create_lead uses contact_name/contact_email/contact_phone (not contacts[]), close_update_lead has explicit field shape (not opaque 'patch'), close_create_opportunity value is optional + in cents + confidence param, close_create_sequence/update_sequence take stringified JSON (sequence_json/patch_json), close_log_email/sms_activity status enum documented. ALL signatures + optional params + enum values now match tool defs. Plus pattern section line 268 fixed (date instead of due_date). Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [P4 v0 heartbeat dry-run simulator]** — smallest cut of P4 that ships a new affordance: preview per-touch verdicts before clicking Approve & run. Reuses existing verdictForAction logic in a no-write path. Inline pills on PlanDayCard (would_fire / SKIP_CODE). No Mermaid, no graph, no vocabulary refactor — those are P5+.
- **after [P4 v0 heartbeat dry-run simulator]** — done. NEW simulatePlanForLead() in src/lib/heartbeat.ts — pure read, reuses verdictForAction with 'approval_required' execution mode (gates evaluate fully, no executor branch). NEW route GET /api/lead/[id]/plan/simulate. Cockpit PlanDayStrip got a Simulate button + summary line (would_fire / would_skip / skip_breakdown). PlanDayCard accepts optional verdicts prop and renders inline 'would fire' (sage) / SKIP_CODE (lemon) pills next to each touch's intent. Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [plan-as-graph view + simulate-driven coloring]** — Jake wants the graph back. Build a plan-as-graph view that converts the 7-day SevenDayPlan via leadPlanToManifest → Workflow with linear positions, mounts AutomationCanvas with externalInspector, and lights up nodes ('fire'/'skip') based on simulatePlanForLead verdicts. New /lead/[id]/graph tab + cockpit link. Reuses everything from Phase 1 + the simulator we just shipped.
- **after [plan-as-graph view + simulate-driven coloring]** — done. NEW /lead/[id]/graph route + LeadSubNav 'Graph' tab. NEW PlanGraphView client component converts SevenDayPlan via leadPlanToManifest → Workflow with linear positions (220px x-step), mounts AutomationCanvas with externalInspector + nodeStates Map. Inspector right rail shows day objective + send window + status + per-touch list with verdict pills when simulated. Simulate button reuses /api/lead/[id]/plan/simulate; verdict aggregate (any-touch-fires) drives node green/yellow tint. AutomationCanvas extended with optional nodeStates prop; data-state attribute renders sage/lemon overlay tints with drop-shadow. Cockpit PlanDayStrip got a 'graph →' link. Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [graph polish + remove /automation]** — five fixes: (a) double-header bug on /lead/[id]/graph (page renders AppHeader+TabNav AND layout already provides them), (b) delete /automation top-level entirely (drafts/sequences/canvas) — graph lives on the lead now; move AutomationCanvas to /components, (c) cockpit PlanDayStrip: remove Simulate button + 'graph →' link + simulation state, keep only 'full Box →', (d) shorten plan-day node labels from 'Day N — objective' to 'Day N' to fit shapes; cycle shapes per day, (e) reuse PlanDayCard inside graph inspector so click-on-node opens the existing edit modal.
- **after [graph polish + remove /automation]** — done. (a) Double header fixed: removed AppHeader + TabNav from /lead/[id]/graph (layout already provides them). (b) /automation deleted entirely; AutomationCanvas moved to src/components/; TabNav, middleware, /console all updated; 3 lib files (automation-drafts, close-workflow-graph, sequence-ai) reimport from /components/. (c) Cockpit PlanDayStrip stripped of Simulate button + simulation state + 'graph →' link + verdict pills — only 'full Box →' remains. (d) Plan day labels shortened to 'Day N' (was 'Day N — objective'); shapes cycle per day through trigger→actor→transform→sink→state for visual rhythm. (e) Graph inspector now renders the existing PlanDayCard for the selected day so click on the card opens the existing edit modal. Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [task #9 tool-panel UI coverage]** — TOOL_CATEGORY map in ChatPanel.tsx covers ~20 tools out of ~32 CLOSE_TOOLS + 3 COMPOSITE_TOOLS + 2 MCP wrappers. Missing tools fall through to default 'read'. Plus: when LLM calls close_mcp_call, panel renders 'close_mcp_call' generically — should show 'MCP · {target_tool}' so operator sees what MCP op was actually invoked.
- **after [task #9 tool-panel UI coverage]** — done. TOOL_CATEGORY map extended to ALL 32 CLOSE_TOOLS + 3 COMPOSITE_TOOLS + 2 MCP wrappers (was ~20). NEW MCP_TOOL_CATEGORY map (~50 entries) maps target_tool → category. categoryOf() now takes args and inherits MCP category from args.tool_name. toolHeadline() special-cases close_mcp_call to render 'MCP · {target_tool}' (e.g. 'MCP · activity_search') so the operator sees what MCP op was invoked instead of the generic 'close_mcp_call'. Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [plan state sync across surfaces]** — three plan consumers (PlanSection /lead, PlanDayStrip cockpit, PlanGraphView /graph) read independently. Generate from one → others stay stale. Risk: operator generates duplicates because cockpit shows 'no plan yet' even though plan exists. Add: (1) visibilitychange refresh on PlanDayStrip + PlanGraphView, (2) window event 'comeketo:plan-changed' dispatch + listen, (3) chat send completion dispatches event when tool calls touch plan, (4) 'Edit plan →' link in cockpit when plan exists.
- **after [plan state sync across surfaces]** — done. PlanDayStrip + PlanGraphView now listen for window 'comeketo:plan-changed' events + visibilitychange (re-fetch on tab focus). PlanDayStrip dispatches the event after its own Generate-plan action. ChatPanel send-completion scans tool calls for plan-mutating tools (generate_seven_day_plan, generate_plans_for_leads, approve_and_fire_plans) and dispatches the event with the affected lead_ids. Plus: cockpit's with-plan card now shows 'edit plan →' link to /lead/[id]/graph (clearer affordance vs accidental duplicate-create). PlanGraphView clears its simulation state on plan change so stale verdicts don't paint over a new graph. Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [PlanPageBeacon — same-window plan-change dispatch]** — close the gap from the previous fix: server-action forms on /lead/[id] (generate, approve, pause, kill, refine, day touch edits) can't dispatch window events directly. Add a tiny client beacon that watches the rendered plan fingerprint (id+status+generated_at+per-day approval_status) and dispatches comeketo:plan-changed when the fingerprint shifts between renders. Mount on /lead/[id] Plan tab + /lead/[id]/graph. Now cockpit + graph sync in real time within the same window, no tab-focus required.
- **after [PlanPageBeacon]** — done. NEW src/app/lead/[id]/PlanPageBeacon.tsx — client component that hashes the rendered plan (id+status+generated_at+per-day approval_status) and dispatches comeketo:plan-changed when the hash shifts between renders. Mounted in /lead/[id]/page.tsx + /lead/[id]/graph/page.tsx. Now ANY server-action mutation (generate, approve, pause, kill, refine whole plan, refine day, edit touch, add touch, delete touch, mark sent, skip day, approve all, approve and run) triggers a re-render → fingerprint shift → event → cockpit + graph re-fetch. Same-window real-time sync. Skips initial mount so navigation doesn't spam. Typecheck ✅, 26/26 tests ✅, build ✅.
- **before [analytics buildout — render 7 missing chart views]:** `/analytics` strip + Source Channels render, but the 5 wired snapshots (seller_performance, win_loss, revenue_trends, booking_lead_time, cohort) sit on disk with only `data ready` pills. Two views from the legacy app (Pipeline funnel, Conversation intel) have no dedicated rendering. Plan: render all 7 from existing snapshots only — no live Close API, no Python ports, no `lib/close.ts` edits. Each chart labels `_meta.generated_at`, degrades gracefully if missing, matches "Leads by source · top 12" aesthetic. Workflow `cb3153a0899c025209d2ccb34de03c44`, branch `archon/thread-b7dbe237` (in worktree — using as-is, plan's `analytics-buildout` name doesn't apply).

- **after [analytics buildout — plan setup]:** Setup phase only — no code changes. Plan parsed (inline, not a `.md` file beyond the workflow's `plan.md`). Branch decision: stay on worktree branch `archon/thread-b7dbe237`. Snapshot shapes inspected: 5 of 7 target views map cleanly to snapshots already on disk; **Conversation Intel has no snapshot** (manifest lists `build_conversation_intelligence.py` as path-only, output not wired) — flagged in plan-context as conditional/omittable per CLAUDE.md "no building without functionality" rule. Pattern targets identified: `cmk-an-channels` / `cmk-an-channel-bar` / `cmk-an-fam-*` for bar primitives; `cmk-an-card-{tone}` for cards; section header with `snapshot {fmtDate(...)}` already established at line 192–198 of `src/app/analytics/page.tsx`. Validation pipeline: `npm run typecheck && npm run lint && npm run test && npm run build`. Artifact at `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/plan-context.md`. Next: `archon-confirm-plan` to resolve Conversation Intel feasibility before implementation.

- **before [analytics buildout — plan confirm]:** Plan written but not validated against current repo state — patterns at `page.tsx:13/30/54-67/159-162/167-174/192-198/200-225/210/282-364`, snapshot shapes, target file existence, and validation commands all still need to be checked before implementation begins. Conversation Intel feasibility unresolved.
- **after [analytics buildout — plan confirm]:** ✅ PROCEED. All 8 pattern lines in `page.tsx` verified intact. All 5 snapshots present in `src/data/analytics/` with the keys the plan needs (`win_loss.funnel`, `revenue.yoy_comparison`, `revenue.deal_size_percentiles`, `revenue.monthly_trend`, `booking_lead_time.histogram` + `urgency_segments`, `cohort.conversion_curves` + `recent_cohort_health`, `seller_perf.owner_profiles` + `global_metrics`). Target dir `src/components/analytics/` does not yet exist (clean for CREATE). Validation commands all map to `npm run …` not `bun run …` — implementation must use npm. **Conversation Intel resolved: OMIT.** No `conversation_intel_snapshot.json` on disk; `win_loss.time_patterns` / `top_win_profiles` are deal-pattern signals, not conversation signals — labeling them "conversation intel" would be dishonest. Drop the surface entirely; remove `"Conversation intel"` from `missingViews` at `page.tsx:132`. Sprint scope settles at **6 chart views** (Owner Performance, Win-Loss, Revenue & Growth, Lead Time, Cohort, Pipeline Funnel). Confirmation artifact at `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/plan-confirmation.md`. Next: `archon-implement-tasks`.

- **before [analytics buildout — implement]:** Execute 12-task plan: types file → swap inline casts to typed imports → 6 inline chart sections (Owner Performance, Pipeline Funnel, Win/Loss breakdown, Revenue & Growth, Lead Time, Cohort) → CSS additions → dataset pills sync → validation. Conversation Intel resolved as OMIT in confirm step (no inert UI). Mirror "Leads by source · top 12" aesthetic; honest snapshot date pills; graceful empty states.

- **after [analytics buildout — implement]:** Done. `/analytics` now renders 6 new chart sections inline in `src/app/analytics/page.tsx` (Owner Performance · top 6, Pipeline Funnel · all-time, Win/Loss breakdown · 4 cuts in 2×2 grid, Revenue & Growth · YoY card + 5 percentile cards + 24-month trend bars + revenue concentration, Booking Lead Time · histogram + urgency triptych + by event type, Acquisition Cohorts · conversion curves + 12-row heatmap + best/worst summary). New file `src/lib/analytics-types.ts` (~370 lines) carries strict shapes for all 7 snapshots; page.tsx swapped 4 inline `as Snap & {…}` casts for `as unknown as TypedSnapshot`. New CSS appended to `src/app/globals.css` (between L322 `cmk-an-pill-missing` and the next section): bar tones (`cmk-an-bar-{sage|sky|peach|lavender|lemon|neutral}`), `cmk-an-empty`, full funnel kit (`cmk-an-funnel*` incl. tone-tinted bars and drop-off arrows), `cmk-an-wl-{grid,sub,h3,row}` for the 2-col win/loss grid, revenue layout (`cmk-an-rev-top`, `cmk-an-rev-yoy*`, `cmk-an-percentile-{strip,card,num}`), monthly-trend vertical bars (`cmk-an-trend-{row,col,bar*,label}` + `cmk-an-trend-grow` keyframe), `cmk-an-urgency-strip`, cohort heatmap table (`cmk-an-cohort-{table*,cohort,num,cell,empty}`). Datasets array updated: `seller_perf`, `win_loss`, `revenue`, `lead_time`, `cohort` flipped to `has_view: true`; `upcoming` left at `false` (intentional — surfaced via metric strip + intel signals only). `missingViews` emptied (`Pipeline funnel` → built; `Conversation intel` → OMIT per confirm decision). Bottom blurb updated to honestly call out the Conversation Intel omission. **Validation:** `npm run typecheck` adds zero new errors over the 11-error baseline (none in analytics files); analytics-types.ts and analytics/page.tsx both confirmed in tsc's file list with clean output. **Pre-existing tooling gaps (not this sprint's to fix):** `next lint` removed in Next 16, `vitest` not installed in this worktree's node_modules, `npm run build` fails on a pre-existing `SettingsActionState` import error in the `/settings` Server Action wiring (parallel agent's surface). **Watch items for `npm run fresh` browser check:** (a) verify `cmk-an-fam-other` fallback fires correctly for any `by_source_family` keys not yet tokenized — current snapshot keys all match existing tokens, no fallback needed; (b) cohort heatmap intensity scaling caps at 75% color-mix opacity, looks tasteful in a static check but worth eyeballing; (c) monthly-trend bars may compress visually on narrow screens — flex children are 1fr each, no min-width set; (d) the YoY card shares vertical alignment with the percentile strip via `align-items: stretch` — should look cohesive but verify the YoY card doesn't dwarf the percentile cards in height when the YoY label wraps.

- **before [analytics buildout — validate]:** Run typecheck + lint + test + build per validation pipeline. Implement-after-tap flagged 4 baseline tooling gaps (no node_modules, vitest missing, lint no-op, build error on `SettingsActionState`); validating means confirming none of those are analytics-introduced and that real validation surfaces the truth.
- **after [analytics buildout — validate]:** ✅ All in-scope checks green. **Type check:** clean (0 errors) after `npm install` (worktree had no node_modules) + `rm -rf .next` (stale validator from a prior Next 15+ build was using async-params types against installed Next 14.2.35 sync-params handlers). All 11 baseline errors resolved. **Tests:** 12/12 pass across 6 files in 662ms. **Build:** clean — 20 routes, `/analytics` rendered as `○ Static` (203 B page, 96.2 kB First Load), `force-static` honored, JSON imports tree-shaken, no runtime fs reads. The previously-flagged `SettingsActionState` build error did not reproduce after the `.next` clear — was a build-cache artifact. **Lint:** baseline ⚠️ — project has no ESLint config, `next lint` enters interactive setup. Pre-existing condition (no `.eslintrc*`, no eslint deps in package.json), not introduced by analytics. Fix deferred to a tooling sprint per CLAUDE.md "don't add features beyond what the task requires." **Files modified by validation:** none in source — only environmental (`node_modules/` populated, `.next/` regenerated). Validation artifact at `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/validation.md`. Next phase: `archon-finalize-pr`.

- **before [analytics buildout — finalize PR]:** Validation green; commit + push + open PR against `main` from `archon/thread-b7dbe237`. No PR template in `.github/` or `docs/` — use the workflow's default body shape. Files to commit: 1 CREATE (`src/lib/analytics-types.ts`) + 5 UPDATE (`src/app/analytics/page.tsx`, `src/app/globals.css`, `_scaffold/{Goals,Problems,Global}.md`). Title: `Analytics: render the 6 missing chart views from existing snapshots`.
- **after [analytics buildout — finalize PR]:** ✅ PR #1 OPEN — https://github.com/RodbotCC/ComeketoAgent/pull/1 — base `main`, head `archon/thread-b7dbe237`, not draft. Commit `44cb648` (+1295 / -65 across 6 files). Body sections: Summary, Changes table, Sections Rendered table (6 + 1 OMIT row), Validation, Implementation Notes (5 deviations + tooling baseline + out-of-scope). Branch first-time push to remote (created `origin/archon/thread-b7dbe237` with upstream tracking). PR-ready artifact at `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/pr-ready.md`. **Watch items:** (i) Vercel deploys only on `main` — this PR doesn't deploy, branch deploys are off (per CLAUDE.md "live site intentionally behind the working tree"); (ii) lint baseline still red on the PR if any GH Action runs — there's no CI config in this repo, so no auto-checks will run, but if Jake adds CI later, lint will need its own setup sprint first; (iii) `.next/types/validator.ts` drift will resurface on any reviewer's local checkout that ran a different Next major against the worktree (`rm -rf .next && npm run build` is the workaround). **Next move (land-in-position):** the most blocking-removal next step is porting `build_source_channel_intelligence.py` from `_reference/` to a real Close-API live wire — it's the single largest gap between snapshot analytics and real-time analytics. Everything else queues behind it. Hand control back to Jake to either (a) review the PR, (b) green-light the Close-API source-channel port as the next sprint, or (c) trigger the parallel-agent fix for `/settings/actions.ts` `SettingsActionState` first if build-from-clean reproduces for him.

- **before [PR #1 comment-quality review]** — 2026-05-02: comment-quality agent auditing comments in changed files; artifact only, no product code touched.

- **before [PR #1 docs-impact review]** — 2026-05-02: docs-impact agent checking whether PR #1 (analytics chart sections) requires updates to CLAUDE.md / docs/ / agent definitions / README. Artifact only, no product code touched.
- **after [PR #1 docs-impact review]** — 2026-05-02: ✅ NO_CHANGES_NEEDED. PR is presentation-only (typed snapshot shapes + 6 chart sections + additive CSS) — introduces no new commands, scripts, env vars, deps, endpoints, or schema. Repo has no `docs/`, no `README.md`, no `.claude/agents/`, no `.archon/commands/` — the only docs are CLAUDE.md (orchestration rules) and Guardrails.md (product spec), and the PR doesn't touch either's surface. Artifact at `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/review/docs-impact-findings.md`. Future trigger to revisit: when a *second* analytics consumer starts importing from `src/lib/analytics-types.ts` or reusing the `cmk-an-bar-{tone}` palette, a short "Analytics primitives" doc note becomes worthwhile; today there's one consumer, so codifying conventions would be premature.
- **before [PR #1 code review]** — 2026-05-02: code-review agent reviewing PR #1 (analytics buildout) for CLAUDE.md compliance, bugs, primitive duplication. Output artifact: `review/code-review-findings.md`. Read-only — no product code touched.
- **after [PR #1 code review]** — 2026-05-02: Verdict **REQUEST_CHANGES** with 5 findings (2 HIGH / 2 MEDIUM / 1 LOW). High-impact bugs: (1) `cohort_grid.slice(0, 12)` returns the **oldest** 12 cohorts under a "most recent 12" heading — JSON is ordered oldest-first; recommend `[...].sort((a,b)=>b.cohort.localeCompare(a.cohort)).slice(0,12)`. (2) Pipeline funnel `↓ X% drop` arrows between Active/Won/Lost are semantically wrong — those are sibling disposition buckets, not sequential funnel stages; recommend keeping the arrow only for Total→Active. Mediums: `UrgencyCardSeg` local type duplicates `UrgencySegment` from the new analytics-types module; `_meta.window_days` typed optional on WinLoss + RevenueTrends snapshots but rendered unconditionally → mismatch with how SourceChannel correctly redeclares it required. CLAUDE.md compliance is otherwise clean: three-ledger taps present, Conversation Intel correctly omitted (not stubbed), `lib/close.ts` untouched, Python builders untouched, CSS additive only, `force-static` preserved, `analytics-types.ts` is a genuine new primitive (no duplicates with existing `src/lib/`). Artifact: `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/review/code-review-findings.md`.
- **after [PR #1 comment-quality review]** — 2026-05-02: artifact written to `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/review/comment-quality-findings.md`. Verdict: APPROVE w/ 1 HIGH (stale path in page.tsx header) + 1 MEDIUM (sprint-tied temporal framing) nit-fixes.
- **before [PR #1 test-coverage review]** — 2026-05-02: test-coverage agent reviewing PR #1. Read-only — no source touched. Output artifact: `review/test-coverage-findings.md`.
- **after [PR #1 test-coverage review]** — 2026-05-02: Verdict **APPROVE** (3 findings: 1 HIGH / 2 MEDIUM, none blocking). Scope was explicit "Test Files (0) — presentation-only." All flagged gaps are pre-existing patterns, not sprint-introduced regressions. **Finding 1 (HIGH, criticality 8):** seven snapshot JSONs cast via `as unknown as TypedSnapshot` with zero runtime validation — Python rebuilders ship from a separate repo so TS cannot catch field renames; drift would render silent NaN/0% bars. Recommended: one bare-vitest test per snapshot under `src/data/analytics/*.test.ts` asserting required keys + types (~7 × 30 lines, no new deps). **Finding 2 (MED, 5):** the page-private pure helpers `fmtCents` / `pct` / `safeMax` / `fmtDate` (page.tsx:44-67) are reused 8+ times but live where they can't be tested; recommend extracting to `src/lib/analytics-fmt.ts`. Boundaries worth pinning: `fmtCents(100_000_000) = "$1.0M"`, `safeMax([]) = 1`, `pct(0, true) = "0.0%"` (no `+`). **Finding 3 (MED, 4):** `buildIntelSignals` (page.tsx:843-923) — 6 conditional branches with hardcoded thresholds, untested; co-required follow-up to Finding 2's extraction. **Triage for SUMMARY aggregation:** Finding 1 = must-do follow-up sprint; Finding 2 = should-do (cheap, enables Finding 3); Finding 3 = nice-to-have. Artifact: `artifacts/runs/cb3153a0899c025209d2ccb34de03c44/review/test-coverage-findings.md`.
- **before [MCP auth fix: Bearer → Basic]** — diagnosed live: closeMcpListTools was 401 'invalid_token' because Close MCP rejects Bearer api_key. Probed 6 schemes; only Authorization: Basic base64(api_key:) returned 200. Same pattern as Close REST. Fix: when CLOSE_MCP_AUTH_HEADER is blank, fall back to Basic instead of Bearer. CLOSE_MCP_AUTH_HEADER override stays for any future OAuth flow.
- **after [MCP auth fix]** — done. closeMcpListTools returns 34 tools end-to-end; closeMcpCallTool('org_info') returns Jake's user + Openera Automation org. Patched authHeader() in src/lib/close-mcp.ts to use Basic base64(api_key:) when CLOSE_MCP_AUTH_HEADER is blank (Bearer api_key returned 401 invalid_token). CLOSE_MCP_AUTH_HEADER override remains for OAuth flows. Diagnostic scripts left in _scaffold/ (mcp-diag.ts, mcp-auth-probe.ts, mcp-end-to-end.ts) for future MCP debugging. Typecheck ✅, 26/26 tests ✅, build ✅.

- **after [proposals plates + workbench overlay]** — 2026-05-05: `/proposals` now scans as compact status plates instead of rendering every draft inline. Clicking a plate opens a large proposal workbench overlay: left side = selectable panels (overview, each touch, add touch, regenerate); top right = the active editor/details for the selected panel; bottom right = a functional AI change request panel wired to existing day/plan refine actions. Existing approve / needs review / mark sent / skip / edit touch / add touch / graph / lead paths stay real. Typecheck ✅, build ✅.

- **before [plan-discovery split]** — 2026-05-05: Move lead intelligence/profile/known/unknowns/stop-condition material out of the Plan tab and into Discovery. Plan should become the sequence/execution surface only. Risk: keep one source of truth from the generated plan and avoid fake discovery data; Discovery should render real plan context when available and honest empty states when not.
- **after [plan-discovery split]** — 2026-05-05: Done. Plan tab now renders only the cycle header/meta, seven-day day cards, and execution controls. Discovery now defaults to a real Profile tab that renders the plan-derived strategy, current lead read, best next question, knowns, unknowns, operating note, and stop conditions, with links to run discovery scan or open the plan. Typecheck ✅, build ✅.

- **before [lead assets + rich-text capability design]** — 2026-05-05: Explore how to add reusable/lead-specific assets (HTML/PNG/etc.) and Close-safe rich-text note/comment support into Lead Box, chat, delegations, proposal/card decision surfaces. Need atomization before implementation because this crosses storage, UI, tool permissions, and outbound execution.

- **after [lead assets + rich-text capability design]** — 2026-05-05: Design call recorded. Yes: introduce an Asset Library with global and lead-scoped assets, plus Close-safe rich XHTML rendering. Current repo already has rich workflow email support (`body_html`) and chat/intake upload plumbing, but not reusable sendable assets. Recommended architecture: Supabase Storage for asset binaries + metadata table, future mirror into `_leads/.../assets/` on `leads-data`; plans/delegations/proposals/workflows reference asset IDs and render channel-specific outputs.

- **before [asset library atom 1 — storage + Lead Box assets]** — 2026-05-05: Implement the first real asset slice: Supabase asset metadata/storage, server helpers, upload/list/delete API/actions, and a Lead Box asset panel. Scope is asset library foundation only; no fake send/attach buttons until outbound execution understands asset IDs.

- **after [asset library atom 1 — storage + Lead Box assets]** — 2026-05-05: Done. Added private Supabase `assets` bucket + `lead_assets` metadata migration, server helper `src/lib/assets.ts`, upload route `/api/assets/upload`, download/delete server actions, `assets` loading in Lead Box data, and a new `LeadAssetsPanel` on `/lead/[id]/box`. It reuses the Intake visual language but separates purpose: Intake = what the agent reads; Assets = what Andre/agents can reuse/select for future customer-facing materials. Typecheck ✅, build ✅ after clearing stale `.next`. Migration still needs applying before runtime upload/list works.

- **before [lead-scoped delegations subtab]** — 2026-05-05: Add a Delegations lead sub-tab that opens chat/delegations already scoped to the current lead/Box, not the last cockpit state. Need inspect current chat lead-mode URL/state support before editing; avoid fake routing.
- **after [lead-scoped delegations subtab]** — 2026-05-05: Done. Lead subnav now includes a Delegations link that opens `/chat?lead={lead_id}&from=lead-box`. Chat consumes the lead URL as authoritative after layout hydration, switches to lead mode, loads the scope pane, clears stale active thread/messages/composer state, and skips auto-selecting the last global thread on deep-link entry. Typecheck ✅, build ✅.

- **before [GPT lead research folder + prompt]** — 2026-05-05: Create a repo-local research drop zone for external ChatGPT agents using Close MCP + GitHub MCP. Scope: Andre-only real Close leads, newest 50 plus previously discovered open chains, per-client directories, full comms/transcripts/profile capture. This is intentionally not app-wired yet; goal is to get research pumping.
- **after [GPT lead research folder + prompt]** — 2026-05-05: Done. Added `lead-research/` with `README.md`, `GPT_AGENT_PROMPT.md`, `clients/.gitkeep`, and `sweeps/.gitkeep`. Contract: GPT agents write docs only under `lead-research/`, one folder per client using `{close_lead_id}__{client_slug}`, Andre-only, newest 50 + already discovered open/paused chains, full Close comms/transcripts/profile capture, sweep summaries under `lead-research/sweeps/`. No app wiring touched.

- **before [Andre-only Leads index cleanup]** — 2026-05-05: Remove the owner segmented control from `/leads` so the Lead Box index is Andre's working universe by default. Preserve search/status filtering. Do not remove backend owner guards elsewhere.
- **after [Andre-only Leads index cleanup]** — 2026-05-05: Done. `/leads` no longer renders or honors the Andre/Jake/All/Practice segmented owner switch. The page shows the full Close lead list by default, with total/shown counts plus search and status filtering. Typecheck ✅, build ✅.

- **stocktake [pre-demo polish queue]** — 2026-05-05: Jake did a full surface-by-surface friction audit. Catalog of the bugs/nags/placeholder pages lives in [Problems.md](Problems.md) under the matching `stocktake [surface-by-surface friction audit]` entry. Goals.md tracks the prioritized work order. **Not all queued for execution yet** — this is taking stock so nothing slips before the boss meeting.

  **P0 — destructive button hygiene (one bad misclick = bad day)**
  - Plan page action row: cluster verbs (constructive | regenerative | destructive); Kill needs distance + weight; remove the inline regenerate-form that lives two pixels from Kill.
  - Modal: `Rewrite plan` (nukes 7 days) sits at sibling weight to `Rewrite day` (nukes 1). Demote Rewrite plan visually OR add a half-beat of friction.

  **P1 — naming collisions + broken empty states (confuses new operators)**
  - Restraint metric collides across scopes (lead-level last 10 heartbeats vs player-level last 30d across all leads). Rename the player-level one — `Discipline · 30d` is the leading candidate.
  - Personal/Overview "Restraint · 30d" shows lonely "—" while siblings show numbers. Reads as broken. Use `0%` / `n/a` / dimmed default.
  - Plan page has THREE Approve buttons with different blast radius. Pick one primary, demote the others.

  **P2 — placeholder pages**
  - **Graph page rebuild (greenfield).** Currently the Plan page in smaller font + simulator chips bolted on. The simulator output IS the graph: which days fire, which gate, why each gate gated. Toggle assumptions to re-simulate. Decide what "Graph" means as a concept (Plan = artifact, Graph = behavior).
  - Numbered circles on Graph redundant with day labels (`01 02 03…` next to `DAY 1 DAY 2 DAY 3…`). Drop one numbering.
  - Plan page polish (after the action-row hygiene from P0). "Plan is stale" should be a soft yellow strip across the top of the cycle plan card, not a sentence floating between button clusters.

  **P3 — compression + cosmetics**
  - Box atomization: activity column becomes a lead-level rail; left side atomizes.
  - Slash-command palette: meta vs action divider (`/help /new /clear /rerun /rail` = meta; `/lead /heartbeat /today /morning` = action). Currently flat-listed.
  - 🟢 emoji in "🟢 SCORE tag distribution" header → CSS dot. Self-betrayal — Jake hates emojis.
  - Channel icons in Proposals card footers: filled vs outline (or tiny dot for "fired") to disambiguate "will touch" vs "already fired".
  - Trailhead-card status-pill asymmetry: pick one — dominant-status-wins-with-hover OR always-show-breakdown. Apply globally.
  - Write-actions need different visual register from read surfaces. "Enroll in Workflow (Close write)" guardrail copy is doing real safety work but looks identical to reads.
  - Day Modal: cluster navigation (Graph / Lead) separately from decision row (Approve / Needs review / Mark sent / Skip).

  **🌐 Top-right nav revitalization** — already underway. Tier 1 = global, Tier 2 = lead. Banked.
