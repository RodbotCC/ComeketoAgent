# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage

# next.js
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.logs/
*.log

# local env files — NEVER commit
.env
.env.*
!.env.example

# local settings + ledger files — written by the running app, operator-specific
.cmk-settings.json
.cmk-auxiliaries.json
.cmk-continuity.jsonl
.cmk-open-problems.jsonl

# Phantom app/ directory — Next dev sometimes regenerates an empty app/
# at the repo root after Server Action / dynamic route changes. The real
# Next.js source lives in src/app/.
/app/

# Reference dumps too large for git (we keep the markdown form)
_reference/close-api-docs.json
_reference/close-api-docs/

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
.vercel/
